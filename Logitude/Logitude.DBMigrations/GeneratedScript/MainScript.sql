-- General Script From ScreenField Unique index removal.sxml File
DECLARE
StartTime TIMESTAMP;
EndTime TIMESTAMP;
BEGIN
SAVEPOINT ScriptSavePoint;
StartTime := SYSTIMESTAMP;
BEGIN
DECLARE
i INTEGER;
BEGIN
SELECT COUNT(*) INTO i FROM user_indexes WHERE index_name = 'UC_SCREENFIELDS_SRN_F';
IF i > 0 THEN
EXECUTE IMMEDIATE 'drop index UC_SCREENFIELDS_SRN_F';
END IF;
END;
END;
EndTime:= SYSTIMESTAMP;
BEGIN
DECLARE ScriptBody NCLOB;
BEGIN
ScriptBody := 'DECLARE
i INTEGER;
BEGIN
SELECT COUNT(*) INTO i FROM user_indexes WHERE index_name = ''UC_SCREENFIELDS_SRN_F'';
IF i > 0 THEN
EXECUTE IMMEDIATE ''drop index UC_SCREENFIELDS_SRN_F'';
END IF;
END;';
INSERT INTO "DBSCRIPTSHISTORY"("SXMLFILENAME", "EXECUTIONDATE", "SCRIPTBODY", "ELAPSEDTIMEINMS", "HASHVALUE", "VERSION")VALUES('ScreenField Unique index removal.sxml', SYSDATE, ScriptBody, EXTRACT(DAY FROM(EndTime - StartTime) * 24 * 60 * 60 * 1000), '34160d36613b33e37d2248815e5bcbfa', 1);
END;
END;
EXCEPTION
WHEN OTHERS THEN
ROLLBACK TO ScriptSavePoint;
COMMIT;
END;

