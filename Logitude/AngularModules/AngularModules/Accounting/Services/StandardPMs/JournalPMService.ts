//------------------------------------------------------------------------------
// <auto-generated>
//     Angular 9.1.1
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { Injectable } from '@angular/core';
import { HttpClient, HttpResponse } from '@angular/common/http';
import { catchError, map } from 'rxjs/operators';
import { defer, of } from 'rxjs';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {JournalPM} from '../../EntityPMs/JournalPM';

import {JournalLinePM} from '../../EntityPMs/JournalLinePM';
import {JournalReconcilePM} from '../../EntityPMs/JournalReconcilePM';
import {JournalExternalReconcilePM} from '../../EntityPMs/JournalExternalReconcilePM';
import {JournalValidator} from '../../Validators/JournalValidator';

@Injectable()

export class JournalPMService {
 private _http: HttpClient;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.HttpClient;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/journals';      
    }

	get(id: string) {       

		var callTime = new Date();		

		return defer(() => {
			return this._http.get(this._apiUrl + '/getsingle?' + 'id=' + id, ServiceHelper.GetHttpFullHeaders())
				.pipe(
					map((response: HttpResponse<any>) => {
						var pm = response.body;
				
						var entity: JournalPM;
						if (pm) {
							entity = this.MapJsonToEntityPM(pm);
						}

						var serviceResponse: ServiceResponse = new ServiceResponse();
						serviceResponse.Result = entity;
              
						var servertime = response.headers.get('ServerExecutionTime');
						PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Journal", "GetSinglePM", 'id=' + id);
				 
						return serviceResponse;

					}),
					
					catchError(ServiceHelper.HandleServiceError));
		});                    
	}

	insert(entityPM: JournalPM) {
 
		var callTime = new Date();  
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();                
			var errorsArray = validator.Validate("Journal", entityPM);

			var customValidator :JournalValidator = new JournalValidator();
			var validationErrorsArr = customValidator.Validate(entityPM);
			if(validationErrorsArr)
			{
				errorsArray = errorsArray.concat(validationErrorsArr);
			}

			if (errorsArray.length == 0) {

				var mappedEntity: JournalPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.post(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {

							var pm = response.body;
							if (pm) {
								var mappedResult: JournalPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}						

							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Journal", "SaveChanges", "");                    
												                             
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

	update(entityPM: JournalPM) {

		var callTime = new Date();     
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();               
			var errorsArray = validator.Validate("Journal", entityPM);

			var customValidator :JournalValidator = new JournalValidator();
			var validationErrorsArr = customValidator.Validate(entityPM);
			if(validationErrorsArr)
			{
				errorsArray = errorsArray.concat(validationErrorsArr);
			}

			if (errorsArray.length == 0) {

				var mappedEntity: JournalPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.put(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {
                 
							var pm = response.body;
							if (pm) {
								var mappedResult: JournalPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}
							 
							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Journal", "SaveChanges", "");                    
					                           
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: JournalPM = null) {

         
        if (!entityPM) {
            
            entityPM = new JournalPM();
			entityPM.DisableMarkAsDirty = true;
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapJournalLines(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapJournalReconciles(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapJournalExternalReconciles(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.JournalLines = [];
            for (var item in entityPM.JournalLines) {
            var myJournalLinePM = entityPM.JournalLines[item];
            var newJournalLinePM: JournalLinePM = this.clone(myJournalLinePM);
						
							 
            entityPM.OldEntityPM.JournalLines.push(newJournalLinePM);
            }
			   			   			   
            entityPM.OldEntityPM.JournalReconciles = [];
            for (var item in entityPM.JournalReconciles) {
            var myJournalReconcilePM = entityPM.JournalReconciles[item];
            var newJournalReconcilePM: JournalReconcilePM = this.clone(myJournalReconcilePM);
						
							 
            entityPM.OldEntityPM.JournalReconciles.push(newJournalReconcilePM);
            }
			   			   			   
            entityPM.OldEntityPM.JournalExternalReconciles = [];
            for (var item in entityPM.JournalExternalReconciles) {
            var myJournalExternalReconcilePM = entityPM.JournalExternalReconciles[item];
            var newJournalExternalReconcilePM: JournalExternalReconcilePM = this.clone(myJournalExternalReconcilePM);
						
							 
            entityPM.OldEntityPM.JournalExternalReconciles.push(newJournalExternalReconcilePM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
	    entityPM.DisableMarkAsDirty = false;

        return entityPM;
    }

    MapJournalLines(entityPM: JournalPM, jsonPM: any, mapParent: boolean = true) {

        var oldJournalLines: JournalLinePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldJournalLines = entityPM.OldEntityPM.JournalLines;
        }

        entityPM.JournalLines = new Array<JournalLinePM>();
        for (var item in jsonPM.JournalLines) {
            var jItem = jsonPM.JournalLines[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newJournalLinePM: JournalLinePM;
	  
            if (mapParent) {
                newJournalLinePM = new JournalLinePM(entityPM);
            }
            else
            {
                newJournalLinePM = new JournalLinePM(null);
            }
 			newJournalLinePM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newJournalLinePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newJournalLinePM.UniqueKey = Guid.newGuid();
                newJournalLinePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newJournalLinePM.OldEntityPM = this.clone(newJournalLinePM);

				
            }
            else {
                if (newJournalLinePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newJournalLinePM.ChangeSetOp = "Update";
                }
                else {
                        newJournalLinePM.ChangeSetOp = "Insert";
                }
 
                newJournalLinePM.OldEntityPM = null;
                newJournalLinePM.EntityParentPM = null;
            }
			 newJournalLinePM.DisableMarkAsDirty = false;
			 newJournalLinePM.IsDirty = false;
            entityPM.JournalLines.push(newJournalLinePM);
        }
        if (oldJournalLines) {
            
            for (var itemKey in oldJournalLines) {
                if (entityPM.JournalLines.filter(p=> p.UniqueKey === oldJournalLines[itemKey].UniqueKey).length === 0) {
				
                    if (oldJournalLines[itemKey]) {
                        //oldJournalLines[itemKey].ChangeSetOp = "Delete";
                        //entityPM.JournalLines.push(oldJournalLines[itemKey]);
						var oldItemJson = oldJournalLines[itemKey];
                        var deletedPM: JournalLinePM = new JournalLinePM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.JournalLines.push(deletedPM);
                    }
                }
            }
        }
    }
    MapJournalReconciles(entityPM: JournalPM, jsonPM: any, mapParent: boolean = true) {

        var oldJournalReconciles: JournalReconcilePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldJournalReconciles = entityPM.OldEntityPM.JournalReconciles;
        }

        entityPM.JournalReconciles = new Array<JournalReconcilePM>();
        for (var item in jsonPM.JournalReconciles) {
            var jItem = jsonPM.JournalReconciles[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newJournalReconcilePM: JournalReconcilePM;
	  
            if (mapParent) {
                newJournalReconcilePM = new JournalReconcilePM(entityPM);
            }
            else
            {
                newJournalReconcilePM = new JournalReconcilePM(null);
            }
 			newJournalReconcilePM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newJournalReconcilePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newJournalReconcilePM.UniqueKey = Guid.newGuid();
                newJournalReconcilePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newJournalReconcilePM.OldEntityPM = this.clone(newJournalReconcilePM);

				
            }
            else {
                if (newJournalReconcilePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newJournalReconcilePM.ChangeSetOp = "Update";
                }
                else {
                        newJournalReconcilePM.ChangeSetOp = "Insert";
                }
 
                newJournalReconcilePM.OldEntityPM = null;
                newJournalReconcilePM.EntityParentPM = null;
            }
			 newJournalReconcilePM.DisableMarkAsDirty = false;
			 newJournalReconcilePM.IsDirty = false;
            entityPM.JournalReconciles.push(newJournalReconcilePM);
        }
        if (oldJournalReconciles) {
            
            for (var itemKey in oldJournalReconciles) {
                if (entityPM.JournalReconciles.filter(p=> p.UniqueKey === oldJournalReconciles[itemKey].UniqueKey).length === 0) {
				
                    if (oldJournalReconciles[itemKey]) {
                        //oldJournalReconciles[itemKey].ChangeSetOp = "Delete";
                        //entityPM.JournalReconciles.push(oldJournalReconciles[itemKey]);
						var oldItemJson = oldJournalReconciles[itemKey];
                        var deletedPM: JournalReconcilePM = new JournalReconcilePM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.JournalReconciles.push(deletedPM);
                    }
                }
            }
        }
    }
    MapJournalExternalReconciles(entityPM: JournalPM, jsonPM: any, mapParent: boolean = true) {

        var oldJournalExternalReconciles: JournalExternalReconcilePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldJournalExternalReconciles = entityPM.OldEntityPM.JournalExternalReconciles;
        }

        entityPM.JournalExternalReconciles = new Array<JournalExternalReconcilePM>();
        for (var item in jsonPM.JournalExternalReconciles) {
            var jItem = jsonPM.JournalExternalReconciles[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newJournalExternalReconcilePM: JournalExternalReconcilePM;
	  
            if (mapParent) {
                newJournalExternalReconcilePM = new JournalExternalReconcilePM(entityPM);
            }
            else
            {
                newJournalExternalReconcilePM = new JournalExternalReconcilePM(null);
            }
 			newJournalExternalReconcilePM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newJournalExternalReconcilePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newJournalExternalReconcilePM.UniqueKey = Guid.newGuid();
                newJournalExternalReconcilePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newJournalExternalReconcilePM.OldEntityPM = this.clone(newJournalExternalReconcilePM);

				
            }
            else {
                if (newJournalExternalReconcilePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newJournalExternalReconcilePM.ChangeSetOp = "Update";
                }
                else {
                        newJournalExternalReconcilePM.ChangeSetOp = "Insert";
                }
 
                newJournalExternalReconcilePM.OldEntityPM = null;
                newJournalExternalReconcilePM.EntityParentPM = null;
            }
			 newJournalExternalReconcilePM.DisableMarkAsDirty = false;
			 newJournalExternalReconcilePM.IsDirty = false;
            entityPM.JournalExternalReconciles.push(newJournalExternalReconcilePM);
        }
        if (oldJournalExternalReconciles) {
            
            for (var itemKey in oldJournalExternalReconciles) {
                if (entityPM.JournalExternalReconciles.filter(p=> p.UniqueKey === oldJournalExternalReconciles[itemKey].UniqueKey).length === 0) {
				
                    if (oldJournalExternalReconciles[itemKey]) {
                        //oldJournalExternalReconciles[itemKey].ChangeSetOp = "Delete";
                        //entityPM.JournalExternalReconciles.push(oldJournalExternalReconciles[itemKey]);
						var oldItemJson = oldJournalExternalReconciles[itemKey];
                        var deletedPM: JournalExternalReconcilePM = new JournalExternalReconcilePM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.JournalExternalReconciles.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: JournalPM;
			entityPM = new JournalPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
