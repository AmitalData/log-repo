import { PriceCheckDetailsPipe } from './price-check-details.pipe';

describe('PriceCheckDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceCheckDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
