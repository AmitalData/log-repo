//file not found! child composition TariffLineExpirationDate
//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import {Injectable} from '@angular/core';
import {Http, Headers} from '@angular/http';
import {Observable}     from 'rxjs/Rx';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {TariffPM} from '../../EntityPMs/TariffPM';

import {TariffVersionPM} from '../../EntityPMs/TariffVersionPM';

import {TariffLinePM} from '../../EntityPMs/TariffLinePM';
import {TariffVersionAllInChargePM} from '../../EntityPMs/TariffVersionAllInChargePM';
import {TariffLineExpirationDatePM} from '../../EntityPMs/TariffLineExpirationDatePM';
import {TariffPMInitService} from '../../EntityPMInitServices/TariffPMInitService';
import {TariffValidator} from '../../Validators/TariffValidator';

@Injectable()

export class TariffPMService {
 private _http: Http;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.Http;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/tariffs';      
    }

 get(id: string) {
         
         
        var authHeader = new Headers();
        authHeader.append('Token', SessionInfo.Token);
        var callTime = new Date();		
		 return Observable.defer(() => {
                return this._http.get(this._apiUrl+'/getsingle?'+'id=' + id, {
                    headers: authHeader
                }).map(response => {
                    var pm = response.json();

                   
					
                    var entity: TariffPM;
					if(pm)
					{
                      entity = this.MapJsonToEntityPM(pm);
                      TariffPMInitService.InitValues(entity, false);
                      TariffPMInitService.ApplyUIPoperties(entity, false);
                    }

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
                serviceResponse.Result = entity;
              
			    var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Tariff", "GetSinglePM", 'id=' + id);
				 
                return serviceResponse;

            }).catch(ServiceHelper.HandleServiceError);
            });                    
    }

	 insert(entityPM: TariffPM) {
 
        var callTime = new Date();        
        return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
                 
                var errorsArray = validator.Validate("Tariff", entityPM);
                var customValidator :TariffValidator = new TariffValidator();
                var validationErrorsArr = customValidator.Validate(entityPM);
				if(validationErrorsArr)
				{
					errorsArray = errorsArray.concat(validationErrorsArr);
				}
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: TariffPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.post(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  TariffPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							}
							

                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Tariff", "SaveChanges", "");                    
												 
                            
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );
    }

    update(entityPM: TariffPM) {

            var callTime = new Date();         
            return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
               
                var errorsArray = validator.Validate("Tariff", entityPM);
                var customValidator :TariffValidator = new TariffValidator();
                var validationErrorsArr = customValidator.Validate(entityPM);
				if(validationErrorsArr)
				{
					errorsArray = errorsArray.concat(validationErrorsArr);
				}
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: TariffPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.put(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {
                 

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  TariffPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							 }
							 
                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Tariff", "SaveChanges", "");                    
					                           
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );

    }

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: TariffPM = null) {

         
        if (!entityPM) {
            
            entityPM = new TariffPM();
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapTariffVersions(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapActiveVersions(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapDeletedLinesExpirationDates(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.TariffVersions = [];
            for (var item in entityPM.TariffVersions) {
            var myTariffVersionPM = entityPM.TariffVersions[item];
            var newTariffVersionPM: TariffVersionPM = this.clone(myTariffVersionPM);
						
                newTariffVersionPM.TariffLines = [];
                for (var k in myTariffVersionPM.TariffLines) {
				    var myTariffLinePM =myTariffVersionPM.TariffLines[k];
				    var newTariffLinePM=this.clone(myTariffVersionPM.TariffLines[k]);
                    newTariffVersionPM.TariffLines.push(newTariffLinePM);

					                 }
                newTariffVersionPM.TariffAllInCharges = [];
                for (var k in myTariffVersionPM.TariffAllInCharges) {
				    var myTariffVersionAllInChargePM =myTariffVersionPM.TariffAllInCharges[k];
				    var newTariffVersionAllInChargePM=this.clone(myTariffVersionPM.TariffAllInCharges[k]);
                    newTariffVersionPM.TariffAllInCharges.push(newTariffVersionAllInChargePM);

					                 }
							 
            entityPM.OldEntityPM.TariffVersions.push(newTariffVersionPM);
            }
			   			   			   
            entityPM.OldEntityPM.ActiveVersions = [];
            for (var item in entityPM.ActiveVersions) {
            var myTariffVersionPM = entityPM.ActiveVersions[item];
            var newTariffVersionPM: TariffVersionPM = this.clone(myTariffVersionPM);
						
                newTariffVersionPM.TariffLines = [];
                for (var k in myTariffVersionPM.TariffLines) {
				    var myTariffLinePM =myTariffVersionPM.TariffLines[k];
				    var newTariffLinePM=this.clone(myTariffVersionPM.TariffLines[k]);
                    newTariffVersionPM.TariffLines.push(newTariffLinePM);

					                 }
                newTariffVersionPM.TariffAllInCharges = [];
                for (var k in myTariffVersionPM.TariffAllInCharges) {
				    var myTariffVersionAllInChargePM =myTariffVersionPM.TariffAllInCharges[k];
				    var newTariffVersionAllInChargePM=this.clone(myTariffVersionPM.TariffAllInCharges[k]);
                    newTariffVersionPM.TariffAllInCharges.push(newTariffVersionAllInChargePM);

					                 }
							 
            entityPM.OldEntityPM.ActiveVersions.push(newTariffVersionPM);
            }
			   			   			   
            entityPM.OldEntityPM.DeletedLinesExpirationDates = [];
            for (var item in entityPM.DeletedLinesExpirationDates) {
            var myTariffLineExpirationDatePM = entityPM.DeletedLinesExpirationDates[item];
            var newTariffLineExpirationDatePM: TariffLineExpirationDatePM = this.clone(myTariffLineExpirationDatePM);
						
							 
            entityPM.OldEntityPM.DeletedLinesExpirationDates.push(newTariffLineExpirationDatePM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
        return entityPM;
    }

    MapTariffVersions(entityPM: TariffPM, jsonPM: any, mapParent: boolean = true) {

        var oldTariffVersions: TariffVersionPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldTariffVersions = entityPM.OldEntityPM.TariffVersions;
        }

        entityPM.TariffVersions = new Array<TariffVersionPM>();
        for (var item in jsonPM.TariffVersions) {
            var jItem = jsonPM.TariffVersions[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newTariffVersionPM: TariffVersionPM;
	  
            if (mapParent) {
                newTariffVersionPM = new TariffVersionPM(entityPM);
            }
            else
            {
                newTariffVersionPM = new TariffVersionPM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newTariffVersionPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newTariffVersionPM.UniqueKey = Guid.newGuid();
                newTariffVersionPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newTariffVersionPM.OldEntityPM = this.clone(newTariffVersionPM);
 

                this.MapTariffLines(newTariffVersionPM, jItem, mapParent);
                newTariffVersionPM.OldEntityPM.TariffLines = [];
                for (var k in newTariffVersionPM.TariffLines) {
                    //var clonedInside = this.clone(newTariffVersionPM.TariffLines[k]);
                    newTariffVersionPM.OldEntityPM.TariffLines.push(newTariffVersionPM.TariffLines[k].OldEntityPM); // clone old TariffLines//
                }
 

                this.MapTariffAllInCharges(newTariffVersionPM, jItem, mapParent);
                newTariffVersionPM.OldEntityPM.TariffAllInCharges = [];
                for (var k in newTariffVersionPM.TariffAllInCharges) {
                    //var clonedInside = this.clone(newTariffVersionPM.TariffAllInCharges[k]);
                    newTariffVersionPM.OldEntityPM.TariffAllInCharges.push(newTariffVersionPM.TariffAllInCharges[k].OldEntityPM); // clone old TariffAllInCharges//
                }

				
            }
            else {
                if (newTariffVersionPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newTariffVersionPM.ChangeSetOp = "Update";
                }
                else {
                        newTariffVersionPM.ChangeSetOp = "Insert";
                }
 

                this.MapTariffLines(newTariffVersionPM, jItem, mapParent);
 

                this.MapTariffAllInCharges(newTariffVersionPM, jItem, mapParent);
 
                newTariffVersionPM.OldEntityPM = null;
                newTariffVersionPM.EntityParentPM = null;
            }
			
			 newTariffVersionPM.IsDirty = false;
            entityPM.TariffVersions.push(newTariffVersionPM);
        }
        if (oldTariffVersions) {
            
            for (var itemKey in oldTariffVersions) {
                if (entityPM.TariffVersions.filter(p=> p.UniqueKey === oldTariffVersions[itemKey].UniqueKey).length === 0) {
				
                    if (oldTariffVersions[itemKey]) {
                        //oldTariffVersions[itemKey].ChangeSetOp = "Delete";
                        //entityPM.TariffVersions.push(oldTariffVersions[itemKey]);
						var oldItemJson = oldTariffVersions[itemKey];
                        var deletedPM: TariffVersionPM = new TariffVersionPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
 

                        this.MapTariffLines(deletedPM, oldItemJson, mapParent);
 

                        this.MapTariffAllInCharges(deletedPM, oldItemJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.TariffVersions.push(deletedPM);
                    }
                }
            }
        }
    }
    MapTariffLines(entityPM: TariffVersionPM, jsonPM: any, mapParent: boolean = true) {

        var oldTariffLines: TariffLinePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldTariffLines = entityPM.OldEntityPM.TariffLines;
        }

        entityPM.TariffLines = new Array<TariffLinePM>();
        for (var item in jsonPM.TariffLines) {
            var jItem = jsonPM.TariffLines[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newTariffLinePM: TariffLinePM;
	  
            if (mapParent) {
                newTariffLinePM = new TariffLinePM(entityPM);
            }
            else
            {
                newTariffLinePM = new TariffLinePM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newTariffLinePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newTariffLinePM.UniqueKey = Guid.newGuid();
                newTariffLinePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newTariffLinePM.OldEntityPM = this.clone(newTariffLinePM);

				
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    newTariffLinePM.ChangeSetOp = "Delete";
                }
                else {
                    if (newTariffLinePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newTariffLinePM.ChangeSetOp = "Update";
                    }
                else {
                        newTariffLinePM.ChangeSetOp = "Insert";
                    }
                }
 
                newTariffLinePM.OldEntityPM = null;
                newTariffLinePM.EntityParentPM = null;
            }
			
			 newTariffLinePM.IsDirty = false;
            entityPM.TariffLines.push(newTariffLinePM);
        }
        if (oldTariffLines) {
            
            for (var itemKey in oldTariffLines) {
                if (entityPM.TariffLines.filter(p=> p.UniqueKey === oldTariffLines[itemKey].UniqueKey).length === 0) {
				
                    if (oldTariffLines[itemKey]) {
                        //oldTariffLines[itemKey].ChangeSetOp = "Delete";
                        //entityPM.TariffLines.push(oldTariffLines[itemKey]);
						var oldItemJson = oldTariffLines[itemKey];
                        var deletedPM: TariffLinePM = new TariffLinePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.TariffLines.push(deletedPM);
                    }
                }
            }
        }
    }
    MapTariffAllInCharges(entityPM: TariffVersionPM, jsonPM: any, mapParent: boolean = true) {

        var oldTariffAllInCharges: TariffVersionAllInChargePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldTariffAllInCharges = entityPM.OldEntityPM.TariffAllInCharges;
        }

        entityPM.TariffAllInCharges = new Array<TariffVersionAllInChargePM>();
        for (var item in jsonPM.TariffAllInCharges) {
            var jItem = jsonPM.TariffAllInCharges[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newTariffVersionAllInChargePM: TariffVersionAllInChargePM;
	  
            if (mapParent) {
                newTariffVersionAllInChargePM = new TariffVersionAllInChargePM(entityPM);
            }
            else
            {
                newTariffVersionAllInChargePM = new TariffVersionAllInChargePM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newTariffVersionAllInChargePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newTariffVersionAllInChargePM.UniqueKey = Guid.newGuid();
                newTariffVersionAllInChargePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newTariffVersionAllInChargePM.OldEntityPM = this.clone(newTariffVersionAllInChargePM);

				
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    newTariffVersionAllInChargePM.ChangeSetOp = "Delete";
                }
                else {
                    if (newTariffVersionAllInChargePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newTariffVersionAllInChargePM.ChangeSetOp = "Update";
                    }
                else {
                        newTariffVersionAllInChargePM.ChangeSetOp = "Insert";
                    }
                }
 
                newTariffVersionAllInChargePM.OldEntityPM = null;
                newTariffVersionAllInChargePM.EntityParentPM = null;
            }
			
			 newTariffVersionAllInChargePM.IsDirty = false;
            entityPM.TariffAllInCharges.push(newTariffVersionAllInChargePM);
        }
        if (oldTariffAllInCharges) {
            
            for (var itemKey in oldTariffAllInCharges) {
                if (entityPM.TariffAllInCharges.filter(p=> p.UniqueKey === oldTariffAllInCharges[itemKey].UniqueKey).length === 0) {
				
                    if (oldTariffAllInCharges[itemKey]) {
                        //oldTariffAllInCharges[itemKey].ChangeSetOp = "Delete";
                        //entityPM.TariffAllInCharges.push(oldTariffAllInCharges[itemKey]);
						var oldItemJson = oldTariffAllInCharges[itemKey];
                        var deletedPM: TariffVersionAllInChargePM = new TariffVersionAllInChargePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.TariffAllInCharges.push(deletedPM);
                    }
                }
            }
        }
    }
 
    MapActiveVersions(entityPM: TariffPM, jsonPM: any, mapParent: boolean = true) {

        var oldActiveVersions: TariffVersionPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldActiveVersions = entityPM.OldEntityPM.ActiveVersions;
        }

        entityPM.ActiveVersions = new Array<TariffVersionPM>();
        for (var item in jsonPM.ActiveVersions) {
            var jItem = jsonPM.ActiveVersions[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newTariffVersionPM: TariffVersionPM;
	  
            if (mapParent) {
                newTariffVersionPM = new TariffVersionPM(entityPM);
            }
            else
            {
                newTariffVersionPM = new TariffVersionPM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newTariffVersionPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newTariffVersionPM.UniqueKey = Guid.newGuid();
                newTariffVersionPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newTariffVersionPM.OldEntityPM = this.clone(newTariffVersionPM);
 

                this.MapTariffLines(newTariffVersionPM, jItem, mapParent);
                newTariffVersionPM.OldEntityPM.TariffLines = [];
                for (var k in newTariffVersionPM.TariffLines) {
                    //var clonedInside = this.clone(newTariffVersionPM.TariffLines[k]);
                    newTariffVersionPM.OldEntityPM.TariffLines.push(newTariffVersionPM.TariffLines[k].OldEntityPM); // clone old TariffLines//
                }
 

                this.MapTariffAllInCharges(newTariffVersionPM, jItem, mapParent);
                newTariffVersionPM.OldEntityPM.TariffAllInCharges = [];
                for (var k in newTariffVersionPM.TariffAllInCharges) {
                    //var clonedInside = this.clone(newTariffVersionPM.TariffAllInCharges[k]);
                    newTariffVersionPM.OldEntityPM.TariffAllInCharges.push(newTariffVersionPM.TariffAllInCharges[k].OldEntityPM); // clone old TariffAllInCharges//
                }

				
            }
            else {
                if (newTariffVersionPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newTariffVersionPM.ChangeSetOp = "Update";
                }
                else {
                        newTariffVersionPM.ChangeSetOp = "Insert";
                }
 

                this.MapTariffLines(newTariffVersionPM, jItem, mapParent);
 

                this.MapTariffAllInCharges(newTariffVersionPM, jItem, mapParent);
 
                newTariffVersionPM.OldEntityPM = null;
                newTariffVersionPM.EntityParentPM = null;
            }
			
			 newTariffVersionPM.IsDirty = false;
            entityPM.ActiveVersions.push(newTariffVersionPM);
        }
        if (oldActiveVersions) {
            
            for (var itemKey in oldActiveVersions) {
                if (entityPM.ActiveVersions.filter(p=> p.UniqueKey === oldActiveVersions[itemKey].UniqueKey).length === 0) {
				
                    if (oldActiveVersions[itemKey]) {
                        //oldActiveVersions[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ActiveVersions.push(oldActiveVersions[itemKey]);
						var oldItemJson = oldActiveVersions[itemKey];
                        var deletedPM: TariffVersionPM = new TariffVersionPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
 

                        this.MapTariffLines(deletedPM, oldItemJson, mapParent);
 

                        this.MapTariffAllInCharges(deletedPM, oldItemJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.ActiveVersions.push(deletedPM);
                    }
                }
            }
        }
    }
 
    MapDeletedLinesExpirationDates(entityPM: TariffPM, jsonPM: any, mapParent: boolean = true) {

        entityPM.DeletedLinesExpirationDates = new Array<TariffLineExpirationDatePM>();
        for (var item in jsonPM.DeletedLinesExpirationDates) {

            var jItem = jsonPM.DeletedLinesExpirationDates[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newTariffLineExpirationDatePM: TariffLineExpirationDatePM;
            newTariffLineExpirationDatePM = new TariffLineExpirationDatePM();
				                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
			
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newTariffLineExpirationDatePM[pmProperty] = jItem[pmProperty];
            }
            newTariffLineExpirationDatePM.IsDirty = false;
            entityPM.DeletedLinesExpirationDates.push(newTariffLineExpirationDatePM);
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: TariffPM;
			entityPM = new TariffPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;

			TariffPMInitService.InitValues(entityPM, true);
			TariffPMInitService.ApplyUIPoperties(entityPM, true);

			return entityPM;
    }
		 

}
