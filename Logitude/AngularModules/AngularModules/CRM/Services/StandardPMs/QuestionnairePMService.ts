//------------------------------------------------------------------------------
// <auto-generated>
//     Angular 9.1.1
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { Injectable } from '@angular/core';
import { HttpClient, HttpResponse } from '@angular/common/http';
import { catchError, map } from 'rxjs/operators';
import { defer, of } from 'rxjs';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {QuestionnairePM} from '../../EntityPMs/QuestionnairePM';

import {QuestionnaireQuestionPM} from '../../EntityPMs/QuestionnaireQuestionPM';

@Injectable()

export class QuestionnairePMService {
 private _http: HttpClient;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.HttpClient;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/questionnaires';      
    }

	get(id: string) {       

		var callTime = new Date();		

		return defer(() => {
			return this._http.get(this._apiUrl + '/getsingle?' + 'id=' + id, ServiceHelper.GetHttpFullHeaders())
				.pipe(
					map((response: HttpResponse<any>) => {
						var pm = response.body;
				
						var entity: QuestionnairePM;
						if (pm) {
							entity = this.MapJsonToEntityPM(pm);
						}

						var serviceResponse: ServiceResponse = new ServiceResponse();
						serviceResponse.Result = entity;
              
						var servertime = response.headers.get('ServerExecutionTime');
						PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Questionnaire", "GetSinglePM", 'id=' + id);
				 
						return serviceResponse;

					}),
					
					catchError(ServiceHelper.HandleServiceError));
		});                    
	}

	insert(entityPM: QuestionnairePM) {
 
		var callTime = new Date();  
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();                
			var errorsArray = validator.Validate("Questionnaire", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: QuestionnairePM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.post(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {

							var pm = response.body;
							if (pm) {
								var mappedResult: QuestionnairePM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}						

							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Questionnaire", "SaveChanges", "");                    
												                             
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

	update(entityPM: QuestionnairePM) {

		var callTime = new Date();     
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();               
			var errorsArray = validator.Validate("Questionnaire", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: QuestionnairePM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.put(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {
                 
							var pm = response.body;
							if (pm) {
								var mappedResult: QuestionnairePM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}
							 
							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Questionnaire", "SaveChanges", "");                    
					                           
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: QuestionnairePM = null) {

         
        if (!entityPM) {
            
            entityPM = new QuestionnairePM();
			entityPM.DisableMarkAsDirty = true;
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapQuestionnaireQuestions(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.QuestionnaireQuestions = [];
            for (var item in entityPM.QuestionnaireQuestions) {
            var myQuestionnaireQuestionPM = entityPM.QuestionnaireQuestions[item];
            var newQuestionnaireQuestionPM: QuestionnaireQuestionPM = this.clone(myQuestionnaireQuestionPM);
						
							 
            entityPM.OldEntityPM.QuestionnaireQuestions.push(newQuestionnaireQuestionPM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
	    entityPM.DisableMarkAsDirty = false;

        return entityPM;
    }

    MapQuestionnaireQuestions(entityPM: QuestionnairePM, jsonPM: any, mapParent: boolean = true) {

        var oldQuestionnaireQuestions: QuestionnaireQuestionPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldQuestionnaireQuestions = entityPM.OldEntityPM.QuestionnaireQuestions;
        }

        entityPM.QuestionnaireQuestions = new Array<QuestionnaireQuestionPM>();
        for (var item in jsonPM.QuestionnaireQuestions) {
            var jItem = jsonPM.QuestionnaireQuestions[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuestionnaireQuestionPM: QuestionnaireQuestionPM;
	  
            if (mapParent) {
                newQuestionnaireQuestionPM = new QuestionnaireQuestionPM(entityPM);
            }
            else
            {
                newQuestionnaireQuestionPM = new QuestionnaireQuestionPM(null);
            }
 			newQuestionnaireQuestionPM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuestionnaireQuestionPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newQuestionnaireQuestionPM.UniqueKey = Guid.newGuid();
                newQuestionnaireQuestionPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newQuestionnaireQuestionPM.OldEntityPM = this.clone(newQuestionnaireQuestionPM);

				
            }
            else {
                if (newQuestionnaireQuestionPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newQuestionnaireQuestionPM.ChangeSetOp = "Update";
                }
                else {
                        newQuestionnaireQuestionPM.ChangeSetOp = "Insert";
                }
 
                newQuestionnaireQuestionPM.OldEntityPM = null;
                newQuestionnaireQuestionPM.EntityParentPM = null;
            }
			 newQuestionnaireQuestionPM.DisableMarkAsDirty = false;
			 newQuestionnaireQuestionPM.IsDirty = false;
            entityPM.QuestionnaireQuestions.push(newQuestionnaireQuestionPM);
        }
        if (oldQuestionnaireQuestions) {
            
            for (var itemKey in oldQuestionnaireQuestions) {
                if (entityPM.QuestionnaireQuestions.filter(p=> p.UniqueKey === oldQuestionnaireQuestions[itemKey].UniqueKey).length === 0) {
				
                    if (oldQuestionnaireQuestions[itemKey]) {
                        //oldQuestionnaireQuestions[itemKey].ChangeSetOp = "Delete";
                        //entityPM.QuestionnaireQuestions.push(oldQuestionnaireQuestions[itemKey]);
						var oldItemJson = oldQuestionnaireQuestions[itemKey];
                        var deletedPM: QuestionnaireQuestionPM = new QuestionnaireQuestionPM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.QuestionnaireQuestions.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: QuestionnairePM;
			entityPM = new QuestionnairePM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
