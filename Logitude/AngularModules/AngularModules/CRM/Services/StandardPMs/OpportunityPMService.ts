//------------------------------------------------------------------------------
// <auto-generated>
//     Angular 9.1.1
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { Injectable } from '@angular/core';
import { HttpClient, HttpResponse } from '@angular/common/http';
import { catchError, map } from 'rxjs/operators';
import { defer, of } from 'rxjs';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {OpportunityPM} from '../../EntityPMs/OpportunityPM';

import {OpportunityProductPM} from '../../EntityPMs/OpportunityProductPM';

import {OpportunityProductLocationPM} from '../../EntityPMs/OpportunityProductLocationPM';
import {OpportunityCompetitorPM} from '../../EntityPMs/OpportunityCompetitorPM';

import {OpportunityCompetitorProductPM} from '../../EntityPMs/OpportunityCompetitorProductPM';
import {OpportunityAdditionalServicePM} from '../../EntityPMs/OpportunityAdditionalServicePM';
import {OpportunityPMInitService} from '../../EntityPMInitServices/OpportunityPMInitService';

@Injectable()

export class OpportunityPMService {
 private _http: HttpClient;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.HttpClient;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/opportunities';      
    }

	get(id: string) {       

		var callTime = new Date();		

		return defer(() => {
			return this._http.get(this._apiUrl + '/getsingle?' + 'id=' + id, ServiceHelper.GetHttpFullHeaders())
				.pipe(
					map((response: HttpResponse<any>) => {
						var pm = response.body;
				
						var entity: OpportunityPM;
						if (pm) {
							entity = this.MapJsonToEntityPM(pm);
                      OpportunityPMInitService.InitValues(entity, false);
                      OpportunityPMInitService.ApplyUIPoperties(entity, false);
						}

						var serviceResponse: ServiceResponse = new ServiceResponse();
						serviceResponse.Result = entity;
              
						var servertime = response.headers.get('ServerExecutionTime');
						PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Opportunity", "GetSinglePM", 'id=' + id);
				 
						return serviceResponse;

					}),
					
					catchError(ServiceHelper.HandleServiceError));
		});                    
	}

	insert(entityPM: OpportunityPM) {
 
		var callTime = new Date();  
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();                
			var errorsArray = validator.Validate("Opportunity", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: OpportunityPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.post(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {

							var pm = response.body;
							if (pm) {
								var mappedResult: OpportunityPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}						

							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Opportunity", "SaveChanges", "");                    
												                             
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

	update(entityPM: OpportunityPM) {

		var callTime = new Date();     
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();               
			var errorsArray = validator.Validate("Opportunity", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: OpportunityPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.put(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {
                 
							var pm = response.body;
							if (pm) {
								var mappedResult: OpportunityPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}
							 
							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Opportunity", "SaveChanges", "");                    
					                           
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: OpportunityPM = null) {

         
        if (!entityPM) {
            
            entityPM = new OpportunityPM();
			entityPM.DisableMarkAsDirty = true;
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 41; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapOpportunityProducts(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapOpportunityCompetitors(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapOpportunityAdditionalServices(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.OpportunityProducts = [];
            for (var item in entityPM.OpportunityProducts) {
            var myOpportunityProductPM = entityPM.OpportunityProducts[item];
            var newOpportunityProductPM: OpportunityProductPM = this.clone(myOpportunityProductPM);
						
                newOpportunityProductPM.OpportunityProductLocations = [];
                for (var k in myOpportunityProductPM.OpportunityProductLocations) {
				    var myOpportunityProductLocationPM =myOpportunityProductPM.OpportunityProductLocations[k];
				    var newOpportunityProductLocationPM=this.clone(myOpportunityProductPM.OpportunityProductLocations[k]);
                    newOpportunityProductPM.OpportunityProductLocations.push(newOpportunityProductLocationPM);

					                 }
							 
            entityPM.OldEntityPM.OpportunityProducts.push(newOpportunityProductPM);
            }
			   			   			   
            entityPM.OldEntityPM.OpportunityCompetitors = [];
            for (var item in entityPM.OpportunityCompetitors) {
            var myOpportunityCompetitorPM = entityPM.OpportunityCompetitors[item];
            var newOpportunityCompetitorPM: OpportunityCompetitorPM = this.clone(myOpportunityCompetitorPM);
						
                newOpportunityCompetitorPM.OpportunityCompetitorProducts = [];
                for (var k in myOpportunityCompetitorPM.OpportunityCompetitorProducts) {
				    var myOpportunityCompetitorProductPM =myOpportunityCompetitorPM.OpportunityCompetitorProducts[k];
				    var newOpportunityCompetitorProductPM=this.clone(myOpportunityCompetitorPM.OpportunityCompetitorProducts[k]);
                    newOpportunityCompetitorPM.OpportunityCompetitorProducts.push(newOpportunityCompetitorProductPM);

					                 }
							 
            entityPM.OldEntityPM.OpportunityCompetitors.push(newOpportunityCompetitorPM);
            }
			   			   			   
            entityPM.OldEntityPM.OpportunityAdditionalServices = [];
            for (var item in entityPM.OpportunityAdditionalServices) {
            var myOpportunityAdditionalServicePM = entityPM.OpportunityAdditionalServices[item];
            var newOpportunityAdditionalServicePM: OpportunityAdditionalServicePM = this.clone(myOpportunityAdditionalServicePM);
						
							 
            entityPM.OldEntityPM.OpportunityAdditionalServices.push(newOpportunityAdditionalServicePM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
	    entityPM.DisableMarkAsDirty = false;

        return entityPM;
    }

    MapOpportunityProducts(entityPM: OpportunityPM, jsonPM: any, mapParent: boolean = true) {

        var oldOpportunityProducts: OpportunityProductPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldOpportunityProducts = entityPM.OldEntityPM.OpportunityProducts;
        }

        entityPM.OpportunityProducts = new Array<OpportunityProductPM>();
        for (var item in jsonPM.OpportunityProducts) {
            var jItem = jsonPM.OpportunityProducts[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newOpportunityProductPM: OpportunityProductPM;
	  
            if (mapParent) {
                newOpportunityProductPM = new OpportunityProductPM(entityPM);
            }
            else
            {
                newOpportunityProductPM = new OpportunityProductPM(null);
            }
 			newOpportunityProductPM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newOpportunityProductPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newOpportunityProductPM.UniqueKey = Guid.newGuid();
                newOpportunityProductPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newOpportunityProductPM.OldEntityPM = this.clone(newOpportunityProductPM);
 

                this.MapOpportunityProductLocations(newOpportunityProductPM, jItem, mapParent);
                newOpportunityProductPM.OldEntityPM.OpportunityProductLocations = [];
                for (var k in newOpportunityProductPM.OpportunityProductLocations) {
                    //var clonedInside = this.clone(newOpportunityProductPM.OpportunityProductLocations[k]);
                    newOpportunityProductPM.OldEntityPM.OpportunityProductLocations.push(newOpportunityProductPM.OpportunityProductLocations[k].OldEntityPM); // clone old OpportunityProductLocations//
                }

				
            }
            else {
                if (newOpportunityProductPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newOpportunityProductPM.ChangeSetOp = "Update";
                }
                else {
                        newOpportunityProductPM.ChangeSetOp = "Insert";
                }
 

                this.MapOpportunityProductLocations(newOpportunityProductPM, jItem, mapParent);
 
                newOpportunityProductPM.OldEntityPM = null;
                newOpportunityProductPM.EntityParentPM = null;
            }
			 newOpportunityProductPM.DisableMarkAsDirty = false;
			 newOpportunityProductPM.IsDirty = false;
            entityPM.OpportunityProducts.push(newOpportunityProductPM);
        }
        if (oldOpportunityProducts) {
            
            for (var itemKey in oldOpportunityProducts) {
                if (entityPM.OpportunityProducts.filter(p=> p.UniqueKey === oldOpportunityProducts[itemKey].UniqueKey).length === 0) {
				
                    if (oldOpportunityProducts[itemKey]) {
                        //oldOpportunityProducts[itemKey].ChangeSetOp = "Delete";
                        //entityPM.OpportunityProducts.push(oldOpportunityProducts[itemKey]);
						var oldItemJson = oldOpportunityProducts[itemKey];
                        var deletedPM: OpportunityProductPM = new OpportunityProductPM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
 

                        this.MapOpportunityProductLocations(deletedPM, oldItemJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.OpportunityProducts.push(deletedPM);
                    }
                }
            }
        }
    }
    MapOpportunityProductLocations(entityPM: OpportunityProductPM, jsonPM: any, mapParent: boolean = true) {

        var oldOpportunityProductLocations: OpportunityProductLocationPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldOpportunityProductLocations = entityPM.OldEntityPM.OpportunityProductLocations;
        }

        entityPM.OpportunityProductLocations = new Array<OpportunityProductLocationPM>();
        for (var item in jsonPM.OpportunityProductLocations) {
            var jItem = jsonPM.OpportunityProductLocations[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newOpportunityProductLocationPM: OpportunityProductLocationPM;
	  
            if (mapParent) {
                newOpportunityProductLocationPM = new OpportunityProductLocationPM(entityPM);
            }
            else
            {
                newOpportunityProductLocationPM = new OpportunityProductLocationPM(null);
            }
 			newOpportunityProductLocationPM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newOpportunityProductLocationPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newOpportunityProductLocationPM.UniqueKey = Guid.newGuid();
                newOpportunityProductLocationPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newOpportunityProductLocationPM.OldEntityPM = this.clone(newOpportunityProductLocationPM);

				
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    newOpportunityProductLocationPM.ChangeSetOp = "Delete";
                }
                else {
                    if (newOpportunityProductLocationPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newOpportunityProductLocationPM.ChangeSetOp = "Update";
                    }
                else {
                        newOpportunityProductLocationPM.ChangeSetOp = "Insert";
                    }
                }
 
                newOpportunityProductLocationPM.OldEntityPM = null;
                newOpportunityProductLocationPM.EntityParentPM = null;
            }
			 newOpportunityProductLocationPM.DisableMarkAsDirty = false;
			 newOpportunityProductLocationPM.IsDirty = false;
            entityPM.OpportunityProductLocations.push(newOpportunityProductLocationPM);
        }
        if (oldOpportunityProductLocations) {
            
            for (var itemKey in oldOpportunityProductLocations) {
                if (entityPM.OpportunityProductLocations.filter(p=> p.UniqueKey === oldOpportunityProductLocations[itemKey].UniqueKey).length === 0) {
				
                    if (oldOpportunityProductLocations[itemKey]) {
                        //oldOpportunityProductLocations[itemKey].ChangeSetOp = "Delete";
                        //entityPM.OpportunityProductLocations.push(oldOpportunityProductLocations[itemKey]);
						var oldItemJson = oldOpportunityProductLocations[itemKey];
                        var deletedPM: OpportunityProductLocationPM = new OpportunityProductLocationPM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.OpportunityProductLocations.push(deletedPM);
                    }
                }
            }
        }
    }
 
    MapOpportunityCompetitors(entityPM: OpportunityPM, jsonPM: any, mapParent: boolean = true) {

        var oldOpportunityCompetitors: OpportunityCompetitorPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldOpportunityCompetitors = entityPM.OldEntityPM.OpportunityCompetitors;
        }

        entityPM.OpportunityCompetitors = new Array<OpportunityCompetitorPM>();
        for (var item in jsonPM.OpportunityCompetitors) {
            var jItem = jsonPM.OpportunityCompetitors[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newOpportunityCompetitorPM: OpportunityCompetitorPM;
	  
            if (mapParent) {
                newOpportunityCompetitorPM = new OpportunityCompetitorPM(entityPM);
            }
            else
            {
                newOpportunityCompetitorPM = new OpportunityCompetitorPM(null);
            }
 			newOpportunityCompetitorPM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newOpportunityCompetitorPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newOpportunityCompetitorPM.UniqueKey = Guid.newGuid();
                newOpportunityCompetitorPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newOpportunityCompetitorPM.OldEntityPM = this.clone(newOpportunityCompetitorPM);
 

                this.MapOpportunityCompetitorProducts(newOpportunityCompetitorPM, jItem, mapParent);
                newOpportunityCompetitorPM.OldEntityPM.OpportunityCompetitorProducts = [];
                for (var k in newOpportunityCompetitorPM.OpportunityCompetitorProducts) {
                    //var clonedInside = this.clone(newOpportunityCompetitorPM.OpportunityCompetitorProducts[k]);
                    newOpportunityCompetitorPM.OldEntityPM.OpportunityCompetitorProducts.push(newOpportunityCompetitorPM.OpportunityCompetitorProducts[k].OldEntityPM); // clone old OpportunityCompetitorProducts//
                }

				
            }
            else {
                if (newOpportunityCompetitorPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newOpportunityCompetitorPM.ChangeSetOp = "Update";
                }
                else {
                        newOpportunityCompetitorPM.ChangeSetOp = "Insert";
                }
 

                this.MapOpportunityCompetitorProducts(newOpportunityCompetitorPM, jItem, mapParent);
 
                newOpportunityCompetitorPM.OldEntityPM = null;
                newOpportunityCompetitorPM.EntityParentPM = null;
            }
			 newOpportunityCompetitorPM.DisableMarkAsDirty = false;
			 newOpportunityCompetitorPM.IsDirty = false;
            entityPM.OpportunityCompetitors.push(newOpportunityCompetitorPM);
        }
        if (oldOpportunityCompetitors) {
            
            for (var itemKey in oldOpportunityCompetitors) {
                if (entityPM.OpportunityCompetitors.filter(p=> p.UniqueKey === oldOpportunityCompetitors[itemKey].UniqueKey).length === 0) {
				
                    if (oldOpportunityCompetitors[itemKey]) {
                        //oldOpportunityCompetitors[itemKey].ChangeSetOp = "Delete";
                        //entityPM.OpportunityCompetitors.push(oldOpportunityCompetitors[itemKey]);
						var oldItemJson = oldOpportunityCompetitors[itemKey];
                        var deletedPM: OpportunityCompetitorPM = new OpportunityCompetitorPM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
 

                        this.MapOpportunityCompetitorProducts(deletedPM, oldItemJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.OpportunityCompetitors.push(deletedPM);
                    }
                }
            }
        }
    }
    MapOpportunityCompetitorProducts(entityPM: OpportunityCompetitorPM, jsonPM: any, mapParent: boolean = true) {

        var oldOpportunityCompetitorProducts: OpportunityCompetitorProductPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldOpportunityCompetitorProducts = entityPM.OldEntityPM.OpportunityCompetitorProducts;
        }

        entityPM.OpportunityCompetitorProducts = new Array<OpportunityCompetitorProductPM>();
        for (var item in jsonPM.OpportunityCompetitorProducts) {
            var jItem = jsonPM.OpportunityCompetitorProducts[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newOpportunityCompetitorProductPM: OpportunityCompetitorProductPM;
	  
            if (mapParent) {
                newOpportunityCompetitorProductPM = new OpportunityCompetitorProductPM(entityPM);
            }
            else
            {
                newOpportunityCompetitorProductPM = new OpportunityCompetitorProductPM(null);
            }
 			newOpportunityCompetitorProductPM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newOpportunityCompetitorProductPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newOpportunityCompetitorProductPM.UniqueKey = Guid.newGuid();
                newOpportunityCompetitorProductPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newOpportunityCompetitorProductPM.OldEntityPM = this.clone(newOpportunityCompetitorProductPM);

				
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    newOpportunityCompetitorProductPM.ChangeSetOp = "Delete";
                }
                else {
                    if (newOpportunityCompetitorProductPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newOpportunityCompetitorProductPM.ChangeSetOp = "Update";
                    }
                else {
                        newOpportunityCompetitorProductPM.ChangeSetOp = "Insert";
                    }
                }
 
                newOpportunityCompetitorProductPM.OldEntityPM = null;
                newOpportunityCompetitorProductPM.EntityParentPM = null;
            }
			 newOpportunityCompetitorProductPM.DisableMarkAsDirty = false;
			 newOpportunityCompetitorProductPM.IsDirty = false;
            entityPM.OpportunityCompetitorProducts.push(newOpportunityCompetitorProductPM);
        }
        if (oldOpportunityCompetitorProducts) {
            
            for (var itemKey in oldOpportunityCompetitorProducts) {
                if (entityPM.OpportunityCompetitorProducts.filter(p=> p.UniqueKey === oldOpportunityCompetitorProducts[itemKey].UniqueKey).length === 0) {
				
                    if (oldOpportunityCompetitorProducts[itemKey]) {
                        //oldOpportunityCompetitorProducts[itemKey].ChangeSetOp = "Delete";
                        //entityPM.OpportunityCompetitorProducts.push(oldOpportunityCompetitorProducts[itemKey]);
						var oldItemJson = oldOpportunityCompetitorProducts[itemKey];
                        var deletedPM: OpportunityCompetitorProductPM = new OpportunityCompetitorProductPM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.OpportunityCompetitorProducts.push(deletedPM);
                    }
                }
            }
        }
    }
 
    MapOpportunityAdditionalServices(entityPM: OpportunityPM, jsonPM: any, mapParent: boolean = true) {

        var oldOpportunityAdditionalServices: OpportunityAdditionalServicePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldOpportunityAdditionalServices = entityPM.OldEntityPM.OpportunityAdditionalServices;
        }

        entityPM.OpportunityAdditionalServices = new Array<OpportunityAdditionalServicePM>();
        for (var item in jsonPM.OpportunityAdditionalServices) {
            var jItem = jsonPM.OpportunityAdditionalServices[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newOpportunityAdditionalServicePM: OpportunityAdditionalServicePM;
	  
            if (mapParent) {
                newOpportunityAdditionalServicePM = new OpportunityAdditionalServicePM(entityPM);
            }
            else
            {
                newOpportunityAdditionalServicePM = new OpportunityAdditionalServicePM(null);
            }
 			newOpportunityAdditionalServicePM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newOpportunityAdditionalServicePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newOpportunityAdditionalServicePM.UniqueKey = Guid.newGuid();
                newOpportunityAdditionalServicePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newOpportunityAdditionalServicePM.OldEntityPM = this.clone(newOpportunityAdditionalServicePM);

				
            }
            else {
                if (newOpportunityAdditionalServicePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newOpportunityAdditionalServicePM.ChangeSetOp = "Update";
                }
                else {
                        newOpportunityAdditionalServicePM.ChangeSetOp = "Insert";
                }
 
                newOpportunityAdditionalServicePM.OldEntityPM = null;
                newOpportunityAdditionalServicePM.EntityParentPM = null;
            }
			 newOpportunityAdditionalServicePM.DisableMarkAsDirty = false;
			 newOpportunityAdditionalServicePM.IsDirty = false;
            entityPM.OpportunityAdditionalServices.push(newOpportunityAdditionalServicePM);
        }
        if (oldOpportunityAdditionalServices) {
            
            for (var itemKey in oldOpportunityAdditionalServices) {
                if (entityPM.OpportunityAdditionalServices.filter(p=> p.UniqueKey === oldOpportunityAdditionalServices[itemKey].UniqueKey).length === 0) {
				
                    if (oldOpportunityAdditionalServices[itemKey]) {
                        //oldOpportunityAdditionalServices[itemKey].ChangeSetOp = "Delete";
                        //entityPM.OpportunityAdditionalServices.push(oldOpportunityAdditionalServices[itemKey]);
						var oldItemJson = oldOpportunityAdditionalServices[itemKey];
                        var deletedPM: OpportunityAdditionalServicePM = new OpportunityAdditionalServicePM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.OpportunityAdditionalServices.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: OpportunityPM;
			entityPM = new OpportunityPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;

			OpportunityPMInitService.InitValues(entityPM, true);
			OpportunityPMInitService.ApplyUIPoperties(entityPM, true);

			return entityPM;
    }
		 

}
