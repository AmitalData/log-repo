//------------------------------------------------------------------------------
// <auto-generated>
//     Angular 9.1.1
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { Injectable } from '@angular/core';
import { HttpClient, HttpResponse } from '@angular/common/http';
import { catchError, map } from 'rxjs/operators';
import { defer, of } from 'rxjs';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {EmployeeGroupPM} from '../../EntityPMs/EmployeeGroupPM';

import {EmployeeGroupLinePM} from '../../EntityPMs/EmployeeGroupLinePM';
import {EmployeeGroupPMInitService} from '../../EntityPMInitServices/EmployeeGroupPMInitService';

@Injectable()

export class EmployeeGroupPMService {
 private _http: HttpClient;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.HttpClient;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/employeegroups';      
    }

	get(id: string) {       

		var callTime = new Date();		

		return defer(() => {
			return this._http.get(this._apiUrl + '/getsingle?' + 'id=' + id, ServiceHelper.GetHttpFullHeaders())
				.pipe(
					map((response: HttpResponse<any>) => {
						var pm = response.body;
				
						var entity: EmployeeGroupPM;
						if (pm) {
							entity = this.MapJsonToEntityPM(pm);
                      EmployeeGroupPMInitService.InitValues(entity, false);
                      EmployeeGroupPMInitService.ApplyUIPoperties(entity, false);
						}

						var serviceResponse: ServiceResponse = new ServiceResponse();
						serviceResponse.Result = entity;
              
						var servertime = response.headers.get('ServerExecutionTime');
						PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "EmployeeGroup", "GetSinglePM", 'id=' + id);
				 
						return serviceResponse;

					}),
					
					catchError(ServiceHelper.HandleServiceError));
		});                    
	}

	insert(entityPM: EmployeeGroupPM) {
 
		var callTime = new Date();  
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();                
			var errorsArray = validator.Validate("EmployeeGroup", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: EmployeeGroupPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.post(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {

							var pm = response.body;
							if (pm) {
								var mappedResult: EmployeeGroupPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}						

							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "EmployeeGroup", "SaveChanges", "");                    
												                             
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

	update(entityPM: EmployeeGroupPM) {

		var callTime = new Date();     
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();               
			var errorsArray = validator.Validate("EmployeeGroup", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: EmployeeGroupPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.put(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {
                 
							var pm = response.body;
							if (pm) {
								var mappedResult: EmployeeGroupPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}
							 
							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "EmployeeGroup", "SaveChanges", "");                    
					                           
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: EmployeeGroupPM = null) {

         
        if (!entityPM) {
            
            entityPM = new EmployeeGroupPM();
			entityPM.DisableMarkAsDirty = true;
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapEmployeeGroupLines(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.EmployeeGroupLines = [];
            for (var item in entityPM.EmployeeGroupLines) {
            var myEmployeeGroupLinePM = entityPM.EmployeeGroupLines[item];
            var newEmployeeGroupLinePM: EmployeeGroupLinePM = this.clone(myEmployeeGroupLinePM);
						
							 
            entityPM.OldEntityPM.EmployeeGroupLines.push(newEmployeeGroupLinePM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
	    entityPM.DisableMarkAsDirty = false;

        return entityPM;
    }

    MapEmployeeGroupLines(entityPM: EmployeeGroupPM, jsonPM: any, mapParent: boolean = true) {

        var oldEmployeeGroupLines: EmployeeGroupLinePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldEmployeeGroupLines = entityPM.OldEntityPM.EmployeeGroupLines;
        }

        entityPM.EmployeeGroupLines = new Array<EmployeeGroupLinePM>();
        for (var item in jsonPM.EmployeeGroupLines) {
            var jItem = jsonPM.EmployeeGroupLines[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newEmployeeGroupLinePM: EmployeeGroupLinePM;
	  
            if (mapParent) {
                newEmployeeGroupLinePM = new EmployeeGroupLinePM(entityPM);
            }
            else
            {
                newEmployeeGroupLinePM = new EmployeeGroupLinePM(null);
            }
 			newEmployeeGroupLinePM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newEmployeeGroupLinePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newEmployeeGroupLinePM.UniqueKey = Guid.newGuid();
                newEmployeeGroupLinePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newEmployeeGroupLinePM.OldEntityPM = this.clone(newEmployeeGroupLinePM);

				
            }
            else {
                if (newEmployeeGroupLinePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newEmployeeGroupLinePM.ChangeSetOp = "Update";
                }
                else {
                        newEmployeeGroupLinePM.ChangeSetOp = "Insert";
                }
 
                newEmployeeGroupLinePM.OldEntityPM = null;
                newEmployeeGroupLinePM.EntityParentPM = null;
            }
			 newEmployeeGroupLinePM.DisableMarkAsDirty = false;
			 newEmployeeGroupLinePM.IsDirty = false;
            entityPM.EmployeeGroupLines.push(newEmployeeGroupLinePM);
        }
        if (oldEmployeeGroupLines) {
            
            for (var itemKey in oldEmployeeGroupLines) {
                if (entityPM.EmployeeGroupLines.filter(p=> p.UniqueKey === oldEmployeeGroupLines[itemKey].UniqueKey).length === 0) {
				
                    if (oldEmployeeGroupLines[itemKey]) {
                        //oldEmployeeGroupLines[itemKey].ChangeSetOp = "Delete";
                        //entityPM.EmployeeGroupLines.push(oldEmployeeGroupLines[itemKey]);
						var oldItemJson = oldEmployeeGroupLines[itemKey];
                        var deletedPM: EmployeeGroupLinePM = new EmployeeGroupLinePM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.EmployeeGroupLines.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: EmployeeGroupPM;
			entityPM = new EmployeeGroupPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;

			EmployeeGroupPMInitService.InitValues(entityPM, true);
			EmployeeGroupPMInitService.ApplyUIPoperties(entityPM, true);

			return entityPM;
    }
		 

}
