//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import {Injectable} from '@angular/core';
import {Http, Headers} from '@angular/http';
import {Observable}     from 'rxjs/Rx';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {QuestionnaireAnswerPM} from '../../EntityPMs/QuestionnaireAnswerPM';

import {QuestionnaireAnswerLinePM} from '../../EntityPMs/QuestionnaireAnswerLinePM';

@Injectable()

export class QuestionnaireAnswerPMService {
 private _http: Http;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.Http;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/questionnaireanswers';      
    }

 get(id: string) {
         
         
        var authHeader = new Headers();
        authHeader.append('Token', SessionInfo.Token);
        var callTime = new Date();		
		 return Observable.defer(() => {
                return this._http.get(this._apiUrl+'/getsingle?'+'id=' + id, {
                    headers: authHeader
                }).map(response => {
                    var pm = response.json();

                   
					
                    var entity: QuestionnaireAnswerPM;
					if(pm)
					{
                      entity = this.MapJsonToEntityPM(pm);
                    }

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
                serviceResponse.Result = entity;
              
			    var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "QuestionnaireAnswer", "GetSinglePM", 'id=' + id);
				 
                return serviceResponse;

            }).catch(ServiceHelper.HandleServiceError);
            });                    
    }

	 insert(entityPM: QuestionnaireAnswerPM) {
 
        var callTime = new Date();        
        return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
                 
                var errorsArray = validator.Validate("QuestionnaireAnswer", entityPM);
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: QuestionnaireAnswerPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.post(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  QuestionnaireAnswerPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							}
							

                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "QuestionnaireAnswer", "SaveChanges", "");                    
												 
                            
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );
    }

    update(entityPM: QuestionnaireAnswerPM) {

            var callTime = new Date();         
            return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
               
                var errorsArray = validator.Validate("QuestionnaireAnswer", entityPM);
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: QuestionnaireAnswerPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.put(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {
                 

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  QuestionnaireAnswerPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							 }
							 
                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "QuestionnaireAnswer", "SaveChanges", "");                    
					                           
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );

    }

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: QuestionnaireAnswerPM = null) {

         
        if (!entityPM) {
            
            entityPM = new QuestionnaireAnswerPM();
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapQuestionnaireAnswerLines(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.QuestionnaireAnswerLines = [];
            for (var item in entityPM.QuestionnaireAnswerLines) {
            var myQuestionnaireAnswerLinePM = entityPM.QuestionnaireAnswerLines[item];
            var newQuestionnaireAnswerLinePM: QuestionnaireAnswerLinePM = this.clone(myQuestionnaireAnswerLinePM);
						
							 
            entityPM.OldEntityPM.QuestionnaireAnswerLines.push(newQuestionnaireAnswerLinePM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
        return entityPM;
    }

    MapQuestionnaireAnswerLines(entityPM: QuestionnaireAnswerPM, jsonPM: any, mapParent: boolean = true) {

        var oldQuestionnaireAnswerLines: QuestionnaireAnswerLinePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldQuestionnaireAnswerLines = entityPM.OldEntityPM.QuestionnaireAnswerLines;
        }

        entityPM.QuestionnaireAnswerLines = new Array<QuestionnaireAnswerLinePM>();
        for (var item in jsonPM.QuestionnaireAnswerLines) {
            var jItem = jsonPM.QuestionnaireAnswerLines[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuestionnaireAnswerLinePM: QuestionnaireAnswerLinePM;
	  
            if (mapParent) {
                newQuestionnaireAnswerLinePM = new QuestionnaireAnswerLinePM(entityPM);
            }
            else
            {
                newQuestionnaireAnswerLinePM = new QuestionnaireAnswerLinePM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuestionnaireAnswerLinePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newQuestionnaireAnswerLinePM.UniqueKey = Guid.newGuid();
                newQuestionnaireAnswerLinePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newQuestionnaireAnswerLinePM.OldEntityPM = this.clone(newQuestionnaireAnswerLinePM);

				
            }
            else {
                if (newQuestionnaireAnswerLinePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newQuestionnaireAnswerLinePM.ChangeSetOp = "Update";
                }
                else {
                        newQuestionnaireAnswerLinePM.ChangeSetOp = "Insert";
                }
 
                newQuestionnaireAnswerLinePM.OldEntityPM = null;
                newQuestionnaireAnswerLinePM.EntityParentPM = null;
            }
			
			 newQuestionnaireAnswerLinePM.IsDirty = false;
            entityPM.QuestionnaireAnswerLines.push(newQuestionnaireAnswerLinePM);
        }
        if (oldQuestionnaireAnswerLines) {
            
            for (var itemKey in oldQuestionnaireAnswerLines) {
                if (entityPM.QuestionnaireAnswerLines.filter(p=> p.UniqueKey === oldQuestionnaireAnswerLines[itemKey].UniqueKey).length === 0) {
				
                    if (oldQuestionnaireAnswerLines[itemKey]) {
                        //oldQuestionnaireAnswerLines[itemKey].ChangeSetOp = "Delete";
                        //entityPM.QuestionnaireAnswerLines.push(oldQuestionnaireAnswerLines[itemKey]);
						var oldItemJson = oldQuestionnaireAnswerLines[itemKey];
                        var deletedPM: QuestionnaireAnswerLinePM = new QuestionnaireAnswerLinePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.QuestionnaireAnswerLines.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: QuestionnaireAnswerPM;
			entityPM = new QuestionnaireAnswerPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
