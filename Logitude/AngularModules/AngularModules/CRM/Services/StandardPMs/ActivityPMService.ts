//file not found! child composition DocumentData
//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import {Injectable} from '@angular/core';
import {Http, Headers} from '@angular/http';
import {Observable}     from 'rxjs/Rx';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {ActivityPM} from '../../EntityPMs/ActivityPM';

import {ActivityInviteePM} from '../../EntityPMs/ActivityInviteePM';
import {ActivityEmailRecipientPM} from '../../EntityPMs/ActivityEmailRecipientPM';
import {ActivityNotePM} from '../../EntityPMs/ActivityNotePM';
import {DocumentDataPM} from '../../EntityPMs/DocumentDataPM';
import {ActivityPMInitService} from '../../EntityPMInitServices/ActivityPMInitService';
import {ActivityValidator} from '../../Validators/ActivityValidator';

@Injectable()

export class ActivityPMService {
 private _http: Http;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.Http;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/activities';      
    }

 get(id: string) {
         
         
        var authHeader = new Headers();
        authHeader.append('Token', SessionInfo.Token);
        var callTime = new Date();		
		 return Observable.defer(() => {
                return this._http.get(this._apiUrl+'/getsingle?'+'id=' + id, {
                    headers: authHeader
                }).map(response => {
                    var pm = response.json();

                   
					
                    var entity: ActivityPM;
					if(pm)
					{
                      entity = this.MapJsonToEntityPM(pm);
                      ActivityPMInitService.InitValues(entity, false);
                      ActivityPMInitService.ApplyUIPoperties(entity, false);
                    }

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
                serviceResponse.Result = entity;
              
			    var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Activity", "GetSinglePM", 'id=' + id);
				 
                return serviceResponse;

            }).catch(ServiceHelper.HandleServiceError);
            });                    
    }

	 insert(entityPM: ActivityPM) {
 
        var callTime = new Date();        
        return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
                 
                var errorsArray = validator.Validate("Activity", entityPM);
                var customValidator :ActivityValidator = new ActivityValidator();
                var validationErrorsArr = customValidator.Validate(entityPM);
				if(validationErrorsArr)
				{
					errorsArray = errorsArray.concat(validationErrorsArr);
				}
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: ActivityPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.post(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  ActivityPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							}
							

                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Activity", "SaveChanges", "");                    
												 
                            
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );
    }

    update(entityPM: ActivityPM) {

            var callTime = new Date();         
            return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
               
                var errorsArray = validator.Validate("Activity", entityPM);
                var customValidator :ActivityValidator = new ActivityValidator();
                var validationErrorsArr = customValidator.Validate(entityPM);
				if(validationErrorsArr)
				{
					errorsArray = errorsArray.concat(validationErrorsArr);
				}
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: ActivityPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.put(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {
                 

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  ActivityPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							 }
							 
                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Activity", "SaveChanges", "");                    
					                           
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );

    }

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: ActivityPM = null) {

         
        if (!entityPM) {
            
            entityPM = new ActivityPM();
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapActivityInvitees(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapActivityEmailRecipients(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapActivityNotes(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapActivityDocumentDatas(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.ActivityInvitees = [];
            for (var item in entityPM.ActivityInvitees) {
            var myActivityInviteePM = entityPM.ActivityInvitees[item];
            var newActivityInviteePM: ActivityInviteePM = this.clone(myActivityInviteePM);
						
							 
            entityPM.OldEntityPM.ActivityInvitees.push(newActivityInviteePM);
            }
			   			   			   
            entityPM.OldEntityPM.ActivityEmailRecipients = [];
            for (var item in entityPM.ActivityEmailRecipients) {
            var myActivityEmailRecipientPM = entityPM.ActivityEmailRecipients[item];
            var newActivityEmailRecipientPM: ActivityEmailRecipientPM = this.clone(myActivityEmailRecipientPM);
						
							 
            entityPM.OldEntityPM.ActivityEmailRecipients.push(newActivityEmailRecipientPM);
            }
			   			   			   
            entityPM.OldEntityPM.ActivityNotes = [];
            for (var item in entityPM.ActivityNotes) {
            var myActivityNotePM = entityPM.ActivityNotes[item];
            var newActivityNotePM: ActivityNotePM = this.clone(myActivityNotePM);
						
							 
            entityPM.OldEntityPM.ActivityNotes.push(newActivityNotePM);
            }
			   			   			   
            entityPM.OldEntityPM.ActivityDocumentDatas = [];
            for (var item in entityPM.ActivityDocumentDatas) {
            var myDocumentDataPM = entityPM.ActivityDocumentDatas[item];
            var newDocumentDataPM: DocumentDataPM = this.clone(myDocumentDataPM);
						
							 
            entityPM.OldEntityPM.ActivityDocumentDatas.push(newDocumentDataPM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
        return entityPM;
    }

    MapActivityInvitees(entityPM: ActivityPM, jsonPM: any, mapParent: boolean = true) {

        var oldActivityInvitees: ActivityInviteePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldActivityInvitees = entityPM.OldEntityPM.ActivityInvitees;
        }

        entityPM.ActivityInvitees = new Array<ActivityInviteePM>();
        for (var item in jsonPM.ActivityInvitees) {
            var jItem = jsonPM.ActivityInvitees[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newActivityInviteePM: ActivityInviteePM;
	  
            if (mapParent) {
                newActivityInviteePM = new ActivityInviteePM(entityPM);
            }
            else
            {
                newActivityInviteePM = new ActivityInviteePM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newActivityInviteePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newActivityInviteePM.UniqueKey = Guid.newGuid();
                newActivityInviteePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newActivityInviteePM.OldEntityPM = this.clone(newActivityInviteePM);

				
            }
            else {
                if (newActivityInviteePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newActivityInviteePM.ChangeSetOp = "Update";
                }
                else {
                        newActivityInviteePM.ChangeSetOp = "Insert";
                }
 
                newActivityInviteePM.OldEntityPM = null;
                newActivityInviteePM.EntityParentPM = null;
            }
			
			 newActivityInviteePM.IsDirty = false;
            entityPM.ActivityInvitees.push(newActivityInviteePM);
        }
        if (oldActivityInvitees) {
            
            for (var itemKey in oldActivityInvitees) {
                if (entityPM.ActivityInvitees.filter(p=> p.UniqueKey === oldActivityInvitees[itemKey].UniqueKey).length === 0) {
				
                    if (oldActivityInvitees[itemKey]) {
                        //oldActivityInvitees[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ActivityInvitees.push(oldActivityInvitees[itemKey]);
						var oldItemJson = oldActivityInvitees[itemKey];
                        var deletedPM: ActivityInviteePM = new ActivityInviteePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.ActivityInvitees.push(deletedPM);
                    }
                }
            }
        }
    }
    MapActivityEmailRecipients(entityPM: ActivityPM, jsonPM: any, mapParent: boolean = true) {

        var oldActivityEmailRecipients: ActivityEmailRecipientPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldActivityEmailRecipients = entityPM.OldEntityPM.ActivityEmailRecipients;
        }

        entityPM.ActivityEmailRecipients = new Array<ActivityEmailRecipientPM>();
        for (var item in jsonPM.ActivityEmailRecipients) {
            var jItem = jsonPM.ActivityEmailRecipients[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newActivityEmailRecipientPM: ActivityEmailRecipientPM;
	  
            if (mapParent) {
                newActivityEmailRecipientPM = new ActivityEmailRecipientPM(entityPM);
            }
            else
            {
                newActivityEmailRecipientPM = new ActivityEmailRecipientPM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newActivityEmailRecipientPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newActivityEmailRecipientPM.UniqueKey = Guid.newGuid();
                newActivityEmailRecipientPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newActivityEmailRecipientPM.OldEntityPM = this.clone(newActivityEmailRecipientPM);

				
            }
            else {
                if (newActivityEmailRecipientPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newActivityEmailRecipientPM.ChangeSetOp = "Update";
                }
                else {
                        newActivityEmailRecipientPM.ChangeSetOp = "Insert";
                }
 
                newActivityEmailRecipientPM.OldEntityPM = null;
                newActivityEmailRecipientPM.EntityParentPM = null;
            }
			
			 newActivityEmailRecipientPM.IsDirty = false;
            entityPM.ActivityEmailRecipients.push(newActivityEmailRecipientPM);
        }
        if (oldActivityEmailRecipients) {
            
            for (var itemKey in oldActivityEmailRecipients) {
                if (entityPM.ActivityEmailRecipients.filter(p=> p.UniqueKey === oldActivityEmailRecipients[itemKey].UniqueKey).length === 0) {
				
                    if (oldActivityEmailRecipients[itemKey]) {
                        //oldActivityEmailRecipients[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ActivityEmailRecipients.push(oldActivityEmailRecipients[itemKey]);
						var oldItemJson = oldActivityEmailRecipients[itemKey];
                        var deletedPM: ActivityEmailRecipientPM = new ActivityEmailRecipientPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.ActivityEmailRecipients.push(deletedPM);
                    }
                }
            }
        }
    }
    MapActivityNotes(entityPM: ActivityPM, jsonPM: any, mapParent: boolean = true) {

        var oldActivityNotes: ActivityNotePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldActivityNotes = entityPM.OldEntityPM.ActivityNotes;
        }

        entityPM.ActivityNotes = new Array<ActivityNotePM>();
        for (var item in jsonPM.ActivityNotes) {
            var jItem = jsonPM.ActivityNotes[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newActivityNotePM: ActivityNotePM;
	  
            if (mapParent) {
                newActivityNotePM = new ActivityNotePM(entityPM);
            }
            else
            {
                newActivityNotePM = new ActivityNotePM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newActivityNotePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newActivityNotePM.UniqueKey = Guid.newGuid();
                newActivityNotePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newActivityNotePM.OldEntityPM = this.clone(newActivityNotePM);

				
            }
            else {
                if (newActivityNotePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newActivityNotePM.ChangeSetOp = "Update";
                }
                else {
                        newActivityNotePM.ChangeSetOp = "Insert";
                }
 
                newActivityNotePM.OldEntityPM = null;
                newActivityNotePM.EntityParentPM = null;
            }
			
			 newActivityNotePM.IsDirty = false;
            entityPM.ActivityNotes.push(newActivityNotePM);
        }
        if (oldActivityNotes) {
            
            for (var itemKey in oldActivityNotes) {
                if (entityPM.ActivityNotes.filter(p=> p.UniqueKey === oldActivityNotes[itemKey].UniqueKey).length === 0) {
				
                    if (oldActivityNotes[itemKey]) {
                        //oldActivityNotes[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ActivityNotes.push(oldActivityNotes[itemKey]);
						var oldItemJson = oldActivityNotes[itemKey];
                        var deletedPM: ActivityNotePM = new ActivityNotePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.ActivityNotes.push(deletedPM);
                    }
                }
            }
        }
    }
    MapActivityDocumentDatas(entityPM: ActivityPM, jsonPM: any, mapParent: boolean = true) {

        entityPM.ActivityDocumentDatas = new Array<DocumentDataPM>();
        for (var item in jsonPM.ActivityDocumentDatas) {

            var jItem = jsonPM.ActivityDocumentDatas[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newDocumentDataPM: DocumentDataPM;
            newDocumentDataPM = new DocumentDataPM();
				                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
			
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newDocumentDataPM[pmProperty] = jItem[pmProperty];
            }
            newDocumentDataPM.IsDirty = false;
            entityPM.ActivityDocumentDatas.push(newDocumentDataPM);
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: ActivityPM;
			entityPM = new ActivityPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;

			ActivityPMInitService.InitValues(entityPM, true);
			ActivityPMInitService.ApplyUIPoperties(entityPM, true);

			return entityPM;
    }
		 

}
