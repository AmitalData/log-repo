//------------------------------------------------------------------------------
// <auto-generated>
//     Angular 9.1.1
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { Injectable } from '@angular/core';
import { HttpClient, HttpResponse } from '@angular/common/http';
import { catchError, map } from 'rxjs/operators';
import { defer, of } from 'rxjs';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {CustomsVendorPM} from '../../EntityPMs/CustomsVendorPM';

import {VendorCommunicationPM} from '../../EntityPMs/VendorCommunicationPM';

@Injectable()

export class CustomsVendorPMService {
 private _http: HttpClient;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.HttpClient;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/customsvendors';      
    }

	get(id: string) {       

		var callTime = new Date();		

		return defer(() => {
			return this._http.get(this._apiUrl + '/getsingle?' + 'id=' + id, ServiceHelper.GetHttpFullHeaders())
				.pipe(
					map((response: HttpResponse<any>) => {
						var pm = response.body;
				
						var entity: CustomsVendorPM;
						if (pm) {
							entity = this.MapJsonToEntityPM(pm);
						}

						var serviceResponse: ServiceResponse = new ServiceResponse();
						serviceResponse.Result = entity;
              
						var servertime = response.headers.get('ServerExecutionTime');
						PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "CustomsVendor", "GetSinglePM", 'id=' + id);
				 
						return serviceResponse;

					}),
					
					catchError(ServiceHelper.HandleServiceError));
		});                    
	}

	insert(entityPM: CustomsVendorPM) {
 
		var callTime = new Date();  
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();                
			var errorsArray = validator.Validate("Customs.CustomsVendor", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: CustomsVendorPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.post(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {

							var pm = response.body;
							if (pm) {
								var mappedResult: CustomsVendorPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}						

							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "CustomsVendor", "SaveChanges", "");                    
												                             
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

	update(entityPM: CustomsVendorPM) {

		var callTime = new Date();     
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();               
			var errorsArray = validator.Validate("Customs.CustomsVendor", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: CustomsVendorPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.put(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {
                 
							var pm = response.body;
							if (pm) {
								var mappedResult: CustomsVendorPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}
							 
							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "CustomsVendor", "SaveChanges", "");                    
					                           
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: CustomsVendorPM = null) {

         
        if (!entityPM) {
            
            entityPM = new CustomsVendorPM();
			entityPM.DisableMarkAsDirty = true;
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapVendorCommunications(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.VendorCommunications = [];
            for (var item in entityPM.VendorCommunications) {
            var myVendorCommunicationPM = entityPM.VendorCommunications[item];
            var newVendorCommunicationPM: VendorCommunicationPM = this.clone(myVendorCommunicationPM);
						
							 
            entityPM.OldEntityPM.VendorCommunications.push(newVendorCommunicationPM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
	    entityPM.DisableMarkAsDirty = false;

        return entityPM;
    }

    MapVendorCommunications(entityPM: CustomsVendorPM, jsonPM: any, mapParent: boolean = true) {

        var oldVendorCommunications: VendorCommunicationPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldVendorCommunications = entityPM.OldEntityPM.VendorCommunications;
        }

        entityPM.VendorCommunications = new Array<VendorCommunicationPM>();
        for (var item in jsonPM.VendorCommunications) {
            var jItem = jsonPM.VendorCommunications[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newVendorCommunicationPM: VendorCommunicationPM;
	  
            if (mapParent) {
                newVendorCommunicationPM = new VendorCommunicationPM(entityPM);
            }
            else
            {
                newVendorCommunicationPM = new VendorCommunicationPM(null);
            }
 			newVendorCommunicationPM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
				                  var pmProperty = pmKeysArray[pmKey];
                newVendorCommunicationPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newVendorCommunicationPM.UniqueKey = Guid.newGuid();
                newVendorCommunicationPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newVendorCommunicationPM.OldEntityPM = this.clone(newVendorCommunicationPM);

				
            }
            else {
                if (newVendorCommunicationPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newVendorCommunicationPM.ChangeSetOp = "Update";
                }
                else {
                        newVendorCommunicationPM.ChangeSetOp = "Insert";
                }
 
                newVendorCommunicationPM.OldEntityPM = null;
                newVendorCommunicationPM.EntityParentPM = null;
            }
			 newVendorCommunicationPM.DisableMarkAsDirty = false;
			 newVendorCommunicationPM.IsDirty = false;
            entityPM.VendorCommunications.push(newVendorCommunicationPM);
        }
        if (oldVendorCommunications) {
            
            for (var itemKey in oldVendorCommunications) {
                if (entityPM.VendorCommunications.filter(p=> p.UniqueKey === oldVendorCommunications[itemKey].UniqueKey).length === 0) {
				
                    if (oldVendorCommunications[itemKey]) {
                        //oldVendorCommunications[itemKey].ChangeSetOp = "Delete";
                        //entityPM.VendorCommunications.push(oldVendorCommunications[itemKey]);
						var oldItemJson = oldVendorCommunications[itemKey];
                        var deletedPM: VendorCommunicationPM = new VendorCommunicationPM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.VendorCommunications.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: CustomsVendorPM;
			entityPM = new CustomsVendorPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
