//------------------------------------------------------------------------------
// <auto-generated>
//     Angular 9.1.1
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { Injectable } from '@angular/core';
import { HttpClient, HttpResponse } from '@angular/common/http';
import { catchError, map } from 'rxjs/operators';
import { defer, of } from 'rxjs';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {VehiclePM} from '../../EntityPMs/VehiclePM';

import {VehicleSafetyAccessoryPM} from '../../EntityPMs/VehicleSafetyAccessoryPM';
import {VehicleOwnerPM} from '../../EntityPMs/VehicleOwnerPM';
import {VehicleValidator} from '../../Validators/VehicleValidator';

@Injectable()

export class VehiclePMService {
 private _http: HttpClient;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.HttpClient;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/vehicles';      
    }

	get(id: string) {       

		var callTime = new Date();		

		return defer(() => {
			return this._http.get(this._apiUrl + '/getsingle?' + 'id=' + id, ServiceHelper.GetHttpFullHeaders())
				.pipe(
					map((response: HttpResponse<any>) => {
						var pm = response.body;
				
						var entity: VehiclePM;
						if (pm) {
							entity = this.MapJsonToEntityPM(pm);
						}

						var serviceResponse: ServiceResponse = new ServiceResponse();
						serviceResponse.Result = entity;
              
						var servertime = response.headers.get('ServerExecutionTime');
						PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Vehicle", "GetSinglePM", 'id=' + id);
				 
						return serviceResponse;

					}),
					
					catchError(ServiceHelper.HandleServiceError));
		});                    
	}

	insert(entityPM: VehiclePM) {
 
		var callTime = new Date();  
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();                
			var errorsArray = validator.Validate("Customs.Vehicle", entityPM);

			var customValidator :VehicleValidator = new VehicleValidator();
			var validationErrorsArr = customValidator.Validate(entityPM);
			if(validationErrorsArr)
			{
				errorsArray = errorsArray.concat(validationErrorsArr);
			}

			if (errorsArray.length == 0) {

				var mappedEntity: VehiclePM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.post(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {

							var pm = response.body;
							if (pm) {
								var mappedResult: VehiclePM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}						

							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Vehicle", "SaveChanges", "");                    
												                             
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

	update(entityPM: VehiclePM) {

		var callTime = new Date();     
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();               
			var errorsArray = validator.Validate("Customs.Vehicle", entityPM);

			var customValidator :VehicleValidator = new VehicleValidator();
			var validationErrorsArr = customValidator.Validate(entityPM);
			if(validationErrorsArr)
			{
				errorsArray = errorsArray.concat(validationErrorsArr);
			}

			if (errorsArray.length == 0) {

				var mappedEntity: VehiclePM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.put(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {
                 
							var pm = response.body;
							if (pm) {
								var mappedResult: VehiclePM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}
							 
							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Vehicle", "SaveChanges", "");                    
					                           
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: VehiclePM = null) {

         
        if (!entityPM) {
            
            entityPM = new VehiclePM();
			entityPM.DisableMarkAsDirty = true;
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapVehicleSafetyAccessories(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapVehicleOwners(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.VehicleSafetyAccessories = [];
            for (var item in entityPM.VehicleSafetyAccessories) {
            var myVehicleSafetyAccessoryPM = entityPM.VehicleSafetyAccessories[item];
            var newVehicleSafetyAccessoryPM: VehicleSafetyAccessoryPM = this.clone(myVehicleSafetyAccessoryPM);
						
							 
            entityPM.OldEntityPM.VehicleSafetyAccessories.push(newVehicleSafetyAccessoryPM);
            }
			   			   			   
            entityPM.OldEntityPM.VehicleOwners = [];
            for (var item in entityPM.VehicleOwners) {
            var myVehicleOwnerPM = entityPM.VehicleOwners[item];
            var newVehicleOwnerPM: VehicleOwnerPM = this.clone(myVehicleOwnerPM);
						
							 
            entityPM.OldEntityPM.VehicleOwners.push(newVehicleOwnerPM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
	    entityPM.DisableMarkAsDirty = false;

        return entityPM;
    }

    MapVehicleSafetyAccessories(entityPM: VehiclePM, jsonPM: any, mapParent: boolean = true) {

        var oldVehicleSafetyAccessories: VehicleSafetyAccessoryPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldVehicleSafetyAccessories = entityPM.OldEntityPM.VehicleSafetyAccessories;
        }

        entityPM.VehicleSafetyAccessories = new Array<VehicleSafetyAccessoryPM>();
        for (var item in jsonPM.VehicleSafetyAccessories) {
            var jItem = jsonPM.VehicleSafetyAccessories[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newVehicleSafetyAccessoryPM: VehicleSafetyAccessoryPM;
	  
            if (mapParent) {
                newVehicleSafetyAccessoryPM = new VehicleSafetyAccessoryPM(entityPM);
            }
            else
            {
                newVehicleSafetyAccessoryPM = new VehicleSafetyAccessoryPM(null);
            }
 			newVehicleSafetyAccessoryPM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newVehicleSafetyAccessoryPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newVehicleSafetyAccessoryPM.UniqueKey = Guid.newGuid();
                newVehicleSafetyAccessoryPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newVehicleSafetyAccessoryPM.OldEntityPM = this.clone(newVehicleSafetyAccessoryPM);

				
            }
            else {
                if (newVehicleSafetyAccessoryPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newVehicleSafetyAccessoryPM.ChangeSetOp = "Update";
                }
                else {
                        newVehicleSafetyAccessoryPM.ChangeSetOp = "Insert";
                }
 
                newVehicleSafetyAccessoryPM.OldEntityPM = null;
                newVehicleSafetyAccessoryPM.EntityParentPM = null;
            }
			 newVehicleSafetyAccessoryPM.DisableMarkAsDirty = false;
			 newVehicleSafetyAccessoryPM.IsDirty = false;
            entityPM.VehicleSafetyAccessories.push(newVehicleSafetyAccessoryPM);
        }
        if (oldVehicleSafetyAccessories) {
            
            for (var itemKey in oldVehicleSafetyAccessories) {
                if (entityPM.VehicleSafetyAccessories.filter(p=> p.UniqueKey === oldVehicleSafetyAccessories[itemKey].UniqueKey).length === 0) {
				
                    if (oldVehicleSafetyAccessories[itemKey]) {
                        //oldVehicleSafetyAccessories[itemKey].ChangeSetOp = "Delete";
                        //entityPM.VehicleSafetyAccessories.push(oldVehicleSafetyAccessories[itemKey]);
						var oldItemJson = oldVehicleSafetyAccessories[itemKey];
                        var deletedPM: VehicleSafetyAccessoryPM = new VehicleSafetyAccessoryPM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.VehicleSafetyAccessories.push(deletedPM);
                    }
                }
            }
        }
    }
    MapVehicleOwners(entityPM: VehiclePM, jsonPM: any, mapParent: boolean = true) {

        var oldVehicleOwners: VehicleOwnerPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldVehicleOwners = entityPM.OldEntityPM.VehicleOwners;
        }

        entityPM.VehicleOwners = new Array<VehicleOwnerPM>();
        for (var item in jsonPM.VehicleOwners) {
            var jItem = jsonPM.VehicleOwners[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newVehicleOwnerPM: VehicleOwnerPM;
	  
            if (mapParent) {
                newVehicleOwnerPM = new VehicleOwnerPM(entityPM);
            }
            else
            {
                newVehicleOwnerPM = new VehicleOwnerPM(null);
            }
 			newVehicleOwnerPM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newVehicleOwnerPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newVehicleOwnerPM.UniqueKey = Guid.newGuid();
                newVehicleOwnerPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newVehicleOwnerPM.OldEntityPM = this.clone(newVehicleOwnerPM);

				
            }
            else {
                if (newVehicleOwnerPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newVehicleOwnerPM.ChangeSetOp = "Update";
                }
                else {
                        newVehicleOwnerPM.ChangeSetOp = "Insert";
                }
 
                newVehicleOwnerPM.OldEntityPM = null;
                newVehicleOwnerPM.EntityParentPM = null;
            }
			 newVehicleOwnerPM.DisableMarkAsDirty = false;
			 newVehicleOwnerPM.IsDirty = false;
            entityPM.VehicleOwners.push(newVehicleOwnerPM);
        }
        if (oldVehicleOwners) {
            
            for (var itemKey in oldVehicleOwners) {
                if (entityPM.VehicleOwners.filter(p=> p.UniqueKey === oldVehicleOwners[itemKey].UniqueKey).length === 0) {
				
                    if (oldVehicleOwners[itemKey]) {
                        //oldVehicleOwners[itemKey].ChangeSetOp = "Delete";
                        //entityPM.VehicleOwners.push(oldVehicleOwners[itemKey]);
						var oldItemJson = oldVehicleOwners[itemKey];
                        var deletedPM: VehicleOwnerPM = new VehicleOwnerPM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.VehicleOwners.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: VehiclePM;
			entityPM = new VehiclePM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
