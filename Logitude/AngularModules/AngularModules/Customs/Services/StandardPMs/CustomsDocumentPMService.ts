//------------------------------------------------------------------------------
// <auto-generated>
//     Angular 9.1.1
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated> C:\LWC\Logitude\Logitude.Customs.MetaData\AutoGenerators\CustomClassesGenerator.tt
//------------------------------------------------------------------------------
import { Injectable } from '@angular/core';
import { HttpClient, HttpResponse } from '@angular/common/http';
import { catchError, map } from 'rxjs/operators';
import { defer, of } from 'rxjs';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {CustomsDocumentPM} from '../../EntityPMs/CustomsDocumentPM';

import {CustomsDocumentMetaDataValuePM} from '../../EntityPMs/CustomsDocumentMetaDataValuePM';

@Injectable()

export class CustomsDocumentPMService {
 private _http: HttpClient;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.HttpClient;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/customsdocuments';      
    }

	get(documentsfilingid: string) {       

		var callTime = new Date();		

		return defer(() => {
			return this._http.get(this._apiUrl + '/getsingle?' + 'documentsfilingid=' + documentsfilingid, ServiceHelper.GetHttpFullHeaders())
				.pipe(
					map((response: HttpResponse<any>) => {
						var pm = response.body;
				
						var entity: CustomsDocumentPM;
						if (pm) {
							entity = this.MapJsonToEntityPM(pm);
						}

						var serviceResponse: ServiceResponse = new ServiceResponse();
						serviceResponse.Result = entity;
              
						var servertime = response.headers.get('ServerExecutionTime');
						PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "CustomsDocument", "GetSinglePM", 'documentsfilingid=' + documentsfilingid);
				 
						return serviceResponse;

					}),
					
					catchError(ServiceHelper.HandleServiceError));
		});                    
	}

	insert(entityPM: CustomsDocumentPM) {
 
		var callTime = new Date();  
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();                
			var errorsArray = validator.Validate("Customs.CustomsDocument", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: CustomsDocumentPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.post(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {

							var pm = response.body;
							if (pm) {
								var mappedResult: CustomsDocumentPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}						

							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "CustomsDocument", "SaveChanges", "");                    
												                             
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

	update(entityPM: CustomsDocumentPM) {

		var callTime = new Date();     
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();               
			var errorsArray = validator.Validate("Customs.CustomsDocument", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: CustomsDocumentPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.put(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {
                 
							var pm = response.body;
							if (pm) {
								var mappedResult: CustomsDocumentPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}
							 
							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "CustomsDocument", "SaveChanges", "");                    
					                           
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: CustomsDocumentPM = null) {

         
        if (!entityPM) {
            
            entityPM = new CustomsDocumentPM();
			entityPM.DisableMarkAsDirty = true;
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapCustomsDocumentMetaDataValues(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.CustomsDocumentMetaDataValues = [];
            for (var item in entityPM.CustomsDocumentMetaDataValues) {
            var myCustomsDocumentMetaDataValuePM = entityPM.CustomsDocumentMetaDataValues[item];
            var newCustomsDocumentMetaDataValuePM: CustomsDocumentMetaDataValuePM = this.clone(myCustomsDocumentMetaDataValuePM);
						
							 
            entityPM.OldEntityPM.CustomsDocumentMetaDataValues.push(newCustomsDocumentMetaDataValuePM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
	    entityPM.DisableMarkAsDirty = false;

        return entityPM;
    }

    MapCustomsDocumentMetaDataValues(entityPM: CustomsDocumentPM, jsonPM: any, mapParent: boolean = true) {

        var oldCustomsDocumentMetaDataValues: CustomsDocumentMetaDataValuePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCustomsDocumentMetaDataValues = entityPM.OldEntityPM.CustomsDocumentMetaDataValues;
        }

        entityPM.CustomsDocumentMetaDataValues = new Array<CustomsDocumentMetaDataValuePM>();
        for (var item in jsonPM.CustomsDocumentMetaDataValues) {
            var jItem = jsonPM.CustomsDocumentMetaDataValues[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newCustomsDocumentMetaDataValuePM: CustomsDocumentMetaDataValuePM;
	  
            if (mapParent) {
                newCustomsDocumentMetaDataValuePM = new CustomsDocumentMetaDataValuePM(entityPM);
            }
            else
            {
                newCustomsDocumentMetaDataValuePM = new CustomsDocumentMetaDataValuePM(null);
            }
 			newCustomsDocumentMetaDataValuePM.DisableMarkAsDirty = true;
               
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
				                  var pmProperty = pmKeysArray[pmKey];
                newCustomsDocumentMetaDataValuePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newCustomsDocumentMetaDataValuePM.UniqueKey = Guid.newGuid();
                newCustomsDocumentMetaDataValuePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newCustomsDocumentMetaDataValuePM.OldEntityPM = this.clone(newCustomsDocumentMetaDataValuePM);

				
            }
            else {
                if (newCustomsDocumentMetaDataValuePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newCustomsDocumentMetaDataValuePM.ChangeSetOp = "Update";
                }
                else {
                        newCustomsDocumentMetaDataValuePM.ChangeSetOp = "Insert";
                }
 
                newCustomsDocumentMetaDataValuePM.OldEntityPM = null;
                newCustomsDocumentMetaDataValuePM.EntityParentPM = null;
            }
			 newCustomsDocumentMetaDataValuePM.DisableMarkAsDirty = false;
			 newCustomsDocumentMetaDataValuePM.IsDirty = false;
            entityPM.CustomsDocumentMetaDataValues.push(newCustomsDocumentMetaDataValuePM);
        }
        if (oldCustomsDocumentMetaDataValues) {
            
            for (var itemKey in oldCustomsDocumentMetaDataValues) {
                if (entityPM.CustomsDocumentMetaDataValues.filter(p=> p.UniqueKey === oldCustomsDocumentMetaDataValues[itemKey].UniqueKey).length === 0) {
				
                    if (oldCustomsDocumentMetaDataValues[itemKey]) {
                        //oldCustomsDocumentMetaDataValues[itemKey].ChangeSetOp = "Delete";
                        //entityPM.CustomsDocumentMetaDataValues.push(oldCustomsDocumentMetaDataValues[itemKey]);
						var oldItemJson = oldCustomsDocumentMetaDataValues[itemKey];
                        var deletedPM: CustomsDocumentMetaDataValuePM = new CustomsDocumentMetaDataValuePM(null);
						deletedPM.DisableMarkAsDirty = true;
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

					    deletedPM.DisableMarkAsDirty = false;
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.CustomsDocumentMetaDataValues.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: CustomsDocumentPM;
			entityPM = new CustomsDocumentPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
