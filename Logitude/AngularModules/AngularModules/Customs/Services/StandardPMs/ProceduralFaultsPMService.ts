//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import {Injectable} from '@angular/core';
import {Http, Headers} from '@angular/http';
import {Observable}     from 'rxjs/Rx';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';

import {ProceduralFaultPM} from '../../EntityPMs/ProceduralFaultPM';

import {ProceduralFaultsConnEntityPM} from '../../EntityPMs/ProceduralFaultsConnEntityPM';

@Injectable()

export class ProceduralFaultPMService {
 private _http: Http;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.Http;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/proceduralfaults';      
    }

 get(id: string) {
         
         
        var authHeader = new Headers();
        authHeader.append('Token', SessionInfo.Token);
		
		 return Observable.defer(() => {
                return this._http.get(this._apiUrl+'/getsingle?'+'id=' + id, {
                    headers: authHeader
                }).map(response => {
                    var pm = response.json();
                    
					
                    var entity: ProceduralFaultPM;
					if(pm)
					{
                      entity = this.MapJsonToEntityPM(pm);
                    }

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
                serviceResponse.Result = entity;
                return serviceResponse;

            }).catch(ServiceHelper.HandleServiceError);
            });                    
    }

	 insert(entityPM: ProceduralFaultPM) {
         
        return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
                 
                var errorsArray = validator.Validate("Customs.ProceduralFault", entityPM);
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: ProceduralFaultPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.post(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((res) => {
                            var pm = res.json();
							if(pm)
							{
                               var mappedResult:  ProceduralFaultPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							}
							
							 
                            
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );
    }

    update(entityPM: ProceduralFaultPM) {

         
            return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
               
                var errorsArray = validator.Validate("Customs.ProceduralFault", entityPM);
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: ProceduralFaultPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.put(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((res) => {
                            var pm = res.json();
							if(pm)
							{
                               var mappedResult:  ProceduralFaultPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							 }
							 
                           
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );

    }

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: ProceduralFaultPM = null) {

         
        if (!entityPM) {
            
            entityPM = new ProceduralFaultPM();
        }
           
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties") {

                continue;
            }
                var property = jsonPMKeys[key];
                entityPM[property] = jsonPM[property];
            }
			
               this.MapProceduralFaultsConnEntities(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            entityPM.IsDirty = false;

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.ProceduralFaultsConnEntities = [];
            for (var item in entityPM.ProceduralFaultsConnEntities) {
            var myProceduralFaultsConnEntityPM = entityPM.ProceduralFaultsConnEntities[item];
            var newProceduralFaultsConnEntityPM: ProceduralFaultsConnEntityPM = this.clone(myProceduralFaultsConnEntityPM);
						
							 
            entityPM.OldEntityPM.ProceduralFaultsConnEntities.push(newProceduralFaultsConnEntityPM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }

        return entityPM;
    }

    MapProceduralFaultsConnEntities(entityPM: ProceduralFaultPM, jsonPM: any, mapParent: boolean = true) {

        var oldProceduralFaultsConnEntities: ProceduralFaultsConnEntityPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldProceduralFaultsConnEntities = entityPM.OldEntityPM.ProceduralFaultsConnEntities;
        }

        entityPM.ProceduralFaultsConnEntities = new Array<ProceduralFaultsConnEntityPM>();
        for (var item in jsonPM.ProceduralFaultsConnEntities) {
            var jItem = jsonPM.ProceduralFaultsConnEntities[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newProceduralFaultsConnEntityPM: ProceduralFaultsConnEntityPM;
	  
            if (mapParent) {
                newProceduralFaultsConnEntityPM = new ProceduralFaultsConnEntityPM(entityPM);
            }
            else
            {
                newProceduralFaultsConnEntityPM = new ProceduralFaultsConnEntityPM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newProceduralFaultsConnEntityPM[pmProperty] = jItem[pmProperty];
            }
            newProceduralFaultsConnEntityPM.IsDirty = false;
			 
            if (mapParent) {
                newProceduralFaultsConnEntityPM.UniqueKey = Guid.newGuid();
                newProceduralFaultsConnEntityPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newProceduralFaultsConnEntityPM.OldEntityPM = this.clone(newProceduralFaultsConnEntityPM);

				
            }
            else {
                if (newProceduralFaultsConnEntityPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newProceduralFaultsConnEntityPM.ChangeSetOp = "Update";
                }
                else {
                        newProceduralFaultsConnEntityPM.ChangeSetOp = "Insert";
                }
 
                newProceduralFaultsConnEntityPM.OldEntityPM = null;
                newProceduralFaultsConnEntityPM.EntityParentPM = null;
            }
			

            entityPM.ProceduralFaultsConnEntities.push(newProceduralFaultsConnEntityPM);
        }
        if (oldProceduralFaultsConnEntities) {
            
            for (var itemKey in oldProceduralFaultsConnEntities) {
                if (entityPM.ProceduralFaultsConnEntities.filter(p=> p.UniqueKey === oldProceduralFaultsConnEntities[itemKey].UniqueKey).length === 0) {
				
                    if (oldProceduralFaultsConnEntities[itemKey]) {
                        //oldProceduralFaultsConnEntities[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ProceduralFaultsConnEntities.push(oldProceduralFaultsConnEntities[itemKey]);
						var oldItemJson = oldProceduralFaultsConnEntities[itemKey];
                        var deletedPM: ProceduralFaultsConnEntityPM = new ProceduralFaultsConnEntityPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.ProceduralFaultsConnEntities.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: ProceduralFaultPM;
			entityPM = new ProceduralFaultPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
