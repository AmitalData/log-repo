import {RequestParamsBase} from './RequestParamsBase';

export class GenericRequestParams extends RequestParamsBase {
    
    public AppicationId: string; //I changed it from ApplicationId to AppicationId because in server side it is AppicationId----Mohammad

}