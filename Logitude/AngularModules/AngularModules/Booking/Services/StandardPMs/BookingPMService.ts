//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import {Injectable} from '@angular/core';
import {Http, Headers} from '@angular/http';
import {Observable}     from 'rxjs/Rx';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {BookingPM} from '../../EntityPMs/BookingPM';

import {BookingPackagePM} from '../../EntityPMs/BookingPackagePM';
import {BookingAnswerPM} from '../../EntityPMs/BookingAnswerPM';

@Injectable()

export class BookingPMService {
 private _http: Http;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.Http;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/bookings';      
    }

 get(id: string) {
         
         
        var authHeader = new Headers();
        authHeader.append('Token', SessionInfo.Token);
        var callTime = new Date();		
		 return Observable.defer(() => {
                return this._http.get(this._apiUrl+'/getsingle?'+'id=' + id, {
                    headers: authHeader
                }).map(response => {
                    var pm = response.json();

                   
					
                    var entity: BookingPM;
					if(pm)
					{
                      entity = this.MapJsonToEntityPM(pm);
                    }

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
                serviceResponse.Result = entity;
              
			    var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Booking", "GetSinglePM", 'id=' + id);
				 
                return serviceResponse;

            }).catch(ServiceHelper.HandleServiceError);
            });                    
    }

	 insert(entityPM: BookingPM) {
 
        var callTime = new Date();        
        return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
                 
                var errorsArray = validator.Validate("Booking", entityPM);
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: BookingPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.post(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  BookingPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							}
							

                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Booking", "SaveChanges", "");                    
												 
                            
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );
    }

    update(entityPM: BookingPM) {

            var callTime = new Date();         
            return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
               
                var errorsArray = validator.Validate("Booking", entityPM);
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: BookingPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.put(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {
                 

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  BookingPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							 }
							 
                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Booking", "SaveChanges", "");                    
					                           
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );

    }

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: BookingPM = null) {

         
        if (!entityPM) {
            
            entityPM = new BookingPM();
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapBookingPackages(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapBookingAnswers(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.BookingPackages = [];
            for (var item in entityPM.BookingPackages) {
            var myBookingPackagePM = entityPM.BookingPackages[item];
            var newBookingPackagePM: BookingPackagePM = this.clone(myBookingPackagePM);
						
							 
            entityPM.OldEntityPM.BookingPackages.push(newBookingPackagePM);
            }
			   			   			   
            entityPM.OldEntityPM.BookingAnswers = [];
            for (var item in entityPM.BookingAnswers) {
            var myBookingAnswerPM = entityPM.BookingAnswers[item];
            var newBookingAnswerPM: BookingAnswerPM = this.clone(myBookingAnswerPM);
						
							 
            entityPM.OldEntityPM.BookingAnswers.push(newBookingAnswerPM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
        return entityPM;
    }

    MapBookingPackages(entityPM: BookingPM, jsonPM: any, mapParent: boolean = true) {

        var oldBookingPackages: BookingPackagePM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldBookingPackages = entityPM.OldEntityPM.BookingPackages;
        }

        entityPM.BookingPackages = new Array<BookingPackagePM>();
        for (var item in jsonPM.BookingPackages) {
            var jItem = jsonPM.BookingPackages[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newBookingPackagePM: BookingPackagePM;
	  
            if (mapParent) {
                newBookingPackagePM = new BookingPackagePM(entityPM);
            }
            else
            {
                newBookingPackagePM = new BookingPackagePM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newBookingPackagePM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newBookingPackagePM.UniqueKey = Guid.newGuid();
                newBookingPackagePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newBookingPackagePM.OldEntityPM = this.clone(newBookingPackagePM);

				
            }
            else {
                if (newBookingPackagePM.UniqueKey) {

                    if (jItem.IsDirty)
                        newBookingPackagePM.ChangeSetOp = "Update";
                }
                else {
                        newBookingPackagePM.ChangeSetOp = "Insert";
                }
 
                newBookingPackagePM.OldEntityPM = null;
                newBookingPackagePM.EntityParentPM = null;
            }
			
			 newBookingPackagePM.IsDirty = false;
            entityPM.BookingPackages.push(newBookingPackagePM);
        }
        if (oldBookingPackages) {
            
            for (var itemKey in oldBookingPackages) {
                if (entityPM.BookingPackages.filter(p=> p.UniqueKey === oldBookingPackages[itemKey].UniqueKey).length === 0) {
				
                    if (oldBookingPackages[itemKey]) {
                        //oldBookingPackages[itemKey].ChangeSetOp = "Delete";
                        //entityPM.BookingPackages.push(oldBookingPackages[itemKey]);
						var oldItemJson = oldBookingPackages[itemKey];
                        var deletedPM: BookingPackagePM = new BookingPackagePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.BookingPackages.push(deletedPM);
                    }
                }
            }
        }
    }
    MapBookingAnswers(entityPM: BookingPM, jsonPM: any, mapParent: boolean = true) {

        var oldBookingAnswers: BookingAnswerPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldBookingAnswers = entityPM.OldEntityPM.BookingAnswers;
        }

        entityPM.BookingAnswers = new Array<BookingAnswerPM>();
        for (var item in jsonPM.BookingAnswers) {
            var jItem = jsonPM.BookingAnswers[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newBookingAnswerPM: BookingAnswerPM;
	  
            if (mapParent) {
                newBookingAnswerPM = new BookingAnswerPM(entityPM);
            }
            else
            {
                newBookingAnswerPM = new BookingAnswerPM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newBookingAnswerPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newBookingAnswerPM.UniqueKey = Guid.newGuid();
                newBookingAnswerPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newBookingAnswerPM.OldEntityPM = this.clone(newBookingAnswerPM);

				
            }
            else {
                if (newBookingAnswerPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newBookingAnswerPM.ChangeSetOp = "Update";
                }
                else {
                        newBookingAnswerPM.ChangeSetOp = "Insert";
                }
 
                newBookingAnswerPM.OldEntityPM = null;
                newBookingAnswerPM.EntityParentPM = null;
            }
			
			 newBookingAnswerPM.IsDirty = false;
            entityPM.BookingAnswers.push(newBookingAnswerPM);
        }
        if (oldBookingAnswers) {
            
            for (var itemKey in oldBookingAnswers) {
                if (entityPM.BookingAnswers.filter(p=> p.UniqueKey === oldBookingAnswers[itemKey].UniqueKey).length === 0) {
				
                    if (oldBookingAnswers[itemKey]) {
                        //oldBookingAnswers[itemKey].ChangeSetOp = "Delete";
                        //entityPM.BookingAnswers.push(oldBookingAnswers[itemKey]);
						var oldItemJson = oldBookingAnswers[itemKey];
                        var deletedPM: BookingAnswerPM = new BookingAnswerPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.BookingAnswers.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: BookingPM;
			entityPM = new BookingPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
