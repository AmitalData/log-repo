//file not found! child composition CustomField
//------------------------------------------------------------------------------
// <auto-generated>
//     Angular 9.1.1
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { Injectable } from '@angular/core';
import { HttpClient, HttpResponse } from '@angular/common/http';
import { catchError, map } from 'rxjs/operators';
import { defer, of } from 'rxjs';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {CardPM} from '../../EntityPMs/CardPM';

import {AddressPM} from '../../EntityPMs/AddressPM';
import {ContactPM} from '../../EntityPMs/ContactPM';

import {CardContactAdditionalServicePM} from '../../EntityPMs/CardContactAdditionalServicePM';
import {CardContactProductPM} from '../../EntityPMs/CardContactProductPM';
// import {CustomFieldPM} from '../../EntityPMs/CustomFieldPM';

@Injectable()

export class CardPMService {
 private _http: HttpClient;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.HttpClient;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/cards';      
    }

	get(id: string) {       

		var callTime = new Date();		

		return defer(() => {
			return this._http.get(this._apiUrl + '/getsingle?' + 'id=' + id, ServiceHelper.GetHttpFullHeaders())
				.pipe(
					map((response: HttpResponse<any>) => {
						var pm = response.body;
				
						var entity: CardPM;
						if (pm) {
							entity = this.MapJsonToEntityPM(pm);
						}

						var serviceResponse: ServiceResponse = new ServiceResponse();
						serviceResponse.Result = entity;
              
						var servertime = response.headers.get('ServerExecutionTime');
						PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Card", "GetSinglePM", 'id=' + id);
				 
						return serviceResponse;

					}),
					
					catchError(ServiceHelper.HandleServiceError));
		});                    
	}

	insert(entityPM: CardPM) {
 
		var callTime = new Date();  
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();                
			var errorsArray = validator.Validate("Card", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: CardPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.post(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {

							var pm = response.body;
							if (pm) {
								var mappedResult: CardPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}						

							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Card", "SaveChanges", "");                    
												                             
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

	update(entityPM: CardPM) {

		var callTime = new Date();     
		
		return defer(() => {

			var serviceResponse: ServiceResponse = new ServiceResponse();
			var validator: ClassLevelValidator = new ClassLevelValidator();               
			var errorsArray = validator.Validate("Card", entityPM);


			if (errorsArray.length == 0) {

				var mappedEntity: CardPM = this.MapJsonToEntityPM(entityPM, false);
				
				return this._http.put(this._apiUrl, JSON.stringify(mappedEntity), ServiceHelper.GetHttpFullHeaders())
					.pipe(
						map((response: HttpResponse<any>) => {
                 
							var pm = response.body;
							if (pm) {
								var mappedResult: CardPM = this.MapJsonToEntityPM(pm, true, entityPM);
								serviceResponse.Result = mappedResult;
							}
							 
							var servertime = response.headers.get('ServerExecutionTime');
							PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Card", "SaveChanges", "");                    
					                           
							return serviceResponse;
						}),

						catchError(ServiceHelper.HandleServiceError));
			}

			else {
				serviceResponse.HasError = true;
				serviceResponse.ErrorsArray = errorsArray;
				return of(serviceResponse);
			}
		});
	}

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: CardPM = null) {

         
        if (!entityPM) {
            
            entityPM = new CardPM();
			entityPM.DisableMarkAsDirty = true;
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 51; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapAddresses(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapContacts(entityPM, jsonPM, mapParent); // Call composition tables map methods
               this.MapCustomFields(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.Addresses = [];
            for (var item in entityPM.Addresses) {
            var myAddressPM = entityPM.Addresses[item];
            var newAddressPM: AddressPM = this.clone(myAddressPM);
						
							 
            entityPM.OldEntityPM.Addresses.push(newAddressPM);
            }
			   			   			   
            entityPM.OldEntityPM.Contacts = [];
            for (var item in entityPM.Contacts) {
            var myContactPM = entityPM.Contacts[item];
            var newContactPM: ContactPM = this.clone(myContactPM);
						
                newContactPM.CardContactAdditionalServices = [];
                for (var k in myContactPM.CardContactAdditionalServices) {
				    var myCardContactAdditionalServicePM =myContactPM.CardContactAdditionalServices[k];
				    var newCardContactAdditionalServicePM=this.clone(myContactPM.CardContactAdditionalServices[k]);
                    newContactPM.CardContactAdditionalServices.push(newCardContactAdditionalServicePM);

					                 }
                newContactPM.CardContactProducts = [];
                for (var k in myContactPM.CardContactProducts) {
				    var myCardContactProductPM =myContactPM.CardContactProducts[k];
				    var newCardContactProductPM=this.clone(myContactPM.CardContactProducts[k]);
                    newContactPM.CardContactProducts.push(newCardContactProductPM);

					                 }
							 
            entityPM.OldEntityPM.Contacts.push(newContactPM);
            }
			   			   			   
            // entityPM.OldEntityPM.CustomFields = [];
            // for (var item in entityPM.CustomFields) {
            // var myCustomFieldPM = entityPM.CustomFields[item];
            // var newCustomFieldPM: CustomFieldPM = this.clone(myCustomFieldPM);
						
							 
            // entityPM.OldEntityPM.CustomFields.push(newCustomFieldPM);
            // }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
	    entityPM.DisableMarkAsDirty = false;

        return entityPM;
    }

    MapAddresses(entityPM: CardPM, jsonPM: any, mapParent: boolean = true) {

        entityPM.Addresses = new Array<AddressPM>();
        for (var item in jsonPM.Addresses) {

            var jItem = jsonPM.Addresses[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newAddressPM: AddressPM;
            newAddressPM = new AddressPM();
		    newAddressPM.DisableMarkAsDirty = true;                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
			
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newAddressPM[pmProperty] = jItem[pmProperty];
            }
			newAddressPM.DisableMarkAsDirty = false;
            newAddressPM.IsDirty = false;
            entityPM.Addresses.push(newAddressPM);
        }
    }
    MapContacts(entityPM: CardPM, jsonPM: any, mapParent: boolean = true) {

        entityPM.Contacts = new Array<ContactPM>();
        for (var item in jsonPM.Contacts) {

            var jItem = jsonPM.Contacts[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newContactPM: ContactPM;
            newContactPM = new ContactPM();
		    newContactPM.DisableMarkAsDirty = true;                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
			
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newContactPM[pmProperty] = jItem[pmProperty];
            }
			newContactPM.DisableMarkAsDirty = false;
            newContactPM.IsDirty = false;
            entityPM.Contacts.push(newContactPM);
        }
    }
    MapCustomFields(entityPM: CardPM, jsonPM: any, mapParent: boolean = true) {

      // entityPM.CustomFields = new Array<CustomFieldPM>();
        // for (var item in jsonPM.CustomFields) {

        //     var jItem = jsonPM.CustomFields[item];
        //     if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
        //         continue;
        //     }
        //     var newCustomFieldPM: CustomFieldPM;
        //     newCustomFieldPM = new CustomFieldPM();
		//     newCustomFieldPM.DisableMarkAsDirty = true;                
        //     var pmKeysArray = Object.keys(jItem);
        //     for (var pmKey in pmKeysArray) {
			
        //         if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
        //             continue;
        //         }
        //         var pmProperty = pmKeysArray[pmKey];
        //         newCustomFieldPM[pmProperty] = jItem[pmProperty];
        //     }
		// 	newCustomFieldPM.DisableMarkAsDirty = false;
        //     newCustomFieldPM.IsDirty = false;
        //  //  entityPM.CustomFields.push(newCustomFieldPM);
        // }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: CardPM;
			entityPM = new CardPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
