//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import {Injectable} from '@angular/core';
import {Http, Headers} from '@angular/http';
import {Observable}     from 'rxjs/Rx';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {CustomerTenantAccessPM} from '../../EntityPMs/CustomerTenantAccessPM';

import {CustomerTenantAccessCardPM} from '../../EntityPMs/CustomerTenantAccessCardPM';

@Injectable()

export class CustomerTenantAccessPMService {
 private _http: Http;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.Http;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/customertenantaccesses';      
    }

 get(id: string) {
         
         
        var authHeader = new Headers();
        authHeader.append('Token', SessionInfo.Token);
        var callTime = new Date();		
		 return Observable.defer(() => {
                return this._http.get(this._apiUrl+'/getsingle?'+'id=' + id, {
                    headers: authHeader
                }).map(response => {
                    var pm = response.json();

                   
					
                    var entity: CustomerTenantAccessPM;
					if(pm)
					{
                      entity = this.MapJsonToEntityPM(pm);
                    }

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
                serviceResponse.Result = entity;
              
			    var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "CustomerTenantAccess", "GetSinglePM", 'id=' + id);
				 
                return serviceResponse;

            }).catch(ServiceHelper.HandleServiceError);
            });                    
    }

	 insert(entityPM: CustomerTenantAccessPM) {
 
        var callTime = new Date();        
        return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
                 
                var errorsArray = validator.Validate("CustomerTenantAccess", entityPM);
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: CustomerTenantAccessPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.post(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  CustomerTenantAccessPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							}
							

                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "CustomerTenantAccess", "SaveChanges", "");                    
												 
                            
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );
    }

    update(entityPM: CustomerTenantAccessPM) {

            var callTime = new Date();         
            return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
               
                var errorsArray = validator.Validate("CustomerTenantAccess", entityPM);
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: CustomerTenantAccessPM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.put(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {
                 

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  CustomerTenantAccessPM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							 }
							 
                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "CustomerTenantAccess", "SaveChanges", "");                    
					                           
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );

    }

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: CustomerTenantAccessPM = null) {

         
        if (!entityPM) {
            
            entityPM = new CustomerTenantAccessPM();
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapCustomerTenantAccessCards(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.CustomerTenantAccessCards = [];
            for (var item in entityPM.CustomerTenantAccessCards) {
            var myCustomerTenantAccessCardPM = entityPM.CustomerTenantAccessCards[item];
            var newCustomerTenantAccessCardPM: CustomerTenantAccessCardPM = this.clone(myCustomerTenantAccessCardPM);
						
							 
            entityPM.OldEntityPM.CustomerTenantAccessCards.push(newCustomerTenantAccessCardPM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
        return entityPM;
    }

    MapCustomerTenantAccessCards(entityPM: CustomerTenantAccessPM, jsonPM: any, mapParent: boolean = true) {

        var oldCustomerTenantAccessCards: CustomerTenantAccessCardPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCustomerTenantAccessCards = entityPM.OldEntityPM.CustomerTenantAccessCards;
        }

        entityPM.CustomerTenantAccessCards = new Array<CustomerTenantAccessCardPM>();
        for (var item in jsonPM.CustomerTenantAccessCards) {
            var jItem = jsonPM.CustomerTenantAccessCards[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newCustomerTenantAccessCardPM: CustomerTenantAccessCardPM;
	  
            if (mapParent) {
                newCustomerTenantAccessCardPM = new CustomerTenantAccessCardPM(entityPM);
            }
            else
            {
                newCustomerTenantAccessCardPM = new CustomerTenantAccessCardPM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newCustomerTenantAccessCardPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newCustomerTenantAccessCardPM.UniqueKey = Guid.newGuid();
                newCustomerTenantAccessCardPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newCustomerTenantAccessCardPM.OldEntityPM = this.clone(newCustomerTenantAccessCardPM);

				
            }
            else {
                if (newCustomerTenantAccessCardPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newCustomerTenantAccessCardPM.ChangeSetOp = "Update";
                }
                else {
                        newCustomerTenantAccessCardPM.ChangeSetOp = "Insert";
                }
 
                newCustomerTenantAccessCardPM.OldEntityPM = null;
                newCustomerTenantAccessCardPM.EntityParentPM = null;
            }
			
			 newCustomerTenantAccessCardPM.IsDirty = false;
            entityPM.CustomerTenantAccessCards.push(newCustomerTenantAccessCardPM);
        }
        if (oldCustomerTenantAccessCards) {
            
            for (var itemKey in oldCustomerTenantAccessCards) {
                if (entityPM.CustomerTenantAccessCards.filter(p=> p.UniqueKey === oldCustomerTenantAccessCards[itemKey].UniqueKey).length === 0) {
				
                    if (oldCustomerTenantAccessCards[itemKey]) {
                        //oldCustomerTenantAccessCards[itemKey].ChangeSetOp = "Delete";
                        //entityPM.CustomerTenantAccessCards.push(oldCustomerTenantAccessCards[itemKey]);
						var oldItemJson = oldCustomerTenantAccessCards[itemKey];
                        var deletedPM: CustomerTenantAccessCardPM = new CustomerTenantAccessCardPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.CustomerTenantAccessCards.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: CustomerTenantAccessPM;
			entityPM = new CustomerTenantAccessPM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
