//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import {Injectable} from '@angular/core';
import {Http, Headers} from '@angular/http';
import {Observable}     from 'rxjs/Rx';
import {ServiceResponse} from '../../../Infrastructure/DataContracts/ServiceResponse';
import {ClassLevelValidator} from '../../../Infrastructure/Validators/ClassLevelValidator';
import {Guid} from '../../../Infrastructure/Utilities/Guid';
import {InfraSettings} from '../../../Infrastructure/Utilities/InfraSettings';
import {ServiceHelper} from '../../../Infrastructure/Utilities/ServiceHelper';
import {SessionInfo} from '../../../Infrastructure/Utilities/SessionInfo';
import {CustomFieldClass} from '../../../Infrastructure/DataContracts/CustomFieldClass'
import {PerformanceLogger} from '../../../Infrastructure/Utilities/PerformanceLogger';

import {ChargesTypePM} from '../../EntityPMs/ChargesTypePM';

import {ChargeTypeAccountingPM} from '../../EntityPMs/ChargeTypeAccountingPM';
import {ChargesTypeValidator} from '../../Validators/ChargesTypeValidator';

@Injectable()

export class ChargesTypePMService {
 private _http: Http;
 private _apiUrl: string;
 constructor() {
        this._http = ServiceHelper.Http;
        this._apiUrl = ServiceHelper.GetLogitudeURL() + 'api/chargestypes';      
    }

 get(id: string) {
         
         
        var authHeader = new Headers();
        authHeader.append('Token', SessionInfo.Token);
        var callTime = new Date();		
		 return Observable.defer(() => {
                return this._http.get(this._apiUrl+'/getsingle?'+'id=' + id, {
                    headers: authHeader
                }).map(response => {
                    var pm = response.json();

                   
					
                    var entity: ChargesTypePM;
					if(pm)
					{
                      entity = this.MapJsonToEntityPM(pm);
                    }

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
                serviceResponse.Result = entity;
              
			    var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "ChargesType", "GetSinglePM", 'id=' + id);
				 
                return serviceResponse;

            }).catch(ServiceHelper.HandleServiceError);
            });                    
    }

	 insert(entityPM: ChargesTypePM) {
 
        var callTime = new Date();        
        return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
                 
                var errorsArray = validator.Validate("ChargesType", entityPM);
                var customValidator :ChargesTypeValidator = new ChargesTypeValidator();
                var validationErrorsArr = customValidator.Validate(entityPM);
				if(validationErrorsArr)
				{
					errorsArray = errorsArray.concat(validationErrorsArr);
				}
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: ChargesTypePM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.post(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  ChargesTypePM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							}
							

                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "ChargesType", "SaveChanges", "");                    
												 
                            
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );
    }

    update(entityPM: ChargesTypePM) {

            var callTime = new Date();         
            return Observable.defer(() => {

                var authHeader = new Headers();
                authHeader.append('Token', SessionInfo.Token);
                authHeader.append('Content-Type', 'application/json');

                var validator: ClassLevelValidator;
                 
                validator = new ClassLevelValidator();
               
                var errorsArray = validator.Validate("ChargesType", entityPM);
                var customValidator :ChargesTypeValidator = new ChargesTypeValidator();
                var validationErrorsArr = customValidator.Validate(entityPM);
				if(validationErrorsArr)
				{
					errorsArray = errorsArray.concat(validationErrorsArr);
				}
                 

                var serviceResponse: ServiceResponse;
                serviceResponse = new ServiceResponse();
				 if (errorsArray.length == 0) {
                    var mappedEntity: ChargesTypePM;
                    mappedEntity = this.MapJsonToEntityPM(entityPM, false);
				
				    return this._http.put(this._apiUrl, JSON.stringify(mappedEntity),
                        { headers: authHeader }).map((response) => {
                 

                            var pm = response.json();
							if(pm)
							{
                               var mappedResult:  ChargesTypePM;
                               mappedResult = this.MapJsonToEntityPM(pm,true,entityPM);
							   serviceResponse.Result = mappedResult;
							 }
							 
                            var servertime = response.headers.get('ServerExecutionTime');
                            PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "ChargesType", "SaveChanges", "");                    
					                           
                            return serviceResponse;

                        }).catch(ServiceHelper.HandleServiceError);
                }
                else {

                    serviceResponse.HasError = true;
                    serviceResponse.ErrorsArray = errorsArray;

                    return Observable.of(serviceResponse);
                   
                }
            }

            );

    }

   

	  MapJsonToEntityPM(jsonPM: any, mapParent: boolean = true, entityPM: ChargesTypePM = null) {

         
        if (!entityPM) {
            
            entityPM = new ChargesTypePM();
        }

		var customFields: Array<string> = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
            var jsonPMKeys = Object.keys(jsonPM);

            for (var key in jsonPMKeys) {
			 if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {

                continue;
            }
                var property = jsonPMKeys[key];
				
			  if(customFields.indexOf(property) > -1)
                {
                if (jsonPM[property]) {
                    var customFieldClass: CustomFieldClass = new CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
                 
            }
			
               this.MapChargeTypeAccountings(entityPM, jsonPM, mapParent); // Call composition tables map methods
			 
            

		if (mapParent) {
                entityPM.OldEntityPM = this.clone(entityPM);
			   			   
            entityPM.OldEntityPM.ChargeTypeAccountings = [];
            for (var item in entityPM.ChargeTypeAccountings) {
            var myChargeTypeAccountingPM = entityPM.ChargeTypeAccountings[item];
            var newChargeTypeAccountingPM: ChargeTypeAccountingPM = this.clone(myChargeTypeAccountingPM);
						
							 
            entityPM.OldEntityPM.ChargeTypeAccountings.push(newChargeTypeAccountingPM);
            }
			   
		}
        else {

            entityPM.OldEntityPM = null;
        }
		entityPM.IsDirty = false;
        return entityPM;
    }

    MapChargeTypeAccountings(entityPM: ChargesTypePM, jsonPM: any, mapParent: boolean = true) {

        var oldChargeTypeAccountings: ChargeTypeAccountingPM[] = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldChargeTypeAccountings = entityPM.OldEntityPM.ChargeTypeAccountings;
        }

        entityPM.ChargeTypeAccountings = new Array<ChargeTypeAccountingPM>();
        for (var item in jsonPM.ChargeTypeAccountings) {
            var jItem = jsonPM.ChargeTypeAccountings[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newChargeTypeAccountingPM: ChargeTypeAccountingPM;
	  
            if (mapParent) {
                newChargeTypeAccountingPM = new ChargeTypeAccountingPM(entityPM);
            }
            else
            {
                newChargeTypeAccountingPM = new ChargeTypeAccountingPM(null);
            }
                
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM" )|| pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newChargeTypeAccountingPM[pmProperty] = jItem[pmProperty];
            }
           
			 
            if (mapParent) {
                newChargeTypeAccountingPM.UniqueKey = Guid.newGuid();
                newChargeTypeAccountingPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newChargeTypeAccountingPM.OldEntityPM = this.clone(newChargeTypeAccountingPM);

				
            }
            else {
                if (newChargeTypeAccountingPM.UniqueKey) {

                    if (jItem.IsDirty)
                        newChargeTypeAccountingPM.ChangeSetOp = "Update";
                }
                else {
                        newChargeTypeAccountingPM.ChangeSetOp = "Insert";
                }
 
                newChargeTypeAccountingPM.OldEntityPM = null;
                newChargeTypeAccountingPM.EntityParentPM = null;
            }
			
			 newChargeTypeAccountingPM.IsDirty = false;
            entityPM.ChargeTypeAccountings.push(newChargeTypeAccountingPM);
        }
        if (oldChargeTypeAccountings) {
            
            for (var itemKey in oldChargeTypeAccountings) {
                if (entityPM.ChargeTypeAccountings.filter(p=> p.UniqueKey === oldChargeTypeAccountings[itemKey].UniqueKey).length === 0) {
				
                    if (oldChargeTypeAccountings[itemKey]) {
                        //oldChargeTypeAccountings[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ChargeTypeAccountings.push(oldChargeTypeAccountings[itemKey]);
						var oldItemJson = oldChargeTypeAccountings[itemKey];
                        var deletedPM: ChargeTypeAccountingPM = new ChargeTypeAccountingPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {

                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }

                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }

                      
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        
                        deletedPM.OldEntityPM = null;
                        entityPM.ChargeTypeAccountings.push(deletedPM);
                    }
                }
            }
        }
    }

	  public clone(jsonPM: any) {
        var entityPM: any;
        entityPM = {};

        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }

            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];

        }
        return entityPM;
    }

	  public GetNewEntityPM() {		 
		    var entityPM: ChargesTypePM;
			entityPM = new ChargesTypePM();
			entityPM.Tenant = InfraSettings.TenantPM.Id;
			return entityPM;
    }
		 

}
