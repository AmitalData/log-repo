import { ShipmentSearch } from './ShipmentSearch';
import { browser, by, element } from 'protractor';

describe('Shipment search', () => {
  let page: ShipmentSearch;

  beforeEach(() => {
    page = new ShipmentSearch();
  });

 
  it('Shipment search', function () {
    // page.QuickSearch();

  });
});
 