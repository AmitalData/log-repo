"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
//file not found! child composition QuoteSalesTotal
//file not found! child composition QuoteVATsTotal
//file not found! child composition QuoteFollowUp
//file not found! child composition QuoteDocumentVersion
//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
var core_1 = require("@angular/core");
var http_1 = require("@angular/http");
var Rx_1 = require("rxjs/Rx");
var ServiceResponse_1 = require("../../../Infrastructure/DataContracts/ServiceResponse");
var ClassLevelValidator_1 = require("../../../Infrastructure/Validators/ClassLevelValidator");
var Guid_1 = require("../../../Infrastructure/Utilities/Guid");
var InfraSettings_1 = require("../../../Infrastructure/Utilities/InfraSettings");
var ServiceHelper_1 = require("../../../Infrastructure/Utilities/ServiceHelper");
var SessionInfo_1 = require("../../../Infrastructure/Utilities/SessionInfo");
var CustomFieldClass_1 = require("../../../Infrastructure/DataContracts/CustomFieldClass");
var PerformanceLogger_1 = require("../../../Infrastructure/Utilities/PerformanceLogger");
var QuotePM_1 = require("../../EntityPMs/QuotePM");
var QuoteCostChargePM_1 = require("../../EntityPMs/QuoteCostChargePM");
var QuoteSaleChargePM_1 = require("../../EntityPMs/QuoteSaleChargePM");
var QuoteChargePM_1 = require("../../EntityPMs/QuoteChargePM");
var QuotePriceStepsPM_1 = require("../../EntityPMs/QuotePriceStepsPM");
var QuotePackagePM_1 = require("../../EntityPMs/QuotePackagePM");
var QuoteSalesTotalPM_1 = require("../../EntityPMs/QuoteSalesTotalPM");
var QuoteVATsTotalPM_1 = require("../../EntityPMs/QuoteVATsTotalPM");
var QuoteFollowUpPM_1 = require("../../EntityPMs/QuoteFollowUpPM");
var QuoteDocumentVersionPM_1 = require("../../EntityPMs/QuoteDocumentVersionPM");
var QuoteTotalVATPM_1 = require("../../EntityPMs/QuoteTotalVATPM");
var QuoteValidator_1 = require("../../Validators/QuoteValidator");
var QuotePMService = /** @class */ (function () {
    function QuotePMService() {
        this._http = ServiceHelper_1.ServiceHelper.Http;
        this._apiUrl = ServiceHelper_1.ServiceHelper.GetLogitudeURL() + 'api/quotes';
    }
    QuotePMService.prototype.get = function (id) {
        var _this = this;
        var authHeader = new http_1.Headers();
        authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/getsingle?' + 'id=' + id, {
                headers: authHeader
            }).map(function (response) {
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var serviceResponse;
                serviceResponse = new ServiceResponse_1.ServiceResponse();
                serviceResponse.Result = entity;
                var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Quote", "GetSinglePM", 'id=' + id);
                return serviceResponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
    };
    QuotePMService.prototype.insert = function (entityPM) {
        var _this = this;
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
            authHeader.append('Content-Type', 'application/json');
            var validator;
            validator = new ClassLevelValidator_1.ClassLevelValidator();
            var errorsArray = validator.Validate("Quote", entityPM);
            var customValidator = new QuoteValidator_1.QuoteValidator();
            var validationErrorsArr = customValidator.Validate(entityPM);
            if (validationErrorsArr) {
                errorsArray = errorsArray.concat(validationErrorsArr);
            }
            var serviceResponse;
            serviceResponse = new ServiceResponse_1.ServiceResponse();
            if (errorsArray.length == 0) {
                var mappedEntity;
                mappedEntity = _this.MapJsonToEntityPM(entityPM, false);
                return _this._http.post(_this._apiUrl, JSON.stringify(mappedEntity), { headers: authHeader }).map(function (response) {
                    var pm = response.json();
                    if (pm) {
                        var mappedResult;
                        mappedResult = _this.MapJsonToEntityPM(pm, true, entityPM);
                        serviceResponse.Result = mappedResult;
                    }
                    var servertime = response.headers.get('ServerExecutionTime');
                    PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Quote", "SaveChanges", "");
                    return serviceResponse;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                serviceResponse.HasError = true;
                serviceResponse.ErrorsArray = errorsArray;
                return Rx_1.Observable.of(serviceResponse);
            }
        });
    };
    QuotePMService.prototype.update = function (entityPM) {
        var _this = this;
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
            authHeader.append('Content-Type', 'application/json');
            var validator;
            validator = new ClassLevelValidator_1.ClassLevelValidator();
            var errorsArray = validator.Validate("Quote", entityPM);
            var customValidator = new QuoteValidator_1.QuoteValidator();
            var validationErrorsArr = customValidator.Validate(entityPM);
            if (validationErrorsArr) {
                errorsArray = errorsArray.concat(validationErrorsArr);
            }
            var serviceResponse;
            serviceResponse = new ServiceResponse_1.ServiceResponse();
            if (errorsArray.length == 0) {
                var mappedEntity;
                mappedEntity = _this.MapJsonToEntityPM(entityPM, false);
                return _this._http.put(_this._apiUrl, JSON.stringify(mappedEntity), { headers: authHeader }).map(function (response) {
                    var pm = response.json();
                    if (pm) {
                        var mappedResult;
                        mappedResult = _this.MapJsonToEntityPM(pm, true, entityPM);
                        serviceResponse.Result = mappedResult;
                    }
                    var servertime = response.headers.get('ServerExecutionTime');
                    PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Quote", "SaveChanges", "");
                    return serviceResponse;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                serviceResponse.HasError = true;
                serviceResponse.ErrorsArray = errorsArray;
                return Rx_1.Observable.of(serviceResponse);
            }
        });
    };
    QuotePMService.prototype.MapJsonToEntityPM = function (jsonPM, mapParent, entityPM) {
        if (mapParent === void 0) { mapParent = true; }
        if (entityPM === void 0) { entityPM = null; }
        if (!entityPM) {
            entityPM = new QuotePM_1.QuotePM();
        }
        var customFields = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }
            var property = jsonPMKeys[key];
            if (customFields.indexOf(property) > -1) {
                if (jsonPM[property]) {
                    var customFieldClass = new CustomFieldClass_1.CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
        }
        this.MapQuoteCostCharges(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapQuoteSaleCharges(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapQuoteCharges(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapQuotePackages(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapQuoteSalesTotals(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapTotalVATPerQuote(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapFollowUps(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapQuoteDocumentVersions(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapTotalVATs(entityPM, jsonPM, mapParent); // Call composition tables map methods
        if (mapParent) {
            entityPM.OldEntityPM = this.clone(entityPM);
            entityPM.OldEntityPM.QuoteCostCharges = [];
            for (var item in entityPM.QuoteCostCharges) {
                var myQuoteCostChargePM = entityPM.QuoteCostCharges[item];
                var newQuoteCostChargePM = this.clone(myQuoteCostChargePM);
                entityPM.OldEntityPM.QuoteCostCharges.push(newQuoteCostChargePM);
            }
            entityPM.OldEntityPM.QuoteSaleCharges = [];
            for (var item in entityPM.QuoteSaleCharges) {
                var myQuoteSaleChargePM = entityPM.QuoteSaleCharges[item];
                var newQuoteSaleChargePM = this.clone(myQuoteSaleChargePM);
                entityPM.OldEntityPM.QuoteSaleCharges.push(newQuoteSaleChargePM);
            }
            entityPM.OldEntityPM.QuoteCharges = [];
            for (var item in entityPM.QuoteCharges) {
                var myQuoteChargePM = entityPM.QuoteCharges[item];
                var newQuoteChargePM = this.clone(myQuoteChargePM);
                newQuoteChargePM.QuoteChargePriceSteps = [];
                for (var k in myQuoteChargePM.QuoteChargePriceSteps) {
                    var myQuotePriceStepsPM = myQuoteChargePM.QuoteChargePriceSteps[k];
                    var newQuotePriceStepsPM = this.clone(myQuoteChargePM.QuoteChargePriceSteps[k]);
                    newQuoteChargePM.QuoteChargePriceSteps.push(newQuotePriceStepsPM);
                }
                entityPM.OldEntityPM.QuoteCharges.push(newQuoteChargePM);
            }
            entityPM.OldEntityPM.QuotePackages = [];
            for (var item in entityPM.QuotePackages) {
                var myQuotePackagePM = entityPM.QuotePackages[item];
                var newQuotePackagePM = this.clone(myQuotePackagePM);
                entityPM.OldEntityPM.QuotePackages.push(newQuotePackagePM);
            }
            entityPM.OldEntityPM.QuoteSalesTotals = [];
            for (var item in entityPM.QuoteSalesTotals) {
                var myQuoteSalesTotalPM = entityPM.QuoteSalesTotals[item];
                var newQuoteSalesTotalPM = this.clone(myQuoteSalesTotalPM);
                entityPM.OldEntityPM.QuoteSalesTotals.push(newQuoteSalesTotalPM);
            }
            entityPM.OldEntityPM.TotalVATPerQuote = [];
            for (var item in entityPM.TotalVATPerQuote) {
                var myQuoteVATsTotalPM = entityPM.TotalVATPerQuote[item];
                var newQuoteVATsTotalPM = this.clone(myQuoteVATsTotalPM);
                entityPM.OldEntityPM.TotalVATPerQuote.push(newQuoteVATsTotalPM);
            }
            entityPM.OldEntityPM.FollowUps = [];
            for (var item in entityPM.FollowUps) {
                var myQuoteFollowUpPM = entityPM.FollowUps[item];
                var newQuoteFollowUpPM = this.clone(myQuoteFollowUpPM);
                entityPM.OldEntityPM.FollowUps.push(newQuoteFollowUpPM);
            }
            entityPM.OldEntityPM.QuoteDocumentVersions = [];
            for (var item in entityPM.QuoteDocumentVersions) {
                var myQuoteDocumentVersionPM = entityPM.QuoteDocumentVersions[item];
                var newQuoteDocumentVersionPM = this.clone(myQuoteDocumentVersionPM);
                entityPM.OldEntityPM.QuoteDocumentVersions.push(newQuoteDocumentVersionPM);
            }
            entityPM.OldEntityPM.TotalVATs = [];
            for (var item in entityPM.TotalVATs) {
                var myQuoteTotalVATPM = entityPM.TotalVATs[item];
                var newQuoteTotalVATPM = this.clone(myQuoteTotalVATPM);
                entityPM.OldEntityPM.TotalVATs.push(newQuoteTotalVATPM);
            }
        }
        else {
            entityPM.OldEntityPM = null;
        }
        entityPM.IsDirty = false;
        return entityPM;
    };
    QuotePMService.prototype.MapQuoteCostCharges = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        entityPM.QuoteCostCharges = new Array();
        for (var item in jsonPM.QuoteCostCharges) {
            var jItem = jsonPM.QuoteCostCharges[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuoteCostChargePM;
            newQuoteCostChargePM = new QuoteCostChargePM_1.QuoteCostChargePM();
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuoteCostChargePM[pmProperty] = jItem[pmProperty];
            }
            newQuoteCostChargePM.IsDirty = false;
            entityPM.QuoteCostCharges.push(newQuoteCostChargePM);
        }
    };
    QuotePMService.prototype.MapQuoteSaleCharges = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        entityPM.QuoteSaleCharges = new Array();
        for (var item in jsonPM.QuoteSaleCharges) {
            var jItem = jsonPM.QuoteSaleCharges[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuoteSaleChargePM;
            newQuoteSaleChargePM = new QuoteSaleChargePM_1.QuoteSaleChargePM();
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuoteSaleChargePM[pmProperty] = jItem[pmProperty];
            }
            newQuoteSaleChargePM.IsDirty = false;
            entityPM.QuoteSaleCharges.push(newQuoteSaleChargePM);
        }
    };
    QuotePMService.prototype.MapQuoteCharges = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldQuoteCharges = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldQuoteCharges = entityPM.OldEntityPM.QuoteCharges;
        }
        entityPM.QuoteCharges = new Array();
        for (var item in jsonPM.QuoteCharges) {
            var jItem = jsonPM.QuoteCharges[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuoteChargePM;
            if (mapParent) {
                newQuoteChargePM = new QuoteChargePM_1.QuoteChargePM(entityPM);
            }
            else {
                newQuoteChargePM = new QuoteChargePM_1.QuoteChargePM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuoteChargePM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newQuoteChargePM.UniqueKey = Guid_1.Guid.newGuid();
                newQuoteChargePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newQuoteChargePM.OldEntityPM = this.clone(newQuoteChargePM);
                this.MapQuoteChargePriceSteps(newQuoteChargePM, jItem, mapParent);
                newQuoteChargePM.OldEntityPM.QuoteChargePriceSteps = [];
                for (var k in newQuoteChargePM.QuoteChargePriceSteps) {
                    //var clonedInside = this.clone(newQuoteChargePM.QuoteChargePriceSteps[k]);
                    newQuoteChargePM.OldEntityPM.QuoteChargePriceSteps.push(newQuoteChargePM.QuoteChargePriceSteps[k].OldEntityPM); // clone old QuoteChargePriceSteps//
                }
            }
            else {
                if (newQuoteChargePM.UniqueKey) {
                    if (jItem.IsDirty)
                        newQuoteChargePM.ChangeSetOp = "Update";
                }
                else {
                    newQuoteChargePM.ChangeSetOp = "Insert";
                }
                this.MapQuoteChargePriceSteps(newQuoteChargePM, jItem, mapParent);
                newQuoteChargePM.OldEntityPM = null;
                newQuoteChargePM.EntityParentPM = null;
            }
            newQuoteChargePM.IsDirty = false;
            entityPM.QuoteCharges.push(newQuoteChargePM);
        }
        if (oldQuoteCharges) {
            for (var itemKey in oldQuoteCharges) {
                if (entityPM.QuoteCharges.filter(function (p) { return p.UniqueKey === oldQuoteCharges[itemKey].UniqueKey; }).length === 0) {
                    if (oldQuoteCharges[itemKey]) {
                        //oldQuoteCharges[itemKey].ChangeSetOp = "Delete";
                        //entityPM.QuoteCharges.push(oldQuoteCharges[itemKey]);
                        var oldItemJson = oldQuoteCharges[itemKey];
                        var deletedPM = new QuoteChargePM_1.QuoteChargePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        this.MapQuoteChargePriceSteps(deletedPM, oldItemJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.QuoteCharges.push(deletedPM);
                    }
                }
            }
        }
    };
    QuotePMService.prototype.MapQuoteChargePriceSteps = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldQuoteChargePriceSteps = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldQuoteChargePriceSteps = entityPM.OldEntityPM.QuoteChargePriceSteps;
        }
        entityPM.QuoteChargePriceSteps = new Array();
        for (var item in jsonPM.QuoteChargePriceSteps) {
            var jItem = jsonPM.QuoteChargePriceSteps[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuotePriceStepsPM;
            if (mapParent) {
                newQuotePriceStepsPM = new QuotePriceStepsPM_1.QuotePriceStepsPM(entityPM);
            }
            else {
                newQuotePriceStepsPM = new QuotePriceStepsPM_1.QuotePriceStepsPM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuotePriceStepsPM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newQuotePriceStepsPM.UniqueKey = Guid_1.Guid.newGuid();
                newQuotePriceStepsPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newQuotePriceStepsPM.OldEntityPM = this.clone(newQuotePriceStepsPM);
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    newQuotePriceStepsPM.ChangeSetOp = "Delete";
                }
                else {
                    if (newQuotePriceStepsPM.UniqueKey) {
                        if (jItem.IsDirty)
                            newQuotePriceStepsPM.ChangeSetOp = "Update";
                    }
                    else {
                        newQuotePriceStepsPM.ChangeSetOp = "Insert";
                    }
                }
                newQuotePriceStepsPM.OldEntityPM = null;
                newQuotePriceStepsPM.EntityParentPM = null;
            }
            newQuotePriceStepsPM.IsDirty = false;
            entityPM.QuoteChargePriceSteps.push(newQuotePriceStepsPM);
        }
        if (oldQuoteChargePriceSteps) {
            for (var itemKey in oldQuoteChargePriceSteps) {
                if (entityPM.QuoteChargePriceSteps.filter(function (p) { return p.UniqueKey === oldQuoteChargePriceSteps[itemKey].UniqueKey; }).length === 0) {
                    if (oldQuoteChargePriceSteps[itemKey]) {
                        //oldQuoteChargePriceSteps[itemKey].ChangeSetOp = "Delete";
                        //entityPM.QuoteChargePriceSteps.push(oldQuoteChargePriceSteps[itemKey]);
                        var oldItemJson = oldQuoteChargePriceSteps[itemKey];
                        var deletedPM = new QuotePriceStepsPM_1.QuotePriceStepsPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        deletedPM.OldEntityPM = null;
                        entityPM.QuoteChargePriceSteps.push(deletedPM);
                    }
                }
            }
        }
    };
    QuotePMService.prototype.MapQuotePackages = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldQuotePackages = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldQuotePackages = entityPM.OldEntityPM.QuotePackages;
        }
        entityPM.QuotePackages = new Array();
        for (var item in jsonPM.QuotePackages) {
            var jItem = jsonPM.QuotePackages[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuotePackagePM;
            if (mapParent) {
                newQuotePackagePM = new QuotePackagePM_1.QuotePackagePM(entityPM);
            }
            else {
                newQuotePackagePM = new QuotePackagePM_1.QuotePackagePM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuotePackagePM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newQuotePackagePM.UniqueKey = Guid_1.Guid.newGuid();
                newQuotePackagePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newQuotePackagePM.OldEntityPM = this.clone(newQuotePackagePM);
            }
            else {
                if (newQuotePackagePM.UniqueKey) {
                    if (jItem.IsDirty)
                        newQuotePackagePM.ChangeSetOp = "Update";
                }
                else {
                    newQuotePackagePM.ChangeSetOp = "Insert";
                }
                newQuotePackagePM.OldEntityPM = null;
                newQuotePackagePM.EntityParentPM = null;
            }
            newQuotePackagePM.IsDirty = false;
            entityPM.QuotePackages.push(newQuotePackagePM);
        }
        if (oldQuotePackages) {
            for (var itemKey in oldQuotePackages) {
                if (entityPM.QuotePackages.filter(function (p) { return p.UniqueKey === oldQuotePackages[itemKey].UniqueKey; }).length === 0) {
                    if (oldQuotePackages[itemKey]) {
                        //oldQuotePackages[itemKey].ChangeSetOp = "Delete";
                        //entityPM.QuotePackages.push(oldQuotePackages[itemKey]);
                        var oldItemJson = oldQuotePackages[itemKey];
                        var deletedPM = new QuotePackagePM_1.QuotePackagePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        deletedPM.OldEntityPM = null;
                        entityPM.QuotePackages.push(deletedPM);
                    }
                }
            }
        }
    };
    QuotePMService.prototype.MapQuoteSalesTotals = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        entityPM.QuoteSalesTotals = new Array();
        for (var item in jsonPM.QuoteSalesTotals) {
            var jItem = jsonPM.QuoteSalesTotals[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuoteSalesTotalPM;
            newQuoteSalesTotalPM = new QuoteSalesTotalPM_1.QuoteSalesTotalPM();
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuoteSalesTotalPM[pmProperty] = jItem[pmProperty];
            }
            newQuoteSalesTotalPM.IsDirty = false;
            entityPM.QuoteSalesTotals.push(newQuoteSalesTotalPM);
        }
    };
    QuotePMService.prototype.MapTotalVATPerQuote = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        entityPM.TotalVATPerQuote = new Array();
        for (var item in jsonPM.TotalVATPerQuote) {
            var jItem = jsonPM.TotalVATPerQuote[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuoteVATsTotalPM;
            newQuoteVATsTotalPM = new QuoteVATsTotalPM_1.QuoteVATsTotalPM();
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuoteVATsTotalPM[pmProperty] = jItem[pmProperty];
            }
            newQuoteVATsTotalPM.IsDirty = false;
            entityPM.TotalVATPerQuote.push(newQuoteVATsTotalPM);
        }
    };
    QuotePMService.prototype.MapFollowUps = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldFollowUps = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldFollowUps = entityPM.OldEntityPM.FollowUps;
        }
        entityPM.FollowUps = new Array();
        for (var item in jsonPM.FollowUps) {
            var jItem = jsonPM.FollowUps[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuoteFollowUpPM;
            if (mapParent) {
                newQuoteFollowUpPM = new QuoteFollowUpPM_1.QuoteFollowUpPM(entityPM);
            }
            else {
                newQuoteFollowUpPM = new QuoteFollowUpPM_1.QuoteFollowUpPM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuoteFollowUpPM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newQuoteFollowUpPM.UniqueKey = Guid_1.Guid.newGuid();
                newQuoteFollowUpPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newQuoteFollowUpPM.OldEntityPM = this.clone(newQuoteFollowUpPM);
                //file not found! child composition QuoteFollowUp
            }
            else {
                if (newQuoteFollowUpPM.UniqueKey) {
                    if (jItem.IsDirty)
                        newQuoteFollowUpPM.ChangeSetOp = "Update";
                }
                else {
                    newQuoteFollowUpPM.ChangeSetOp = "Insert";
                }
                //file not found! child composition QuoteFollowUp
                newQuoteFollowUpPM.OldEntityPM = null;
                newQuoteFollowUpPM.EntityParentPM = null;
            }
            newQuoteFollowUpPM.IsDirty = false;
            entityPM.FollowUps.push(newQuoteFollowUpPM);
        }
        if (oldFollowUps) {
            for (var itemKey in oldFollowUps) {
                if (entityPM.FollowUps.filter(function (p) { return p.UniqueKey === oldFollowUps[itemKey].UniqueKey; }).length === 0) {
                    if (oldFollowUps[itemKey]) {
                        //oldFollowUps[itemKey].ChangeSetOp = "Delete";
                        //entityPM.FollowUps.push(oldFollowUps[itemKey]);
                        var oldItemJson = oldFollowUps[itemKey];
                        var deletedPM = new QuoteFollowUpPM_1.QuoteFollowUpPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        //file not found! child composition QuoteFollowUp
                        deletedPM.OldEntityPM = null;
                        entityPM.FollowUps.push(deletedPM);
                    }
                }
            }
        }
    };
    //file not found! for child composition QuoteFollowUp
    QuotePMService.prototype.MapQuoteDocumentVersions = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldQuoteDocumentVersions = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldQuoteDocumentVersions = entityPM.OldEntityPM.QuoteDocumentVersions;
        }
        entityPM.QuoteDocumentVersions = new Array();
        for (var item in jsonPM.QuoteDocumentVersions) {
            var jItem = jsonPM.QuoteDocumentVersions[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuoteDocumentVersionPM;
            if (mapParent) {
                newQuoteDocumentVersionPM = new QuoteDocumentVersionPM_1.QuoteDocumentVersionPM(entityPM);
            }
            else {
                newQuoteDocumentVersionPM = new QuoteDocumentVersionPM_1.QuoteDocumentVersionPM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuoteDocumentVersionPM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newQuoteDocumentVersionPM.UniqueKey = Guid_1.Guid.newGuid();
                newQuoteDocumentVersionPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newQuoteDocumentVersionPM.OldEntityPM = this.clone(newQuoteDocumentVersionPM);
                //file not found! child composition QuoteDocumentVersion
            }
            else {
                if (newQuoteDocumentVersionPM.UniqueKey) {
                    if (jItem.IsDirty)
                        newQuoteDocumentVersionPM.ChangeSetOp = "Update";
                }
                else {
                    newQuoteDocumentVersionPM.ChangeSetOp = "Insert";
                }
                //file not found! child composition QuoteDocumentVersion
                newQuoteDocumentVersionPM.OldEntityPM = null;
                newQuoteDocumentVersionPM.EntityParentPM = null;
            }
            newQuoteDocumentVersionPM.IsDirty = false;
            entityPM.QuoteDocumentVersions.push(newQuoteDocumentVersionPM);
        }
        if (oldQuoteDocumentVersions) {
            for (var itemKey in oldQuoteDocumentVersions) {
                if (entityPM.QuoteDocumentVersions.filter(function (p) { return p.UniqueKey === oldQuoteDocumentVersions[itemKey].UniqueKey; }).length === 0) {
                    if (oldQuoteDocumentVersions[itemKey]) {
                        //oldQuoteDocumentVersions[itemKey].ChangeSetOp = "Delete";
                        //entityPM.QuoteDocumentVersions.push(oldQuoteDocumentVersions[itemKey]);
                        var oldItemJson = oldQuoteDocumentVersions[itemKey];
                        var deletedPM = new QuoteDocumentVersionPM_1.QuoteDocumentVersionPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        //file not found! child composition QuoteDocumentVersion
                        deletedPM.OldEntityPM = null;
                        entityPM.QuoteDocumentVersions.push(deletedPM);
                    }
                }
            }
        }
    };
    //file not found! for child composition QuoteDocumentVersion
    QuotePMService.prototype.MapTotalVATs = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldTotalVATs = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldTotalVATs = entityPM.OldEntityPM.TotalVATs;
        }
        entityPM.TotalVATs = new Array();
        for (var item in jsonPM.TotalVATs) {
            var jItem = jsonPM.TotalVATs[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newQuoteTotalVATPM;
            if (mapParent) {
                newQuoteTotalVATPM = new QuoteTotalVATPM_1.QuoteTotalVATPM(entityPM);
            }
            else {
                newQuoteTotalVATPM = new QuoteTotalVATPM_1.QuoteTotalVATPM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newQuoteTotalVATPM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newQuoteTotalVATPM.UniqueKey = Guid_1.Guid.newGuid();
                newQuoteTotalVATPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newQuoteTotalVATPM.OldEntityPM = this.clone(newQuoteTotalVATPM);
            }
            else {
                if (newQuoteTotalVATPM.UniqueKey) {
                    if (jItem.IsDirty)
                        newQuoteTotalVATPM.ChangeSetOp = "Update";
                }
                else {
                    newQuoteTotalVATPM.ChangeSetOp = "Insert";
                }
                newQuoteTotalVATPM.OldEntityPM = null;
                newQuoteTotalVATPM.EntityParentPM = null;
            }
            newQuoteTotalVATPM.IsDirty = false;
            entityPM.TotalVATs.push(newQuoteTotalVATPM);
        }
        if (oldTotalVATs) {
            for (var itemKey in oldTotalVATs) {
                if (entityPM.TotalVATs.filter(function (p) { return p.UniqueKey === oldTotalVATs[itemKey].UniqueKey; }).length === 0) {
                    if (oldTotalVATs[itemKey]) {
                        //oldTotalVATs[itemKey].ChangeSetOp = "Delete";
                        //entityPM.TotalVATs.push(oldTotalVATs[itemKey]);
                        var oldItemJson = oldTotalVATs[itemKey];
                        var deletedPM = new QuoteTotalVATPM_1.QuoteTotalVATPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        deletedPM.OldEntityPM = null;
                        entityPM.TotalVATs.push(deletedPM);
                    }
                }
            }
        }
    };
    QuotePMService.prototype.clone = function (jsonPM) {
        var entityPM;
        entityPM = {};
        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }
            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];
        }
        return entityPM;
    };
    QuotePMService.prototype.GetNewEntityPM = function () {
        var entityPM;
        entityPM = new QuotePM_1.QuotePM();
        entityPM.Tenant = InfraSettings_1.InfraSettings.TenantPM.Id;
        return entityPM;
    };
    QuotePMService = __decorate([
        core_1.Injectable(),
        __metadata("design:paramtypes", [])
    ], QuotePMService);
    return QuotePMService;
}());
exports.QuotePMService = QuotePMService;
//# sourceMappingURL=QuotePMService.js.map