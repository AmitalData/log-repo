"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
var core_1 = require("@angular/core");
var http_1 = require("@angular/http");
var Rx_1 = require("rxjs/Rx");
var ServiceResponse_1 = require("../../../Infrastructure/DataContracts/ServiceResponse");
var ClassLevelValidator_1 = require("../../../Infrastructure/Validators/ClassLevelValidator");
var Guid_1 = require("../../../Infrastructure/Utilities/Guid");
var InfraSettings_1 = require("../../../Infrastructure/Utilities/InfraSettings");
var ServiceHelper_1 = require("../../../Infrastructure/Utilities/ServiceHelper");
var SessionInfo_1 = require("../../../Infrastructure/Utilities/SessionInfo");
var CustomFieldClass_1 = require("../../../Infrastructure/DataContracts/CustomFieldClass");
var PerformanceLogger_1 = require("../../../Infrastructure/Utilities/PerformanceLogger");
var ClientPM_1 = require("../../EntityPMs/ClientPM");
var ClientAddressPM_1 = require("../../EntityPMs/ClientAddressPM");
var ClientsAddressCommTypePM_1 = require("../../EntityPMs/ClientsAddressCommTypePM");
var ClientDrivingLicensePM_1 = require("../../EntityPMs/ClientDrivingLicensePM");
var ClientDrivingLicenseTypePM_1 = require("../../EntityPMs/ClientDrivingLicenseTypePM");
var ClientPMService = /** @class */ (function () {
    function ClientPMService() {
        this._http = ServiceHelper_1.ServiceHelper.Http;
        this._apiUrl = ServiceHelper_1.ServiceHelper.GetLogitudeURL() + 'api/clients';
    }
    ClientPMService.prototype.get = function (id) {
        var _this = this;
        var authHeader = new http_1.Headers();
        authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/getsingle?' + 'id=' + id, {
                headers: authHeader
            }).map(function (response) {
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var serviceResponse;
                serviceResponse = new ServiceResponse_1.ServiceResponse();
                serviceResponse.Result = entity;
                var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Client", "GetSinglePM", 'id=' + id);
                return serviceResponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
    };
    ClientPMService.prototype.insert = function (entityPM) {
        var _this = this;
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
            authHeader.append('Content-Type', 'application/json');
            var validator;
            validator = new ClassLevelValidator_1.ClassLevelValidator();
            var errorsArray = validator.Validate("Customs.Client", entityPM);
            var serviceResponse;
            serviceResponse = new ServiceResponse_1.ServiceResponse();
            if (errorsArray.length == 0) {
                var mappedEntity;
                mappedEntity = _this.MapJsonToEntityPM(entityPM, false);
                return _this._http.post(_this._apiUrl, JSON.stringify(mappedEntity), { headers: authHeader }).map(function (response) {
                    var pm = response.json();
                    if (pm) {
                        var mappedResult;
                        mappedResult = _this.MapJsonToEntityPM(pm, true, entityPM);
                        serviceResponse.Result = mappedResult;
                    }
                    var servertime = response.headers.get('ServerExecutionTime');
                    PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Client", "SaveChanges", "");
                    return serviceResponse;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                serviceResponse.HasError = true;
                serviceResponse.ErrorsArray = errorsArray;
                return Rx_1.Observable.of(serviceResponse);
            }
        });
    };
    ClientPMService.prototype.update = function (entityPM) {
        var _this = this;
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
            authHeader.append('Content-Type', 'application/json');
            var validator;
            validator = new ClassLevelValidator_1.ClassLevelValidator();
            var errorsArray = validator.Validate("Customs.Client", entityPM);
            var serviceResponse;
            serviceResponse = new ServiceResponse_1.ServiceResponse();
            if (errorsArray.length == 0) {
                var mappedEntity;
                mappedEntity = _this.MapJsonToEntityPM(entityPM, false);
                return _this._http.put(_this._apiUrl, JSON.stringify(mappedEntity), { headers: authHeader }).map(function (response) {
                    var pm = response.json();
                    if (pm) {
                        var mappedResult;
                        mappedResult = _this.MapJsonToEntityPM(pm, true, entityPM);
                        serviceResponse.Result = mappedResult;
                    }
                    var servertime = response.headers.get('ServerExecutionTime');
                    PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Client", "SaveChanges", "");
                    return serviceResponse;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                serviceResponse.HasError = true;
                serviceResponse.ErrorsArray = errorsArray;
                return Rx_1.Observable.of(serviceResponse);
            }
        });
    };
    ClientPMService.prototype.MapJsonToEntityPM = function (jsonPM, mapParent, entityPM) {
        if (mapParent === void 0) { mapParent = true; }
        if (entityPM === void 0) { entityPM = null; }
        if (!entityPM) {
            entityPM = new ClientPM_1.ClientPM();
        }
        var customFields = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }
            var property = jsonPMKeys[key];
            if (customFields.indexOf(property) > -1) {
                if (jsonPM[property]) {
                    var customFieldClass = new CustomFieldClass_1.CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
        }
        this.MapClientAddresses(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapClientDrivingLicenses(entityPM, jsonPM, mapParent); // Call composition tables map methods
        if (mapParent) {
            entityPM.OldEntityPM = this.clone(entityPM);
            entityPM.OldEntityPM.ClientAddresses = [];
            for (var item in entityPM.ClientAddresses) {
                var myClientAddressPM = entityPM.ClientAddresses[item];
                var newClientAddressPM = this.clone(myClientAddressPM);
                newClientAddressPM.ClientsAddressCommTypes = [];
                for (var k in myClientAddressPM.ClientsAddressCommTypes) {
                    var myClientsAddressCommTypePM = myClientAddressPM.ClientsAddressCommTypes[k];
                    var newClientsAddressCommTypePM = this.clone(myClientAddressPM.ClientsAddressCommTypes[k]);
                    newClientAddressPM.ClientsAddressCommTypes.push(newClientsAddressCommTypePM);
                }
                entityPM.OldEntityPM.ClientAddresses.push(newClientAddressPM);
            }
            entityPM.OldEntityPM.ClientDrivingLicenses = [];
            for (var item in entityPM.ClientDrivingLicenses) {
                var myClientDrivingLicensePM = entityPM.ClientDrivingLicenses[item];
                var newClientDrivingLicensePM = this.clone(myClientDrivingLicensePM);
                newClientDrivingLicensePM.ClientDrivingLicenseTypes = [];
                for (var k in myClientDrivingLicensePM.ClientDrivingLicenseTypes) {
                    var myClientDrivingLicenseTypePM = myClientDrivingLicensePM.ClientDrivingLicenseTypes[k];
                    var newClientDrivingLicenseTypePM = this.clone(myClientDrivingLicensePM.ClientDrivingLicenseTypes[k]);
                    newClientDrivingLicensePM.ClientDrivingLicenseTypes.push(newClientDrivingLicenseTypePM);
                }
                entityPM.OldEntityPM.ClientDrivingLicenses.push(newClientDrivingLicensePM);
            }
        }
        else {
            entityPM.OldEntityPM = null;
        }
        entityPM.IsDirty = false;
        return entityPM;
    };
    ClientPMService.prototype.MapClientAddresses = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldClientAddresses = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldClientAddresses = entityPM.OldEntityPM.ClientAddresses;
        }
        entityPM.ClientAddresses = new Array();
        for (var item in jsonPM.ClientAddresses) {
            var jItem = jsonPM.ClientAddresses[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newClientAddressPM;
            if (mapParent) {
                newClientAddressPM = new ClientAddressPM_1.ClientAddressPM(entityPM);
            }
            else {
                newClientAddressPM = new ClientAddressPM_1.ClientAddressPM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newClientAddressPM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newClientAddressPM.UniqueKey = Guid_1.Guid.newGuid();
                newClientAddressPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newClientAddressPM.OldEntityPM = this.clone(newClientAddressPM);
                this.MapClientsAddressCommTypes(newClientAddressPM, jItem, mapParent);
                newClientAddressPM.OldEntityPM.ClientsAddressCommTypes = [];
                for (var k in newClientAddressPM.ClientsAddressCommTypes) {
                    //var clonedInside = this.clone(newClientAddressPM.ClientsAddressCommTypes[k]);
                    newClientAddressPM.OldEntityPM.ClientsAddressCommTypes.push(newClientAddressPM.ClientsAddressCommTypes[k].OldEntityPM); // clone old ClientsAddressCommTypes//
                }
            }
            else {
                if (newClientAddressPM.UniqueKey) {
                    if (jItem.IsDirty)
                        newClientAddressPM.ChangeSetOp = "Update";
                }
                else {
                    newClientAddressPM.ChangeSetOp = "Insert";
                }
                this.MapClientsAddressCommTypes(newClientAddressPM, jItem, mapParent);
                newClientAddressPM.OldEntityPM = null;
                newClientAddressPM.EntityParentPM = null;
            }
            newClientAddressPM.IsDirty = false;
            entityPM.ClientAddresses.push(newClientAddressPM);
        }
        if (oldClientAddresses) {
            for (var itemKey in oldClientAddresses) {
                if (entityPM.ClientAddresses.filter(function (p) { return p.UniqueKey === oldClientAddresses[itemKey].UniqueKey; }).length === 0) {
                    if (oldClientAddresses[itemKey]) {
                        //oldClientAddresses[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ClientAddresses.push(oldClientAddresses[itemKey]);
                        var oldItemJson = oldClientAddresses[itemKey];
                        var deletedPM = new ClientAddressPM_1.ClientAddressPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        this.MapClientsAddressCommTypes(deletedPM, oldItemJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.ClientAddresses.push(deletedPM);
                    }
                }
            }
        }
    };
    ClientPMService.prototype.MapClientsAddressCommTypes = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldClientsAddressCommTypes = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldClientsAddressCommTypes = entityPM.OldEntityPM.ClientsAddressCommTypes;
        }
        entityPM.ClientsAddressCommTypes = new Array();
        for (var item in jsonPM.ClientsAddressCommTypes) {
            var jItem = jsonPM.ClientsAddressCommTypes[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newClientsAddressCommTypePM;
            if (mapParent) {
                newClientsAddressCommTypePM = new ClientsAddressCommTypePM_1.ClientsAddressCommTypePM(entityPM);
            }
            else {
                newClientsAddressCommTypePM = new ClientsAddressCommTypePM_1.ClientsAddressCommTypePM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newClientsAddressCommTypePM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newClientsAddressCommTypePM.UniqueKey = Guid_1.Guid.newGuid();
                newClientsAddressCommTypePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newClientsAddressCommTypePM.OldEntityPM = this.clone(newClientsAddressCommTypePM);
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    newClientsAddressCommTypePM.ChangeSetOp = "Delete";
                }
                else {
                    if (newClientsAddressCommTypePM.UniqueKey) {
                        if (jItem.IsDirty)
                            newClientsAddressCommTypePM.ChangeSetOp = "Update";
                    }
                    else {
                        newClientsAddressCommTypePM.ChangeSetOp = "Insert";
                    }
                }
                newClientsAddressCommTypePM.OldEntityPM = null;
                newClientsAddressCommTypePM.EntityParentPM = null;
            }
            newClientsAddressCommTypePM.IsDirty = false;
            entityPM.ClientsAddressCommTypes.push(newClientsAddressCommTypePM);
        }
        if (oldClientsAddressCommTypes) {
            for (var itemKey in oldClientsAddressCommTypes) {
                if (entityPM.ClientsAddressCommTypes.filter(function (p) { return p.UniqueKey === oldClientsAddressCommTypes[itemKey].UniqueKey; }).length === 0) {
                    if (oldClientsAddressCommTypes[itemKey]) {
                        //oldClientsAddressCommTypes[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ClientsAddressCommTypes.push(oldClientsAddressCommTypes[itemKey]);
                        var oldItemJson = oldClientsAddressCommTypes[itemKey];
                        var deletedPM = new ClientsAddressCommTypePM_1.ClientsAddressCommTypePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        deletedPM.OldEntityPM = null;
                        entityPM.ClientsAddressCommTypes.push(deletedPM);
                    }
                }
            }
        }
    };
    ClientPMService.prototype.MapClientDrivingLicenses = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldClientDrivingLicenses = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldClientDrivingLicenses = entityPM.OldEntityPM.ClientDrivingLicenses;
        }
        entityPM.ClientDrivingLicenses = new Array();
        for (var item in jsonPM.ClientDrivingLicenses) {
            var jItem = jsonPM.ClientDrivingLicenses[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newClientDrivingLicensePM;
            if (mapParent) {
                newClientDrivingLicensePM = new ClientDrivingLicensePM_1.ClientDrivingLicensePM(entityPM);
            }
            else {
                newClientDrivingLicensePM = new ClientDrivingLicensePM_1.ClientDrivingLicensePM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newClientDrivingLicensePM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newClientDrivingLicensePM.UniqueKey = Guid_1.Guid.newGuid();
                newClientDrivingLicensePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newClientDrivingLicensePM.OldEntityPM = this.clone(newClientDrivingLicensePM);
                this.MapClientDrivingLicenseTypes(newClientDrivingLicensePM, jItem, mapParent);
                newClientDrivingLicensePM.OldEntityPM.ClientDrivingLicenseTypes = [];
                for (var k in newClientDrivingLicensePM.ClientDrivingLicenseTypes) {
                    //var clonedInside = this.clone(newClientDrivingLicensePM.ClientDrivingLicenseTypes[k]);
                    newClientDrivingLicensePM.OldEntityPM.ClientDrivingLicenseTypes.push(newClientDrivingLicensePM.ClientDrivingLicenseTypes[k].OldEntityPM); // clone old ClientDrivingLicenseTypes//
                }
            }
            else {
                if (newClientDrivingLicensePM.UniqueKey) {
                    if (jItem.IsDirty)
                        newClientDrivingLicensePM.ChangeSetOp = "Update";
                }
                else {
                    newClientDrivingLicensePM.ChangeSetOp = "Insert";
                }
                this.MapClientDrivingLicenseTypes(newClientDrivingLicensePM, jItem, mapParent);
                newClientDrivingLicensePM.OldEntityPM = null;
                newClientDrivingLicensePM.EntityParentPM = null;
            }
            newClientDrivingLicensePM.IsDirty = false;
            entityPM.ClientDrivingLicenses.push(newClientDrivingLicensePM);
        }
        if (oldClientDrivingLicenses) {
            for (var itemKey in oldClientDrivingLicenses) {
                if (entityPM.ClientDrivingLicenses.filter(function (p) { return p.UniqueKey === oldClientDrivingLicenses[itemKey].UniqueKey; }).length === 0) {
                    if (oldClientDrivingLicenses[itemKey]) {
                        //oldClientDrivingLicenses[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ClientDrivingLicenses.push(oldClientDrivingLicenses[itemKey]);
                        var oldItemJson = oldClientDrivingLicenses[itemKey];
                        var deletedPM = new ClientDrivingLicensePM_1.ClientDrivingLicensePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        this.MapClientDrivingLicenseTypes(deletedPM, oldItemJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.ClientDrivingLicenses.push(deletedPM);
                    }
                }
            }
        }
    };
    ClientPMService.prototype.MapClientDrivingLicenseTypes = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldClientDrivingLicenseTypes = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldClientDrivingLicenseTypes = entityPM.OldEntityPM.ClientDrivingLicenseTypes;
        }
        entityPM.ClientDrivingLicenseTypes = new Array();
        for (var item in jsonPM.ClientDrivingLicenseTypes) {
            var jItem = jsonPM.ClientDrivingLicenseTypes[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newClientDrivingLicenseTypePM;
            if (mapParent) {
                newClientDrivingLicenseTypePM = new ClientDrivingLicenseTypePM_1.ClientDrivingLicenseTypePM(entityPM);
            }
            else {
                newClientDrivingLicenseTypePM = new ClientDrivingLicenseTypePM_1.ClientDrivingLicenseTypePM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newClientDrivingLicenseTypePM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newClientDrivingLicenseTypePM.UniqueKey = Guid_1.Guid.newGuid();
                newClientDrivingLicenseTypePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newClientDrivingLicenseTypePM.OldEntityPM = this.clone(newClientDrivingLicenseTypePM);
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    newClientDrivingLicenseTypePM.ChangeSetOp = "Delete";
                }
                else {
                    if (newClientDrivingLicenseTypePM.UniqueKey) {
                        if (jItem.IsDirty)
                            newClientDrivingLicenseTypePM.ChangeSetOp = "Update";
                    }
                    else {
                        newClientDrivingLicenseTypePM.ChangeSetOp = "Insert";
                    }
                }
                newClientDrivingLicenseTypePM.OldEntityPM = null;
                newClientDrivingLicenseTypePM.EntityParentPM = null;
            }
            newClientDrivingLicenseTypePM.IsDirty = false;
            entityPM.ClientDrivingLicenseTypes.push(newClientDrivingLicenseTypePM);
        }
        if (oldClientDrivingLicenseTypes) {
            for (var itemKey in oldClientDrivingLicenseTypes) {
                if (entityPM.ClientDrivingLicenseTypes.filter(function (p) { return p.UniqueKey === oldClientDrivingLicenseTypes[itemKey].UniqueKey; }).length === 0) {
                    if (oldClientDrivingLicenseTypes[itemKey]) {
                        //oldClientDrivingLicenseTypes[itemKey].ChangeSetOp = "Delete";
                        //entityPM.ClientDrivingLicenseTypes.push(oldClientDrivingLicenseTypes[itemKey]);
                        var oldItemJson = oldClientDrivingLicenseTypes[itemKey];
                        var deletedPM = new ClientDrivingLicenseTypePM_1.ClientDrivingLicenseTypePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        deletedPM.OldEntityPM = null;
                        entityPM.ClientDrivingLicenseTypes.push(deletedPM);
                    }
                }
            }
        }
    };
    ClientPMService.prototype.clone = function (jsonPM) {
        var entityPM;
        entityPM = {};
        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }
            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];
        }
        return entityPM;
    };
    ClientPMService.prototype.GetNewEntityPM = function () {
        var entityPM;
        entityPM = new ClientPM_1.ClientPM();
        entityPM.Tenant = InfraSettings_1.InfraSettings.TenantPM.Id;
        return entityPM;
    };
    ClientPMService = __decorate([
        core_1.Injectable(),
        __metadata("design:paramtypes", [])
    ], ClientPMService);
    return ClientPMService;
}());
exports.ClientPMService = ClientPMService;
//# sourceMappingURL=ClientPMService.js.map