"use strict";
//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
var core_1 = require("@angular/core");
var http_1 = require("@angular/http");
require("rxjs/add/operator/map");
require("rxjs/add/operator/catch");
var Rx_1 = require("rxjs/Rx");
var ShipmentPM_1 = require("../../EntityPMs/ShipmentPM");
var AWBOCIPM_1 = require("../../EntityPMs/AWBOCIPM");
var ShipmentPackagePM_1 = require("../../EntityPMs/ShipmentPackagePM");
var InsideShipmentPackagePM_1 = require("../../EntityPMs/InsideShipmentPackagePM");
var ShipmentCommodityPM_1 = require("../../EntityPMs/ShipmentCommodityPM");
var ShipmentOrderPackagePM_1 = require("../../EntityPMs/ShipmentOrderPackagePM");
var ShipmentPayablePM_1 = require("../../EntityPMs/ShipmentPayablePM");
var ShipmentReceivablePM_1 = require("../../EntityPMs/ShipmentReceivablePM");
var ShipmentAWBPrintOnlyPM_1 = require("../../EntityPMs/ShipmentAWBPrintOnlyPM");
var ConsoleShipmentPM_1 = require("../../EntityPMs/ConsoleShipmentPM");
var ShipmentPickUpPM_1 = require("../../EntityPMs/ShipmentPickUpPM");
var ShipmentDeliveryPM_1 = require("../../EntityPMs/ShipmentDeliveryPM");
var ShipmentPickUpDeliveryPackagePM_1 = require("../../EntityPMs/ShipmentPickUpDeliveryPackagePM");
var ShipmentPackageItemPM_1 = require("../../EntityPMs/ShipmentPackageItemPM");
var ShipmentPackageHarmonizePM_1 = require("../../EntityPMs/ShipmentPackageHarmonizePM");
var ShipmentFollowUpPM_1 = require("../../EntityPMs/ShipmentFollowUpPM");
var ClassLevelValidator_1 = require("../../../Infrastructure/Validators/ClassLevelValidator");
var ShipmentValidator_1 = require("../../Validators/ShipmentValidator");
var ServiceResponse_1 = require("../../../Infrastructure/DataContracts/ServiceResponse");
var Guid_1 = require("../../../Infrastructure/Utilities/Guid");
var ServiceHelper_1 = require("../../../Infrastructure/Utilities/ServiceHelper");
var InfraSettings_1 = require("../../../Infrastructure/Utilities/InfraSettings");
var ShipmentPMInitService_1 = require("../../EntityPMInitServices/ShipmentPMInitService");
var CustomFieldClass_1 = require("../../../Infrastructure/DataContracts/CustomFieldClass");
var PerformanceLogger_1 = require("../../../Infrastructure/Utilities/PerformanceLogger");
var ShipmentAssemblyPM_1 = require("../../EntityPMs/ShipmentAssemblyPM");
var PickUpDeliveryPackageHarmonizePM_1 = require("../../EntityPMs/PickUpDeliveryPackageHarmonizePM");
var ShipmentPMService = /** @class */ (function () {
    function ShipmentPMService() {
        this._http = ServiceHelper_1.ServiceHelper.Http;
        this._apiUrl = ServiceHelper_1.ServiceHelper.GetLogitudeURL() + 'api/shipment';
    }
    ShipmentPMService.prototype.get = function (id) {
        var _this = this;
        var authHeader = new http_1.Headers();
        authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
        //authHeader.append('CallTimKey', ServiceHelper.GetLoggedUserToken());
        //var key = PerformanceLogger.AddLogTime();
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/GetSingle?id=' + id, {
                headers: authHeader
            }).map(function (response) {
                var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Shipment", "GetSinglePM", id);
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var pmresponse;
                pmresponse = new ServiceResponse_1.ServiceResponse();
                pmresponse.Result = entity;
                return pmresponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
        // .flatMap((res: Response) => {
        //    var location = res.headers.get('Location');
        //    return this._http.get(location);
        //}).map((res: Response) => res.json()))
        //.catch(this.handleError)
        /*
        .flatMap((res: Response) => {
                var serverTime = res.headers.get('ServerTime');
                return this._http.get(serverTime);
            })
        */
    };
    ShipmentPMService.prototype.getSingleByForwarderShipmentNumber = function (ForwarderShipmentNumber) {
        var _this = this;
        var authHeader = new http_1.Headers();
        authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
        //authHeader.append('CallTimKey', ServiceHelper.GetLoggedUserToken());
        //var key = PerformanceLogger.AddLogTime();
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/GetSingleByForwarderShipmentNumber?fsn=' + ForwarderShipmentNumber, {
                headers: authHeader
            }).map(function (response) {
                var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Shipment", "getSingleByForwarderShipmentNumber", ForwarderShipmentNumber);
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var pmresponse;
                pmresponse = new ServiceResponse_1.ServiceResponse();
                pmresponse.Result = entity;
                return pmresponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
        // .flatMap((res: Response) => {
        //    var location = res.headers.get('Location');
        //    return this._http.get(location);
        //}).map((res: Response) => res.json()))
        //.catch(this.handleError)
        /*
        .flatMap((res: Response) => {
                var serverTime = res.headers.get('ServerTime');
                return this._http.get(serverTime);
            })
        */
    };
    ShipmentPMService.prototype.getSingleBySecurityKeyTenantWithoutToken = function (SecurityKey, Tenant) {
        var _this = this;
        var authHeader = new http_1.Headers();
        //authHeader.append('Token', ServiceHelper.GetLoggedUserToken());
        //authHeader.append('CallTimKey', ServiceHelper.GetLoggedUserToken());
        //var key = PerformanceLogger.AddLogTime();
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/GetSingleBySecurityKeyWithoutToken?key=' + SecurityKey + '&tenant=' + Tenant, {
                headers: authHeader
            }).map(function (response) {
                //var servertime = response.headers.get('ServerExecutionTime');
                //PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Shipment", "getSingleBySecurityKey", SecurityKey);
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var pmresponse;
                pmresponse = new ServiceResponse_1.ServiceResponse();
                pmresponse.Result = entity;
                return pmresponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
        // .flatMap((res: Response) => {
        //    var location = res.headers.get('Location');
        //    return this._http.get(location);
        //}).map((res: Response) => res.json()))
        //.catch(this.handleError)
        /*
        .flatMap((res: Response) => {
                var serverTime = res.headers.get('ServerTime');
                return this._http.get(serverTime);
            })
        */
    };
    ShipmentPMService.prototype.getSingleByShipmentNumber = function (number) {
        var _this = this;
        var authHeader = new http_1.Headers();
        authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/getSingleByShipmentNumber?number=' + number, {
                headers: authHeader
            }).map(function (response) {
                var Id = response.json();
                var pmresponse;
                pmresponse = new ServiceResponse_1.ServiceResponse();
                pmresponse.Result = Id;
                return pmresponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
    };
    ShipmentPMService.prototype.GetSingleByCustomerReference1 = function (CustomerReference1, IsForwarderShipment) {
        var _this = this;
        if (IsForwarderShipment === void 0) { IsForwarderShipment = true; }
        var authHeader = new http_1.Headers();
        authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
        //authHeader.append('CallTimKey', ServiceHelper.GetLoggedUserToken());
        //var key = PerformanceLogger.AddLogTime();
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/GetSingleByCustomerReference1?CustomerReference1=' + CustomerReference1 + '&IsForwarderShipment=' + IsForwarderShipment, {
                headers: authHeader
            }).map(function (response) {
                var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Shipment", "GetSingleByCustomerReference1", CustomerReference1);
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var pmresponse;
                pmresponse = new ServiceResponse_1.ServiceResponse();
                pmresponse.Result = entity;
                return pmresponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
        // .flatMap((res: Response) => {
        //    var location = res.headers.get('Location');
        //    return this._http.get(location);
        //}).map((res: Response) => res.json()))
        //.catch(this.handleError)
        /*
        .flatMap((res: Response) => {
                var serverTime = res.headers.get('ServerTime');
                return this._http.get(serverTime);
            })
        */
    };
    ShipmentPMService.prototype.GetByCustomerReference1 = function (CustomerReference1, IsForwarderShipment) {
        var _this = this;
        if (IsForwarderShipment === void 0) { IsForwarderShipment = true; }
        var authHeader = new http_1.Headers();
        authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
        //authHeader.append('CallTimKey', ServiceHelper.GetLoggedUserToken());
        //var key = PerformanceLogger.AddLogTime();
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/GetByCustomerReference1?CustomerReference1=' + CustomerReference1 + '&IsForwarderShipment=' + IsForwarderShipment, {
                headers: authHeader
            }).map(function (response) {
                var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Shipment", "GetByCustomerReference1", CustomerReference1);
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var pmresponse;
                pmresponse = new ServiceResponse_1.ServiceResponse();
                pmresponse.Result = entity;
                return pmresponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
        // .flatMap((res: Response) => {
        //    var location = res.headers.get('Location');
        //    return this._http.get(location);
        //}).map((res: Response) => res.json()))
        //.catch(this.handleError)
        /*
        .flatMap((res: Response) => {
                var serverTime = res.headers.get('ServerTime');
                return this._http.get(serverTime);
            })
        */
    };
    ShipmentPMService.prototype.GetByCustomerReference1ForUpdate = function (CustomerReference1, ShipmentId, IsForwarderShipment) {
        var _this = this;
        if (IsForwarderShipment === void 0) { IsForwarderShipment = true; }
        var authHeader = new http_1.Headers();
        authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
        //authHeader.append('CallTimKey', ServiceHelper.GetLoggedUserToken());
        //var key = PerformanceLogger.AddLogTime();
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/GetByCustomerReference1ForUpdate?CustomerReference1=' + CustomerReference1 + '&ShipmentId=' + ShipmentId + '&IsForwarderShipment=' + IsForwarderShipment, {
                headers: authHeader
            }).map(function (response) {
                var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Shipment", "GetByCustomerReference1ForUpdate", CustomerReference1);
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var pmresponse;
                pmresponse = new ServiceResponse_1.ServiceResponse();
                pmresponse.Result = entity;
                return pmresponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
        // .flatMap((res: Response) => {
        //    var location = res.headers.get('Location');
        //    return this._http.get(location);
        //}).map((res: Response) => res.json()))
        //.catch(this.handleError)
        /*
        .flatMap((res: Response) => {
                var serverTime = res.headers.get('ServerTime');
                return this._http.get(serverTime);
            })
        */
    };
    ShipmentPMService.prototype.insert = function (entityPM) {
        var _this = this;
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
            authHeader.append('Content-Type', 'application/json');
            var validator = new ClassLevelValidator_1.ClassLevelValidator();
            var entityValidator = new ShipmentValidator_1.ShipmentValidator();
            var errors = validator.Validate("Shipment", entityPM);
            var entityErrors = entityValidator.Validate(entityPM);
            if (entityErrors) {
                errors = errors.concat(entityErrors);
            }
            var response;
            response = new ServiceResponse_1.ServiceResponse();
            if (errors.length == 0) {
                var shipment;
                shipment = _this.MapJsonToEntityPM(entityPM, false);
                var shipString;
                shipString = JSON.stringify(shipment);
                //console.log(shipString);
                return _this._http.post(_this._apiUrl, shipString, { headers: authHeader }).map(function (res) {
                    var pm = res.json();
                    var shipment;
                    shipment = _this.MapJsonToEntityPM(pm, true, entityPM);
                    response.Result = shipment;
                    return response;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                response.HasError = true;
                response.ErrorsArray = errors;
                return Rx_1.Observable.of(response);
            }
        });
    };
    ShipmentPMService.prototype.update = function (entityPM) {
        var _this = this;
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
            authHeader.append('Content-Type', 'application/json');
            var validator = new ClassLevelValidator_1.ClassLevelValidator();
            var entityValidator = new ShipmentValidator_1.ShipmentValidator();
            var errors = validator.Validate("Shipment", entityPM);
            var entityErrors = entityValidator.Validate(entityPM);
            if (entityErrors) {
                errors = errors.concat(entityErrors);
            }
            var response;
            response = new ServiceResponse_1.ServiceResponse();
            //errorsArray = [];
            if (errors.length == 0) {
                var shipment;
                shipment = _this.MapJsonToEntityPM(entityPM, false);
                var shipString;
                shipString = JSON.stringify(shipment);
                //console.log(shipString);
                return _this._http.put(_this._apiUrl, shipString, { headers: authHeader }).map(function (res) {
                    var pm = res.json();
                    var shipment;
                    shipment = _this.MapJsonToEntityPM(pm, true, entityPM);
                    response.Result = shipment;
                    return response;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                response.HasError = true;
                response.ErrorsArray = errors;
                return Rx_1.Observable.of(response);
            }
        });
    };
    ShipmentPMService.prototype.GetNewEntityPM = function () {
        var entityPM;
        entityPM = new ShipmentPM_1.ShipmentPM();
        entityPM.Tenant = InfraSettings_1.InfraSettings.TenantPM.Id;
        ShipmentPMInitService_1.ShipmentPMInitService.InitValues(entityPM, true);
        return entityPM;
    };
    ShipmentPMService.prototype.handleError = function (error) {
        var apiException = error.json();
        var response;
        response = new ServiceResponse_1.ServiceResponse();
        response.HasError = true;
        response.ErrorsArray.push(apiException.ShortErrorMessage);
        return Rx_1.Observable.of(response);
    };
    ShipmentPMService.prototype.clone = function (jsonPM) {
        var entityPM;
        entityPM = {};
        try {
            var jsonPMKeys = Object.keys(jsonPM);
            for (var key in jsonPMKeys) {
                if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM") {
                    continue;
                }
                var property = jsonPMKeys[key];
                entityPM[property] = jsonPM[property];
            }
        }
        catch (e) {
            var d = jsonPM;
        }
        return entityPM;
    };
    ShipmentPMService.prototype.MapJsonToEntityPM = function (jsonPM, mapParent, entityPM) {
        if (mapParent === void 0) { mapParent = true; }
        if (entityPM === void 0) { entityPM = null; }
        var customFields = [];
        for (var i = 1; i < 41; i++) {
            customFields.push("Field" + i);
        }
        if (!entityPM) {
            entityPM = new ShipmentPM_1.ShipmentPM();
        }
        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }
            var property = jsonPMKeys[key];
            if (customFields.indexOf(property) > -1) {
                if (jsonPM[property]) {
                    var customFieldClass = new CustomFieldClass_1.CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
        }
        this.MapShipmentOCIs(entityPM, jsonPM, mapParent);
        this.MapShipmentPackages(entityPM, jsonPM, mapParent);
        this.MapShipmentCommodities(entityPM, jsonPM, mapParent);
        this.MapShipmentOrderPackages(entityPM, jsonPM, mapParent);
        this.MapShipmentPayables(entityPM, jsonPM, mapParent);
        this.MapShipmentReceivables(entityPM, jsonPM, mapParent);
        this.MapShipmentAWBPrintOnlies(entityPM, jsonPM, mapParent);
        this.MapShipmentConsoleShipments(entityPM, jsonPM, mapParent);
        this.MapShipmentPickups(entityPM, jsonPM, mapParent);
        this.MapShipmentDeliveries(entityPM, jsonPM, mapParent);
        this.MapShipmentFollowups(entityPM, jsonPM, mapParent);
        this.MapShipmentAssemblies(entityPM, jsonPM, mapParent);
        entityPM.IsDirty = false;
        if (mapParent) {
            entityPM.OldEntityPM = this.clone(entityPM);
            entityPM.OldEntityPM.AWBOCIPMs = [];
            for (var item in entityPM.AWBOCIPMs) {
                entityPM.OldEntityPM.AWBOCIPMs.push(this.clone(entityPM.AWBOCIPMs[item]));
            }
            entityPM.OldEntityPM.ShipmentCommodities = [];
            for (var item in entityPM.ShipmentCommodities) {
                entityPM.OldEntityPM.ShipmentCommodities.push(this.clone(entityPM.ShipmentCommodities[item]));
            }
            entityPM.OldEntityPM.ShipmentOrderPackages = [];
            for (var item in entityPM.ShipmentOrderPackages) {
                entityPM.OldEntityPM.ShipmentOrderPackages.push(this.clone(entityPM.ShipmentOrderPackages[item]));
            }
            entityPM.OldEntityPM.ShipmentPayables = [];
            for (var item in entityPM.ShipmentPayables) {
                entityPM.OldEntityPM.ShipmentPayables.push(this.clone(entityPM.ShipmentPayables[item]));
            }
            entityPM.OldEntityPM.ShipmentReceivables = [];
            for (var item in entityPM.ShipmentReceivables) {
                entityPM.OldEntityPM.ShipmentReceivables.push(this.clone(entityPM.ShipmentReceivables[item]));
            }
            entityPM.OldEntityPM.ShipmentAWBPrintOnlies = [];
            for (var item in entityPM.ShipmentAWBPrintOnlies) {
                entityPM.OldEntityPM.ShipmentAWBPrintOnlies.push(this.clone(entityPM.ShipmentAWBPrintOnlies[item]));
            }
            entityPM.OldEntityPM.ShipmentConsoleShipments = [];
            for (var item in entityPM.ShipmentConsoleShipments) {
                entityPM.OldEntityPM.ShipmentConsoleShipments.push(this.clone(entityPM.ShipmentConsoleShipments[item]));
            }
            entityPM.OldEntityPM.ShipmentPackages = [];
            for (var item in entityPM.ShipmentPackages) {
                var myShipmentPackage = entityPM.ShipmentPackages[item];
                var newPackage = this.clone(myShipmentPackage);
                newPackage.InsideShipmentPackages = [];
                newPackage.ShipmentPackageItems = [];
                newPackage.ShipmentPackageHarmonizes = [];
                for (var k in myShipmentPackage.InsideShipmentPackages) {
                    newPackage.InsideShipmentPackages.push(this.clone(myShipmentPackage.InsideShipmentPackages[k]));
                }
                for (var k1 in myShipmentPackage.ShipmentPackageItems) {
                    newPackage.ShipmentPackageItems.push(this.clone(myShipmentPackage.ShipmentPackageItems[k1]));
                }
                for (var k3 in myShipmentPackage.ShipmentPackageHarmonizes) {
                    newPackage.ShipmentPackageHarmonizes.push(this.clone(myShipmentPackage.ShipmentPackageHarmonizes[k3]));
                }
                entityPM.OldEntityPM.ShipmentPackages.push(newPackage);
            }
            entityPM.OldEntityPM.ShipmentPickUps = [];
            for (var item in entityPM.ShipmentPickUps) {
                var myShipmentPickup = entityPM.ShipmentPickUps[item];
                var newPickup = this.clone(myShipmentPickup);
                newPickup.ShipmentPickUpDeliveryPackages = [];
                for (var r in myShipmentPickup.ShipmentPickUpDeliveryPackages) {
                    var myPickUpPackage = myShipmentPickup.ShipmentPickUpDeliveryPackages[r];
                    var newPickUpPackage = this.clone(myPickUpPackage);
                    newPickUpPackage.PickUpDeliveryPackageHarmonizes = [];
                    for (var kk3 in myPickUpPackage.PickUpDeliveryPackageHarmonizes) {
                        newPickUpPackage.PickUpDeliveryPackageHarmonizes.push(this.clone(myPickUpPackage.PickUpDeliveryPackageHarmonizes[kk3]));
                    }
                    newPickup.ShipmentPickUpDeliveryPackages.push(myPickUpPackage);
                }
                entityPM.OldEntityPM.ShipmentPickUps.push(newPickup);
            }
            entityPM.OldEntityPM.ShipmentDeliveries = [];
            for (var item in entityPM.ShipmentDeliveries) {
                var myShipmentDelivery = entityPM.ShipmentDeliveries[item];
                var newDelivery = this.clone(myShipmentDelivery);
                newDelivery.ShipmentPickUpDeliveryPackages = [];
                for (var k in myShipmentDelivery.ShipmentPickUpDeliveryPackages) {
                    var myDeliveryPackage = myShipmentDelivery.ShipmentPickUpDeliveryPackages[k];
                    var newDeliveryPackage = this.clone(myDeliveryPackage);
                    newDeliveryPackage.PickUpDeliveryPackageHarmonizes = [];
                    for (var kkk3 in myDeliveryPackage.PickUpDeliveryPackageHarmonizes) {
                        newDeliveryPackage.PickUpDeliveryPackageHarmonizes.push(this.clone(myDeliveryPackage.PickUpDeliveryPackageHarmonizes[kkk3]));
                    }
                    newDelivery.ShipmentPickUpDeliveryPackages.push(myDeliveryPackage);
                }
                entityPM.OldEntityPM.ShipmentDeliveries.push(newDelivery);
            }
            entityPM.OldEntityPM.FollowUps = [];
            for (var item in entityPM.FollowUps) {
                entityPM.OldEntityPM.FollowUps.push(this.clone(entityPM.FollowUps[item]));
            }
            entityPM.OldEntityPM.ShipmentAssemblies = [];
            for (var item in entityPM.ShipmentAssemblies) {
                entityPM.OldEntityPM.ShipmentAssemblies.push(this.clone(entityPM.ShipmentAssemblies[item]));
            }
        }
        else {
            entityPM.OldEntityPM = null;
        }
        return entityPM;
    };
    ShipmentPMService.prototype.MapShipmentOCIs = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.AWBOCIPMs;
        }
        entityPM.AWBOCIPMs = new Array();
        for (var item in jsonPM.AWBOCIPMs) {
            var itemJson = jsonPM.AWBOCIPMs[item];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) {
                itemPM = new AWBOCIPM_1.AWBOCIPM(entityPM);
            }
            else {
                itemPM = new AWBOCIPM_1.AWBOCIPM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            itemPM.IsDirty = false;
            if (mapParent) {
                itemPM.OldEntityPM = this.clone(itemPM);
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty) {
                        itemPM.ChangeSetOp = "Update";
                    }
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.OldEntityPM = null;
            }
            entityPM.AWBOCIPMs.push(itemPM);
        }
        if (oldCollection) {
            for (var item in oldCollection) {
                if (entityPM.AWBOCIPMs.filter(function (p) { return p.UniqueKey === oldCollection[item].UniqueKey; }).length === 0) {
                    if (oldCollection[item]) {
                        oldCollection[item].ChangeSetOp = "Delete";
                        entityPM.AWBOCIPMs.push(oldCollection[item]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentPackages = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentPackages;
        }
        entityPM.ShipmentPackages = new Array();
        for (var pack in jsonPM.ShipmentPackages) {
            var itemJson = jsonPM.ShipmentPackages[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentPackagePM_1.ShipmentPackagePM(entityPM);
            }
            else { // update mapping             
                itemPM = new ShipmentPackagePM_1.ShipmentPackagePM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
                itemPM.OldEntityPM = this.clone(itemPM);
                this.MapInsideShipmentPackages(itemPM, itemJson, mapParent);
                itemPM.OldEntityPM.InsideShipmentPackages = [];
                for (var k1 in itemPM.InsideShipmentPackages) {
                    var clonedInside = this.clone(itemPM.InsideShipmentPackages[k1]);
                    itemPM.OldEntityPM.InsideShipmentPackages.push(clonedInside); // clone old inside packages//
                }
                this.MapShipmentPackageItems(itemPM, itemJson, mapParent);
                itemPM.OldEntityPM.ShipmentPackageItems = [];
                for (var k2 in itemPM.ShipmentPackageItems) {
                    var clonedInside = this.clone(itemPM.ShipmentPackageItems[k2]);
                    itemPM.OldEntityPM.ShipmentPackageItems.push(clonedInside); // clone old inside packages//
                }
                this.MapShipmentPackageHarmonizes(itemPM, itemJson, mapParent);
                itemPM.OldEntityPM.ShipmentPackageHarmonizes = [];
                for (var k3 in itemPM.ShipmentPackageHarmonizes) {
                    var clonedInside = this.clone(itemPM.ShipmentPackageHarmonizes[k3]);
                    itemPM.OldEntityPM.ShipmentPackageHarmonizes.push(clonedInside);
                }
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty) {
                        itemPM.ChangeSetOp = "Update";
                    }
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                this.MapInsideShipmentPackages(itemPM, itemJson, mapParent);
                this.MapShipmentPackageItems(itemPM, itemJson, mapParent);
                this.MapShipmentPackageHarmonizes(itemPM, itemJson, mapParent);
                itemPM.EntityParentPM = null;
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentPackages.push(itemPM);
        }
        if (oldCollection) {
            for (var pack in oldCollection) {
                if (entityPM.ShipmentPackages.filter(function (p) { return p.UniqueKey === oldCollection[pack].UniqueKey; }).length === 0) {
                    if (oldCollection[pack]) {
                        var oldpackageJson = oldCollection[pack];
                        var deletedPM = new ShipmentPackagePM_1.ShipmentPackagePM(null);
                        var pmKeys = Object.keys(oldpackageJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldpackageJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        this.MapInsideShipmentPackages(deletedPM, oldpackageJson, mapParent);
                        this.MapShipmentPackageItems(deletedPM, oldpackageJson, mapParent);
                        this.MapShipmentPackageHarmonizes(deletedPM, oldpackageJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.ShipmentPackages.push(deletedPM);
                        //oldCollection[pack].ChangeSetOp = "Delete";
                        //oldCollection[pack].OldEntityPM = null;
                        //oldCollection[pack].UIProperties = null;
                        //oldCollection[pack].EntityParentPM = null;
                        ////if the parent is deleted then delete all of its children
                        //for (k in oldCollection[pack].InsideShipmentPackages) {
                        //    oldCollection[pack].InsideShipmentPackages[k].ChangeSetOp = "Delete";
                        //    oldCollection[pack].InsideShipmentPackages[k].OldEntityPM = null;
                        //    oldCollection[pack].InsideShipmentPackages[k].UIProperties = null;
                        //    oldCollection[pack].InsideShipmentPackages[k].EntityParentPM = null;
                        //}
                        //entityPM.ShipmentPackages.push(oldCollection[pack]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentCommodities = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentCommodities;
        }
        entityPM.ShipmentCommodities = new Array();
        for (var item in jsonPM.ShipmentCommodities) {
            var itemJson = jsonPM.ShipmentCommodities[item];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) {
                itemPM = new ShipmentCommodityPM_1.ShipmentCommodityPM(entityPM);
            }
            else {
                itemPM = new ShipmentCommodityPM_1.ShipmentCommodityPM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.OldEntityPM = this.clone(itemPM);
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty) {
                        itemPM.ChangeSetOp = "Update";
                    }
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentCommodities.push(itemPM);
        }
        if (oldCollection) {
            for (var item in oldCollection) {
                if (entityPM.ShipmentCommodities.filter(function (p) { return p.UniqueKey === oldCollection[item].UniqueKey; }).length === 0) {
                    if (oldCollection[item]) {
                        oldCollection[item].ChangeSetOp = "Delete";
                        entityPM.ShipmentCommodities.push(oldCollection[item]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentOrderPackages = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentOrderPackages;
        }
        entityPM.ShipmentOrderPackages = new Array();
        for (var item in jsonPM.ShipmentOrderPackages) {
            var itemJson = jsonPM.ShipmentOrderPackages[item];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) {
                itemPM = new ShipmentOrderPackagePM_1.ShipmentOrderPackagePM(entityPM);
            }
            else {
                itemPM = new ShipmentOrderPackagePM_1.ShipmentOrderPackagePM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.OldEntityPM = this.clone(itemPM);
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty) {
                        itemPM.ChangeSetOp = "Update";
                    }
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentOrderPackages.push(itemPM);
        }
        if (oldCollection) {
            for (var item in oldCollection) {
                if (entityPM.ShipmentOrderPackages.filter(function (p) { return p.UniqueKey === oldCollection[item].UniqueKey; }).length === 0) {
                    if (oldCollection[item]) {
                        oldCollection[item].ChangeSetOp = "Delete";
                        entityPM.ShipmentOrderPackages.push(oldCollection[item]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentPayables = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldPayables = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldPayables = entityPM.OldEntityPM.ShipmentPayables;
        }
        entityPM.ShipmentPayables = new Array();
        for (var pack in jsonPM.ShipmentPayables) {
            var itemJson = jsonPM.ShipmentPayables[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentPayablePM_1.ShipmentPayablePM(entityPM);
            }
            else { // update mapping
                itemPM = new ShipmentPayablePM_1.ShipmentPayablePM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.OldEntityPM = this.clone(itemPM);
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty)
                        itemPM.ChangeSetOp = "Update";
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentPayables.push(itemPM);
        }
        if (oldPayables) {
            for (var pack in oldPayables) {
                if (entityPM.ShipmentPayables.filter(function (p) { return p.UniqueKey === oldPayables[pack].UniqueKey; }).length === 0) {
                    if (oldPayables[pack]) {
                        oldPayables[pack].ChangeSetOp = "Delete";
                        entityPM.ShipmentPayables.push(oldPayables[pack]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentReceivables = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldReceivables = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldReceivables = entityPM.OldEntityPM.ShipmentReceivables;
        }
        entityPM.ShipmentReceivables = new Array();
        for (var pack in jsonPM.ShipmentReceivables) {
            var itemJson = jsonPM.ShipmentReceivables[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentReceivablePM_1.ShipmentReceivablePM(entityPM);
            }
            else { // update mapping
                itemPM = new ShipmentReceivablePM_1.ShipmentReceivablePM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.OldEntityPM = this.clone(itemPM);
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty)
                        itemPM.ChangeSetOp = "Update";
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentReceivables.push(itemPM);
        }
        if (oldReceivables) {
            for (var pack in oldReceivables) {
                if (entityPM.ShipmentReceivables.filter(function (p) { return p.UniqueKey === oldReceivables[pack].UniqueKey; }).length === 0) {
                    if (oldReceivables[pack]) {
                        oldReceivables[pack].ChangeSetOp = "Delete";
                        entityPM.ShipmentReceivables.push(oldReceivables[pack]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentAWBPrintOnlies = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentAWBPrintOnlies;
        }
        entityPM.ShipmentAWBPrintOnlies = new Array();
        for (var item in jsonPM.ShipmentAWBPrintOnlies) {
            var itemJson = jsonPM.ShipmentAWBPrintOnlies[item];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) {
                itemPM = new ShipmentAWBPrintOnlyPM_1.ShipmentAWBPrintOnlyPM(entityPM);
            }
            else {
                itemPM = new ShipmentAWBPrintOnlyPM_1.ShipmentAWBPrintOnlyPM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.OldEntityPM = this.clone(itemPM);
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty) {
                        itemPM.ChangeSetOp = "Update";
                    }
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentAWBPrintOnlies.push(itemPM);
        }
        if (oldCollection) {
            for (var item in oldCollection) {
                if (entityPM.ShipmentAWBPrintOnlies.filter(function (p) { return p.UniqueKey === oldCollection[item].UniqueKey; }).length === 0) {
                    if (oldCollection[item]) {
                        oldCollection[item].ChangeSetOp = "Delete";
                        entityPM.ShipmentAWBPrintOnlies.push(oldCollection[item]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentConsoleShipments = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentConsoleShipments;
        }
        entityPM.ShipmentConsoleShipments = new Array();
        for (var item in jsonPM.ShipmentConsoleShipments) {
            var itemJson = jsonPM.ShipmentConsoleShipments[item];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) {
                itemPM = new ConsoleShipmentPM_1.ConsoleShipmentPM(entityPM);
            }
            else {
                itemPM = new ConsoleShipmentPM_1.ConsoleShipmentPM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.OldEntityPM = this.clone(itemPM);
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty) {
                        itemPM.ChangeSetOp = "Update";
                    }
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentConsoleShipments.push(itemPM);
        }
        if (oldCollection) {
            for (var item in oldCollection) {
                if (entityPM.ShipmentConsoleShipments.filter(function (p) { return p.UniqueKey === oldCollection[item].UniqueKey; }).length === 0) {
                    if (oldCollection[item]) {
                        oldCollection[item].ChangeSetOp = "Delete";
                        entityPM.ShipmentConsoleShipments.push(oldCollection[item]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentPickups = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentPickUps;
        }
        entityPM.ShipmentPickUps = new Array();
        for (var pack in jsonPM.ShipmentPickUps) {
            var itemJson = jsonPM.ShipmentPickUps[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentPickUpPM_1.ShipmentPickUpPM(entityPM);
            }
            else { // update mapping             
                itemPM = new ShipmentPickUpPM_1.ShipmentPickUpPM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "PropertyChanged") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
                itemPM.OldEntityPM = this.clone(itemPM);
                this.MapPickupPackages(itemPM, itemJson, mapParent);
                itemPM.OldEntityPM.ShipmentPickUpDeliveryPackages = [];
                for (var k in itemPM.ShipmentPickUpDeliveryPackages) {
                    var clonedInside = this.clone(itemPM.ShipmentPickUpDeliveryPackages[k]);
                    itemPM.OldEntityPM.ShipmentPickUpDeliveryPackages.push(clonedInside); // clone old inside packages//
                }
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty) {
                        itemPM.ChangeSetOp = "Update";
                    }
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.ShipmentPickUpDeliveryPackages = [];
                this.MapPickupPackages(itemPM, itemJson, mapParent);
                itemPM.EntityParentPM = null;
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentPickUps.push(itemPM);
        }
        if (oldCollection) {
            for (var pack in oldCollection) {
                if (entityPM.ShipmentPickUps.filter(function (p) { return p.UniqueKey === oldCollection[pack].UniqueKey; }).length === 0) {
                    if (oldCollection[pack]) {
                        var oldPickUpJson = oldCollection[pack];
                        var deletedPM = new ShipmentPickUpPM_1.ShipmentPickUpPM(null);
                        var pmKeys = Object.keys(oldPickUpJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldPickUpJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        this.MapPickupPackages(deletedPM, oldPickUpJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.ShipmentPickUps.push(deletedPM);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentDeliveries = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentDeliveries;
        }
        entityPM.ShipmentDeliveries = new Array();
        for (var pack in jsonPM.ShipmentDeliveries) {
            var itemJson = jsonPM.ShipmentDeliveries[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentDeliveryPM_1.ShipmentDeliveryPM(entityPM);
            }
            else { // update mapping             
                itemPM = new ShipmentDeliveryPM_1.ShipmentDeliveryPM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "PropertyChanged") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
                itemPM.OldEntityPM = this.clone(itemPM);
                this.MapDeliveryPackages(itemPM, itemJson, mapParent);
                itemPM.OldEntityPM.ShipmentPickUpDeliveryPackages = [];
                for (var k in itemPM.ShipmentPickUpDeliveryPackages) {
                    var clonedInside = this.clone(itemPM.ShipmentPickUpDeliveryPackages[k]);
                    itemPM.OldEntityPM.ShipmentPickUpDeliveryPackages.push(clonedInside);
                }
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty) {
                        itemPM.ChangeSetOp = "Update";
                    }
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                this.MapDeliveryPackages(itemPM, itemJson, mapParent);
                itemPM.EntityParentPM = null;
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentDeliveries.push(itemPM);
        }
        if (oldCollection) {
            for (var pack in oldCollection) {
                if (entityPM.ShipmentDeliveries.filter(function (p) { return p.UniqueKey === oldCollection[pack].UniqueKey; }).length === 0) {
                    if (oldCollection[pack]) {
                        var oldDeliveryJson = oldCollection[pack];
                        var deletedPM = new ShipmentDeliveryPM_1.ShipmentDeliveryPM(null);
                        var pmKeys = Object.keys(oldDeliveryJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldDeliveryJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        this.MapDeliveryPackages(deletedPM, oldDeliveryJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.ShipmentDeliveries.push(deletedPM);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentFollowups = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldFollowups = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldFollowups = entityPM.OldEntityPM.FollowUps;
        }
        entityPM.FollowUps = new Array();
        for (var pack in jsonPM.FollowUps) {
            var itemJson = jsonPM.FollowUps[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentFollowUpPM_1.ShipmentFollowUpPM(entityPM);
            }
            else { // update mapping
                itemPM = new ShipmentFollowUpPM_1.ShipmentFollowUpPM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.OldEntityPM = this.clone(itemPM);
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty)
                        itemPM.ChangeSetOp = "Update";
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.FollowUps.push(itemPM);
        }
        if (oldFollowups) {
            for (var pack in oldFollowups) {
                if (entityPM.FollowUps.filter(function (p) { return p.UniqueKey === oldFollowups[pack].UniqueKey; }).length === 0) {
                    if (oldFollowups[pack]) {
                        oldFollowups[pack].ChangeSetOp = "Delete";
                        entityPM.FollowUps.push(oldFollowups[pack]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentAssemblies = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldAssemblies = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldAssemblies = entityPM.OldEntityPM.ShipmentAssemblies;
        }
        entityPM.ShipmentAssemblies = new Array();
        for (var pack in jsonPM.ShipmentAssemblies) {
            var itemJson = jsonPM.ShipmentAssemblies[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentAssemblyPM_1.ShipmentAssemblyPM(entityPM);
            }
            else { // update mapping
                itemPM = new ShipmentAssemblyPM_1.ShipmentAssemblyPM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.OldEntityPM = this.clone(itemPM);
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
            }
            else {
                if (itemPM.UniqueKey) {
                    if (itemJson.IsDirty)
                        itemPM.ChangeSetOp = "Update";
                }
                else {
                    itemPM.ChangeSetOp = "Insert";
                }
                itemPM.OldEntityPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentAssemblies.push(itemPM);
        }
        if (oldAssemblies) {
            for (var pack in oldAssemblies) {
                if (entityPM.ShipmentAssemblies.filter(function (p) { return p.UniqueKey === oldAssemblies[pack].UniqueKey; }).length === 0) {
                    if (oldAssemblies[pack]) {
                        oldAssemblies[pack].ChangeSetOp = "Delete";
                        entityPM.ShipmentAssemblies.push(oldAssemblies[pack]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapInsideShipmentPackages = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.InsideShipmentPackages;
        }
        entityPM.InsideShipmentPackages = new Array();
        for (var pack in jsonPM.InsideShipmentPackages) {
            var itemJson = jsonPM.InsideShipmentPackages[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new InsideShipmentPackagePM_1.InsideShipmentPackagePM(entityPM);
            }
            else { // update mapping             
                itemPM = new InsideShipmentPackagePM_1.InsideShipmentPackagePM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
                itemPM.OldEntityPM = this.clone(itemPM);
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    itemPM.ChangeSetOp = "Delete";
                }
                else {
                    if (itemPM.UniqueKey) {
                        if (itemJson.IsDirty) {
                            itemPM.ChangeSetOp = "Update";
                        }
                    }
                    else {
                        itemPM.ChangeSetOp = "Insert";
                    }
                }
                itemPM.OldEntityPM = null;
                itemPM.EntityParentPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.InsideShipmentPackages.push(itemPM);
        }
        if (oldCollection) {
            for (var pack in oldCollection) {
                if (entityPM.InsideShipmentPackages.filter(function (p) { return p.UniqueKey === oldCollection[pack].UniqueKey; }).length === 0) {
                    if (oldCollection[pack]) {
                        oldCollection[pack].ChangeSetOp = "Delete";
                        oldCollection[pack].OldEntityPM = null;
                        entityPM.InsideShipmentPackages.push(oldCollection[pack]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapPickupPackages = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentPickUpDeliveryPackages;
        }
        entityPM.ShipmentPickUpDeliveryPackages = new Array();
        for (var pack in jsonPM.ShipmentPickUpDeliveryPackages) {
            var itemJson = jsonPM.ShipmentPickUpDeliveryPackages[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentPickUpDeliveryPackagePM_1.ShipmentPickUpDeliveryPackagePM(entityPM);
            }
            else { // update mapping             
                itemPM = new ShipmentPickUpDeliveryPackagePM_1.ShipmentPickUpDeliveryPackagePM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
                itemPM.OldEntityPM = this.clone(itemPM);
                this.MapPickUpDeliveryPackageHarmonizes(itemPM, itemJson, mapParent);
                itemPM.OldEntityPM.PickUpDeliveryPackageHarmonizes = [];
                for (var kk3 in itemPM.PickUpDeliveryPackageHarmonizes) {
                    var clonedInside = this.clone(itemPM.PickUpDeliveryPackageHarmonizes[kk3]);
                    itemPM.OldEntityPM.PickUpDeliveryPackageHarmonizes.push(clonedInside);
                }
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    itemPM.ChangeSetOp = "Delete";
                }
                else {
                    if (itemPM.UniqueKey) {
                        if (itemJson.IsDirty) {
                            itemPM.ChangeSetOp = "Update";
                        }
                    }
                    else {
                        itemPM.ChangeSetOp = "Insert";
                    }
                }
                this.MapPickUpDeliveryPackageHarmonizes(itemPM, itemJson, mapParent);
                itemPM.OldEntityPM = null;
                itemPM.EntityParentPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentPickUpDeliveryPackages.push(itemPM);
        }
        if (oldCollection) {
            for (var pack in oldCollection) {
                if (entityPM.ShipmentPickUpDeliveryPackages.filter(function (p) { return p.UniqueKey === oldCollection[pack].UniqueKey; }).length === 0) {
                    if (oldCollection[pack]) {
                        var oldpackageJson = oldCollection[pack];
                        var deletedPM = new ShipmentPickUpDeliveryPackagePM_1.ShipmentPickUpDeliveryPackagePM(null);
                        var pmKeys = Object.keys(oldpackageJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldpackageJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        this.MapPickUpDeliveryPackageHarmonizes(deletedPM, oldpackageJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.ShipmentPickUpDeliveryPackages.push(deletedPM);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapDeliveryPackages = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentPickUpDeliveryPackages;
        }
        entityPM.ShipmentPickUpDeliveryPackages = new Array();
        for (var pack in jsonPM.ShipmentPickUpDeliveryPackages) {
            var itemJson = jsonPM.ShipmentPickUpDeliveryPackages[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentPickUpDeliveryPackagePM_1.ShipmentPickUpDeliveryPackagePM(entityPM);
            }
            else { // update mapping             
                itemPM = new ShipmentPickUpDeliveryPackagePM_1.ShipmentPickUpDeliveryPackagePM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
                itemPM.OldEntityPM = this.clone(itemPM);
                this.MapPickUpDeliveryPackageHarmonizes(itemPM, itemJson, mapParent);
                itemPM.OldEntityPM.PickUpDeliveryPackageHarmonizes = [];
                for (var kk3 in itemPM.PickUpDeliveryPackageHarmonizes) {
                    var clonedInside = this.clone(itemPM.PickUpDeliveryPackageHarmonizes[kk3]);
                    itemPM.OldEntityPM.PickUpDeliveryPackageHarmonizes.push(clonedInside);
                }
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    itemPM.ChangeSetOp = "Delete";
                }
                else {
                    if (itemPM.UniqueKey) {
                        if (itemJson.IsDirty) {
                            itemPM.ChangeSetOp = "Update";
                        }
                    }
                    else {
                        itemPM.ChangeSetOp = "Insert";
                    }
                }
                this.MapPickUpDeliveryPackageHarmonizes(itemPM, itemJson, mapParent);
                itemPM.OldEntityPM = null;
                itemPM.EntityParentPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentPickUpDeliveryPackages.push(itemPM);
        }
        if (oldCollection) {
            for (var pack in oldCollection) {
                if (entityPM.ShipmentPickUpDeliveryPackages.filter(function (p) { return p.UniqueKey === oldCollection[pack].UniqueKey; }).length === 0) {
                    if (oldCollection[pack]) {
                        var oldpackageJson = oldCollection[pack];
                        var deletedPM = new ShipmentPickUpDeliveryPackagePM_1.ShipmentPickUpDeliveryPackagePM(null);
                        var pmKeys = Object.keys(oldpackageJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldpackageJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        this.MapPickUpDeliveryPackageHarmonizes(deletedPM, oldpackageJson, mapParent);
                        deletedPM.OldEntityPM = null;
                        entityPM.ShipmentPickUpDeliveryPackages.push(deletedPM);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentPackageItems = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentPackageItems;
        }
        entityPM.ShipmentPackageItems = new Array();
        for (var pack in jsonPM.ShipmentPackageItems) {
            var itemJson = jsonPM.ShipmentPackageItems[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentPackageItemPM_1.ShipmentPackageItemPM(entityPM);
            }
            else { // update mapping             
                itemPM = new ShipmentPackageItemPM_1.ShipmentPackageItemPM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
                itemPM.OldEntityPM = this.clone(itemPM);
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    itemPM.ChangeSetOp = "Delete";
                }
                else {
                    if (itemPM.UniqueKey) {
                        if (itemJson.IsDirty) {
                            itemPM.ChangeSetOp = "Update";
                        }
                    }
                    else {
                        itemPM.ChangeSetOp = "Insert";
                    }
                }
                itemPM.OldEntityPM = null;
                itemPM.EntityParentPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentPackageItems.push(itemPM);
        }
        if (oldCollection) {
            for (var pack in oldCollection) {
                if (entityPM.ShipmentPackageItems.filter(function (p) { return p.UniqueKey === oldCollection[pack].UniqueKey; }).length === 0) {
                    if (oldCollection[pack]) {
                        oldCollection[pack].ChangeSetOp = "Delete";
                        oldCollection[pack].OldEntityPM = null;
                        entityPM.ShipmentPackageItems.push(oldCollection[pack]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapShipmentPackageHarmonizes = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.ShipmentPackageHarmonizes;
        }
        entityPM.ShipmentPackageHarmonizes = new Array();
        for (var pack in jsonPM.ShipmentPackageHarmonizes) {
            var itemJson = jsonPM.ShipmentPackageHarmonizes[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new ShipmentPackageHarmonizePM_1.ShipmentPackageHarmonizePM(entityPM);
            }
            else { // update mapping             
                itemPM = new ShipmentPackageHarmonizePM_1.ShipmentPackageHarmonizePM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
                itemPM.OldEntityPM = this.clone(itemPM);
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    itemPM.ChangeSetOp = "Delete";
                }
                else {
                    if (itemPM.UniqueKey) {
                        if (itemJson.IsDirty) {
                            itemPM.ChangeSetOp = "Update";
                        }
                    }
                    else {
                        itemPM.ChangeSetOp = "Insert";
                    }
                }
                itemPM.OldEntityPM = null;
                itemPM.EntityParentPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.ShipmentPackageHarmonizes.push(itemPM);
        }
        if (oldCollection) {
            for (var pack in oldCollection) {
                if (entityPM.ShipmentPackageHarmonizes.filter(function (p) { return p.UniqueKey === oldCollection[pack].UniqueKey; }).length === 0) {
                    if (oldCollection[pack]) {
                        oldCollection[pack].ChangeSetOp = "Delete";
                        oldCollection[pack].OldEntityPM = null;
                        entityPM.ShipmentPackageHarmonizes.push(oldCollection[pack]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.MapPickUpDeliveryPackageHarmonizes = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldCollection = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldCollection = entityPM.OldEntityPM.PickUpDeliveryPackageHarmonizes;
        }
        entityPM.PickUpDeliveryPackageHarmonizes = new Array();
        for (var pack in jsonPM.PickUpDeliveryPackageHarmonizes) {
            var itemJson = jsonPM.PickUpDeliveryPackageHarmonizes[pack];
            if (mapParent && (itemJson.ChangeSetOp == "Delete" || itemJson.ChangeSetOp == 3)) {
                continue;
            }
            var itemPM;
            if (mapParent) { // get mapping
                itemPM = new PickUpDeliveryPackageHarmonizePM_1.PickUpDeliveryPackageHarmonizePM(entityPM);
            }
            else { // update mapping             
                itemPM = new PickUpDeliveryPackageHarmonizePM_1.PickUpDeliveryPackageHarmonizePM(null);
            }
            var pmKeys = Object.keys(itemJson);
            for (var key in pmKeys) {
                if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties") {
                    continue;
                }
                var property = pmKeys[key];
                itemPM[property] = itemJson[property];
            }
            if (mapParent) {
                itemPM.UniqueKey = Guid_1.Guid.newGuid();
                itemPM.ChangeSetOp = "None";
                itemJson.ChangeSetOp = "None";
                itemPM.OldEntityPM = this.clone(itemPM);
            }
            else {
                if (entityPM.ChangeSetOp === "Delete") {
                    itemPM.ChangeSetOp = "Delete";
                }
                else {
                    if (itemPM.UniqueKey) {
                        if (itemJson.IsDirty) {
                            itemPM.ChangeSetOp = "Update";
                        }
                    }
                    else {
                        itemPM.ChangeSetOp = "Insert";
                    }
                }
                itemPM.OldEntityPM = null;
                itemPM.EntityParentPM = null;
            }
            itemPM.IsDirty = false;
            entityPM.PickUpDeliveryPackageHarmonizes.push(itemPM);
        }
        if (oldCollection) {
            for (var pack in oldCollection) {
                if (entityPM.PickUpDeliveryPackageHarmonizes.filter(function (p) { return p.UniqueKey === oldCollection[pack].UniqueKey; }).length === 0) {
                    if (oldCollection[pack]) {
                        oldCollection[pack].ChangeSetOp = "Delete";
                        oldCollection[pack].OldEntityPM = null;
                        entityPM.PickUpDeliveryPackageHarmonizes.push(oldCollection[pack]);
                    }
                }
            }
        }
    };
    ShipmentPMService.prototype.ArchiveShipments = function (Ids) {
        var _this = this;
        // Send request
        return Rx_1.Observable.defer(function () {
            // Prepare parameters
            var IdsParameterString = "";
            if (Ids && Ids.length > 0) {
                Ids.forEach(function (el) {
                    IdsParameterString += 'Ids=' + el + '&';
                });
            }
            else {
                console.log("[ERROR] cannot Archive Shipments without Ids!", Ids);
                return;
            }
            var authHeader = new http_1.Headers();
            authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
            authHeader.append('Content-Type', 'application/json');
            var serviceResponse;
            serviceResponse = new ServiceResponse_1.ServiceResponse();
            return _this._http.get(_this._apiUrl + "/GetArchiveShipments/?" + IdsParameterString, { headers: authHeader }).map(function (response) {
                //var res = response.json();
                var serviceResponse = new ServiceResponse_1.ServiceResponse();
                //serviceResponse.Result = res;
                return serviceResponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
    };
    ShipmentPMService.prototype.ArchiveAllShipments = function (filters) {
        var _this = this;
        var urlparameters = '/GetArchiveAllShipments?';
        var mykeys = Object.keys(filters);
        var addtionalFiltersValues = null;
        for (var i in mykeys) {
            var propName = mykeys[i];
            var propValue = filters[propName];
            var ignoreFilter = ((propName.indexOf("Operator") > 0 && propValue == "Equals") || propName == "AdditionalFilters");
            if (urlparameters != "?") {
                urlparameters = urlparameters.concat('&');
            }
            if (!ignoreFilter) {
                propValue = encodeURIComponent(propValue);
                urlparameters = urlparameters.concat(propName.concat('=').concat(propValue));
            }
            if (propName == "AdditionalFilters" && propValue.length > 0)
                addtionalFiltersValues = JSON.stringify(propValue);
        }
        if (addtionalFiltersValues) {
            urlparameters = urlparameters.concat("&AdditionalFilters=").concat(addtionalFiltersValues);
        }
        var authHeader = new http_1.Headers();
        authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
        var callUrl = this._apiUrl.concat(urlparameters); //
        return Rx_1.Observable.defer(function () {
            return _this._http.get(callUrl, {
                headers: authHeader
            }).map(function (response) {
                var serviceResponse = new ServiceResponse_1.ServiceResponse();
                return serviceResponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
    };
    ShipmentPMService.prototype.GetTop100ShipmentIds = function (filters) {
        var _this = this;
        var urlparameters = '/GetTop100ShipmentIds?';
        var mykeys = Object.keys(filters);
        var addtionalFiltersValues = null;
        for (var i in mykeys) {
            var propName = mykeys[i];
            var propValue = filters[propName];
            var ignoreFilter = ((propName.indexOf("Operator") > 0 && propValue == "Equals") || propName == "AdditionalFilters");
            if (urlparameters != "?") {
                urlparameters = urlparameters.concat('&');
            }
            if (!ignoreFilter) {
                propValue = encodeURIComponent(propValue);
                urlparameters = urlparameters.concat(propName.concat('=').concat(propValue));
            }
            if (propName == "AdditionalFilters" && propValue.length > 0)
                addtionalFiltersValues = JSON.stringify(propValue);
        }
        if (addtionalFiltersValues) {
            urlparameters = urlparameters.concat("&AdditionalFilters=").concat(addtionalFiltersValues);
        }
        var authHeader = new http_1.Headers();
        authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
        var callUrl = this._apiUrl.concat(urlparameters); //
        return Rx_1.Observable.defer(function () {
            return _this._http.get(callUrl, {
                headers: authHeader
            }).map(function (response) {
                var Ids = response.json();
                var serviceResponse = new ServiceResponse_1.ServiceResponse();
                serviceResponse.Result = Ids;
                return serviceResponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
    };
    ShipmentPMService.prototype.RemoveShipmentTasks = function (id) {
        var _this = this;
        var authHeader = new http_1.Headers();
        authHeader.append('Token', ServiceHelper_1.ServiceHelper.GetLoggedUserToken());
        //authHeader.append('CallTimKey', ServiceHelper.GetLoggedUserToken());
        //var key = PerformanceLogger.AddLogTime();
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/RemoveShipmentTasks?id=' + id, {
                headers: authHeader
            }).map(function (response) {
                //var servertime = response.headers.get('ServerExecutionTime');
                //PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "Shipment", "GetSinglePM", id);
                //var pm = response.json();
                //var entity: ShipmentPM;
                //if (pm) {
                //    entity = this.MapJsonToEntityPM(pm);
                //}
                var pmresponse;
                pmresponse = new ServiceResponse_1.ServiceResponse();
                pmresponse.Result = response;
                return pmresponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
        // .flatMap((res: Response) => {
        //    var location = res.headers.get('Location');
        //    return this._http.get(location);
        //}).map((res: Response) => res.json()))
        //.catch(this.handleError)
        /*
        .flatMap((res: Response) => {
                var serverTime = res.headers.get('ServerTime');
                return this._http.get(serverTime);
            })
        */
    };
    ShipmentPMService = __decorate([
        core_1.Injectable(),
        __metadata("design:paramtypes", [])
    ], ShipmentPMService);
    return ShipmentPMService;
}());
exports.ShipmentPMService = ShipmentPMService;
//# sourceMappingURL=ShipmentPMService.js.map