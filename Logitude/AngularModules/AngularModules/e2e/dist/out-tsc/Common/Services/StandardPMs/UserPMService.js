"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
//file not found! child composition UserRoles
//file not found! child composition UserPermittedBranch
//file not found! child composition UserPermittedProduct
//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
var core_1 = require("@angular/core");
var http_1 = require("@angular/http");
var Rx_1 = require("rxjs/Rx");
var ServiceResponse_1 = require("../../../Infrastructure/DataContracts/ServiceResponse");
var ClassLevelValidator_1 = require("../../../Infrastructure/Validators/ClassLevelValidator");
var Guid_1 = require("../../../Infrastructure/Utilities/Guid");
var InfraSettings_1 = require("../../../Infrastructure/Utilities/InfraSettings");
var ServiceHelper_1 = require("../../../Infrastructure/Utilities/ServiceHelper");
var SessionInfo_1 = require("../../../Infrastructure/Utilities/SessionInfo");
var CustomFieldClass_1 = require("../../../Infrastructure/DataContracts/CustomFieldClass");
var PerformanceLogger_1 = require("../../../Infrastructure/Utilities/PerformanceLogger");
var UserPM_1 = require("../../EntityPMs/UserPM");
var UserRolesPM_1 = require("../../EntityPMs/UserRolesPM");
var UserPermittedBranchPM_1 = require("../../EntityPMs/UserPermittedBranchPM");
var UserPermittedProductPM_1 = require("../../EntityPMs/UserPermittedProductPM");
var RolePM_1 = require("../../EntityPMs/RolePM");
var UserPMService = /** @class */ (function () {
    function UserPMService() {
        this._http = ServiceHelper_1.ServiceHelper.Http;
        this._apiUrl = ServiceHelper_1.ServiceHelper.GetLogitudeURL() + 'api/users';
    }
    UserPMService.prototype.get = function (id) {
        var _this = this;
        var authHeader = new http_1.Headers();
        authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/getsingle?' + 'id=' + id, {
                headers: authHeader
            }).map(function (response) {
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var serviceResponse;
                serviceResponse = new ServiceResponse_1.ServiceResponse();
                serviceResponse.Result = entity;
                var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "User", "GetSinglePM", 'id=' + id);
                return serviceResponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
    };
    UserPMService.prototype.insert = function (entityPM) {
        var _this = this;
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
            authHeader.append('Content-Type', 'application/json');
            var validator;
            validator = new ClassLevelValidator_1.ClassLevelValidator();
            var errorsArray = validator.Validate("User", entityPM);
            var serviceResponse;
            serviceResponse = new ServiceResponse_1.ServiceResponse();
            if (errorsArray.length == 0) {
                var mappedEntity;
                mappedEntity = _this.MapJsonToEntityPM(entityPM, false);
                return _this._http.post(_this._apiUrl, JSON.stringify(mappedEntity), { headers: authHeader }).map(function (response) {
                    var pm = response.json();
                    if (pm) {
                        var mappedResult;
                        mappedResult = _this.MapJsonToEntityPM(pm, true, entityPM);
                        serviceResponse.Result = mappedResult;
                    }
                    var servertime = response.headers.get('ServerExecutionTime');
                    PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "User", "SaveChanges", "");
                    return serviceResponse;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                serviceResponse.HasError = true;
                serviceResponse.ErrorsArray = errorsArray;
                return Rx_1.Observable.of(serviceResponse);
            }
        });
    };
    UserPMService.prototype.update = function (entityPM) {
        var _this = this;
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
            authHeader.append('Content-Type', 'application/json');
            var validator;
            validator = new ClassLevelValidator_1.ClassLevelValidator();
            var errorsArray = validator.Validate("User", entityPM);
            var serviceResponse;
            serviceResponse = new ServiceResponse_1.ServiceResponse();
            if (errorsArray.length == 0) {
                var mappedEntity;
                mappedEntity = _this.MapJsonToEntityPM(entityPM, false);
                return _this._http.put(_this._apiUrl, JSON.stringify(mappedEntity), { headers: authHeader }).map(function (response) {
                    var pm = response.json();
                    if (pm) {
                        var mappedResult;
                        mappedResult = _this.MapJsonToEntityPM(pm, true, entityPM);
                        serviceResponse.Result = mappedResult;
                    }
                    var servertime = response.headers.get('ServerExecutionTime');
                    PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "User", "SaveChanges", "");
                    return serviceResponse;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                serviceResponse.HasError = true;
                serviceResponse.ErrorsArray = errorsArray;
                return Rx_1.Observable.of(serviceResponse);
            }
        });
    };
    UserPMService.prototype.MapJsonToEntityPM = function (jsonPM, mapParent, entityPM) {
        if (mapParent === void 0) { mapParent = true; }
        if (entityPM === void 0) { entityPM = null; }
        if (!entityPM) {
            entityPM = new UserPM_1.UserPM();
        }
        var customFields = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }
            var property = jsonPMKeys[key];
            if (customFields.indexOf(property) > -1) {
                if (jsonPM[property]) {
                    var customFieldClass = new CustomFieldClass_1.CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
        }
        this.MapRoles(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapUserPermittedBranches(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapUserPermittedProducts(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapRolePMLists(entityPM, jsonPM, mapParent); // Call composition tables map methods
        if (mapParent) {
            entityPM.OldEntityPM = this.clone(entityPM);
            entityPM.OldEntityPM.Roles = [];
            for (var item in entityPM.Roles) {
                var myUserRolesPM = entityPM.Roles[item];
                var newUserRolesPM = this.clone(myUserRolesPM);
                entityPM.OldEntityPM.Roles.push(newUserRolesPM);
            }
            entityPM.OldEntityPM.UserPermittedBranches = [];
            for (var item in entityPM.UserPermittedBranches) {
                var myUserPermittedBranchPM = entityPM.UserPermittedBranches[item];
                var newUserPermittedBranchPM = this.clone(myUserPermittedBranchPM);
                entityPM.OldEntityPM.UserPermittedBranches.push(newUserPermittedBranchPM);
            }
            entityPM.OldEntityPM.UserPermittedProducts = [];
            for (var item in entityPM.UserPermittedProducts) {
                var myUserPermittedProductPM = entityPM.UserPermittedProducts[item];
                var newUserPermittedProductPM = this.clone(myUserPermittedProductPM);
                entityPM.OldEntityPM.UserPermittedProducts.push(newUserPermittedProductPM);
            }
            entityPM.OldEntityPM.RolePMLists = [];
            for (var item in entityPM.RolePMLists) {
                var myRolePM = entityPM.RolePMLists[item];
                var newRolePM = this.clone(myRolePM);
                entityPM.OldEntityPM.RolePMLists.push(newRolePM);
            }
        }
        else {
            entityPM.OldEntityPM = null;
        }
        entityPM.IsDirty = false;
        return entityPM;
    };
    UserPMService.prototype.MapRoles = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldRoles = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldRoles = entityPM.OldEntityPM.Roles;
        }
        entityPM.Roles = new Array();
        for (var item in jsonPM.Roles) {
            var jItem = jsonPM.Roles[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newUserRolesPM;
            if (mapParent) {
                newUserRolesPM = new UserRolesPM_1.UserRolesPM(entityPM);
            }
            else {
                newUserRolesPM = new UserRolesPM_1.UserRolesPM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newUserRolesPM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newUserRolesPM.UniqueKey = Guid_1.Guid.newGuid();
                newUserRolesPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newUserRolesPM.OldEntityPM = this.clone(newUserRolesPM);
                //file not found! child composition UserRoles
            }
            else {
                if (newUserRolesPM.UniqueKey) {
                    if (jItem.IsDirty)
                        newUserRolesPM.ChangeSetOp = "Update";
                }
                else {
                    newUserRolesPM.ChangeSetOp = "Insert";
                }
                //file not found! child composition UserRoles
                newUserRolesPM.OldEntityPM = null;
                newUserRolesPM.EntityParentPM = null;
            }
            newUserRolesPM.IsDirty = false;
            entityPM.Roles.push(newUserRolesPM);
        }
        if (oldRoles) {
            for (var itemKey in oldRoles) {
                if (entityPM.Roles.filter(function (p) { return p.UniqueKey === oldRoles[itemKey].UniqueKey; }).length === 0) {
                    if (oldRoles[itemKey]) {
                        //oldRoles[itemKey].ChangeSetOp = "Delete";
                        //entityPM.Roles.push(oldRoles[itemKey]);
                        var oldItemJson = oldRoles[itemKey];
                        var deletedPM = new UserRolesPM_1.UserRolesPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        //file not found! child composition UserRoles
                        deletedPM.OldEntityPM = null;
                        entityPM.Roles.push(deletedPM);
                    }
                }
            }
        }
    };
    //file not found! for child composition UserRoles
    UserPMService.prototype.MapUserPermittedBranches = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldUserPermittedBranches = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldUserPermittedBranches = entityPM.OldEntityPM.UserPermittedBranches;
        }
        entityPM.UserPermittedBranches = new Array();
        for (var item in jsonPM.UserPermittedBranches) {
            var jItem = jsonPM.UserPermittedBranches[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newUserPermittedBranchPM;
            if (mapParent) {
                newUserPermittedBranchPM = new UserPermittedBranchPM_1.UserPermittedBranchPM(entityPM);
            }
            else {
                newUserPermittedBranchPM = new UserPermittedBranchPM_1.UserPermittedBranchPM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newUserPermittedBranchPM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newUserPermittedBranchPM.UniqueKey = Guid_1.Guid.newGuid();
                newUserPermittedBranchPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newUserPermittedBranchPM.OldEntityPM = this.clone(newUserPermittedBranchPM);
                //file not found! child composition UserPermittedBranch
            }
            else {
                if (newUserPermittedBranchPM.UniqueKey) {
                    if (jItem.IsDirty)
                        newUserPermittedBranchPM.ChangeSetOp = "Update";
                }
                else {
                    newUserPermittedBranchPM.ChangeSetOp = "Insert";
                }
                //file not found! child composition UserPermittedBranch
                newUserPermittedBranchPM.OldEntityPM = null;
                newUserPermittedBranchPM.EntityParentPM = null;
            }
            newUserPermittedBranchPM.IsDirty = false;
            entityPM.UserPermittedBranches.push(newUserPermittedBranchPM);
        }
        if (oldUserPermittedBranches) {
            for (var itemKey in oldUserPermittedBranches) {
                if (entityPM.UserPermittedBranches.filter(function (p) { return p.UniqueKey === oldUserPermittedBranches[itemKey].UniqueKey; }).length === 0) {
                    if (oldUserPermittedBranches[itemKey]) {
                        //oldUserPermittedBranches[itemKey].ChangeSetOp = "Delete";
                        //entityPM.UserPermittedBranches.push(oldUserPermittedBranches[itemKey]);
                        var oldItemJson = oldUserPermittedBranches[itemKey];
                        var deletedPM = new UserPermittedBranchPM_1.UserPermittedBranchPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        //file not found! child composition UserPermittedBranch
                        deletedPM.OldEntityPM = null;
                        entityPM.UserPermittedBranches.push(deletedPM);
                    }
                }
            }
        }
    };
    //file not found! for child composition UserPermittedBranch
    UserPMService.prototype.MapUserPermittedProducts = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldUserPermittedProducts = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldUserPermittedProducts = entityPM.OldEntityPM.UserPermittedProducts;
        }
        entityPM.UserPermittedProducts = new Array();
        for (var item in jsonPM.UserPermittedProducts) {
            var jItem = jsonPM.UserPermittedProducts[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newUserPermittedProductPM;
            if (mapParent) {
                newUserPermittedProductPM = new UserPermittedProductPM_1.UserPermittedProductPM(entityPM);
            }
            else {
                newUserPermittedProductPM = new UserPermittedProductPM_1.UserPermittedProductPM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newUserPermittedProductPM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newUserPermittedProductPM.UniqueKey = Guid_1.Guid.newGuid();
                newUserPermittedProductPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newUserPermittedProductPM.OldEntityPM = this.clone(newUserPermittedProductPM);
                //file not found! child composition UserPermittedProduct
            }
            else {
                if (newUserPermittedProductPM.UniqueKey) {
                    if (jItem.IsDirty)
                        newUserPermittedProductPM.ChangeSetOp = "Update";
                }
                else {
                    newUserPermittedProductPM.ChangeSetOp = "Insert";
                }
                //file not found! child composition UserPermittedProduct
                newUserPermittedProductPM.OldEntityPM = null;
                newUserPermittedProductPM.EntityParentPM = null;
            }
            newUserPermittedProductPM.IsDirty = false;
            entityPM.UserPermittedProducts.push(newUserPermittedProductPM);
        }
        if (oldUserPermittedProducts) {
            for (var itemKey in oldUserPermittedProducts) {
                if (entityPM.UserPermittedProducts.filter(function (p) { return p.UniqueKey === oldUserPermittedProducts[itemKey].UniqueKey; }).length === 0) {
                    if (oldUserPermittedProducts[itemKey]) {
                        //oldUserPermittedProducts[itemKey].ChangeSetOp = "Delete";
                        //entityPM.UserPermittedProducts.push(oldUserPermittedProducts[itemKey]);
                        var oldItemJson = oldUserPermittedProducts[itemKey];
                        var deletedPM = new UserPermittedProductPM_1.UserPermittedProductPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        //file not found! child composition UserPermittedProduct
                        deletedPM.OldEntityPM = null;
                        entityPM.UserPermittedProducts.push(deletedPM);
                    }
                }
            }
        }
    };
    //file not found! for child composition UserPermittedProduct
    UserPMService.prototype.MapRolePMLists = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        entityPM.RolePMLists = new Array();
        for (var item in jsonPM.RolePMLists) {
            var jItem = jsonPM.RolePMLists[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newRolePM;
            newRolePM = new RolePM_1.RolePM();
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newRolePM[pmProperty] = jItem[pmProperty];
            }
            newRolePM.IsDirty = false;
            entityPM.RolePMLists.push(newRolePM);
        }
    };
    UserPMService.prototype.clone = function (jsonPM) {
        var entityPM;
        entityPM = {};
        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }
            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];
        }
        return entityPM;
    };
    UserPMService.prototype.GetNewEntityPM = function () {
        var entityPM;
        entityPM = new UserPM_1.UserPM();
        entityPM.Tenant = InfraSettings_1.InfraSettings.TenantPM.Id;
        return entityPM;
    };
    UserPMService = __decorate([
        core_1.Injectable(),
        __metadata("design:paramtypes", [])
    ], UserPMService);
    return UserPMService;
}());
exports.UserPMService = UserPMService;
//# sourceMappingURL=UserPMService.js.map