"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
//file not found! child composition TarrifFromTo
//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a logitude.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
var core_1 = require("@angular/core");
var http_1 = require("@angular/http");
var Rx_1 = require("rxjs/Rx");
var ServiceResponse_1 = require("../../../Infrastructure/DataContracts/ServiceResponse");
var ClassLevelValidator_1 = require("../../../Infrastructure/Validators/ClassLevelValidator");
var Guid_1 = require("../../../Infrastructure/Utilities/Guid");
var InfraSettings_1 = require("../../../Infrastructure/Utilities/InfraSettings");
var ServiceHelper_1 = require("../../../Infrastructure/Utilities/ServiceHelper");
var SessionInfo_1 = require("../../../Infrastructure/Utilities/SessionInfo");
var CustomFieldClass_1 = require("../../../Infrastructure/DataContracts/CustomFieldClass");
var PerformanceLogger_1 = require("../../../Infrastructure/Utilities/PerformanceLogger");
var TarrifHeaderPM_1 = require("../../EntityPMs/TarrifHeaderPM");
var TarrifChargePM_1 = require("../../EntityPMs/TarrifChargePM");
var TarrifFromToPM_1 = require("../../EntityPMs/TarrifFromToPM");
var TarrifHeaderPMService = /** @class */ (function () {
    function TarrifHeaderPMService() {
        this._http = ServiceHelper_1.ServiceHelper.Http;
        this._apiUrl = ServiceHelper_1.ServiceHelper.GetLogitudeURL() + 'api/tarrifheaders';
    }
    TarrifHeaderPMService.prototype.get = function (id) {
        var _this = this;
        var authHeader = new http_1.Headers();
        authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            return _this._http.get(_this._apiUrl + '/getsingle?' + 'id=' + id, {
                headers: authHeader
            }).map(function (response) {
                var pm = response.json();
                var entity;
                if (pm) {
                    entity = _this.MapJsonToEntityPM(pm);
                }
                var serviceResponse;
                serviceResponse = new ServiceResponse_1.ServiceResponse();
                serviceResponse.Result = entity;
                var servertime = response.headers.get('ServerExecutionTime');
                PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "TarrifHeader", "GetSinglePM", 'id=' + id);
                return serviceResponse;
            }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
        });
    };
    TarrifHeaderPMService.prototype.insert = function (entityPM) {
        var _this = this;
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
            authHeader.append('Content-Type', 'application/json');
            var validator;
            validator = new ClassLevelValidator_1.ClassLevelValidator();
            var errorsArray = validator.Validate("TarrifHeader", entityPM);
            var serviceResponse;
            serviceResponse = new ServiceResponse_1.ServiceResponse();
            if (errorsArray.length == 0) {
                var mappedEntity;
                mappedEntity = _this.MapJsonToEntityPM(entityPM, false);
                return _this._http.post(_this._apiUrl, JSON.stringify(mappedEntity), { headers: authHeader }).map(function (response) {
                    var pm = response.json();
                    if (pm) {
                        var mappedResult;
                        mappedResult = _this.MapJsonToEntityPM(pm, true, entityPM);
                        serviceResponse.Result = mappedResult;
                    }
                    var servertime = response.headers.get('ServerExecutionTime');
                    PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "TarrifHeader", "SaveChanges", "");
                    return serviceResponse;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                serviceResponse.HasError = true;
                serviceResponse.ErrorsArray = errorsArray;
                return Rx_1.Observable.of(serviceResponse);
            }
        });
    };
    TarrifHeaderPMService.prototype.update = function (entityPM) {
        var _this = this;
        var callTime = new Date();
        return Rx_1.Observable.defer(function () {
            var authHeader = new http_1.Headers();
            authHeader.append('Token', SessionInfo_1.SessionInfo.Token);
            authHeader.append('Content-Type', 'application/json');
            var validator;
            validator = new ClassLevelValidator_1.ClassLevelValidator();
            var errorsArray = validator.Validate("TarrifHeader", entityPM);
            var serviceResponse;
            serviceResponse = new ServiceResponse_1.ServiceResponse();
            if (errorsArray.length == 0) {
                var mappedEntity;
                mappedEntity = _this.MapJsonToEntityPM(entityPM, false);
                return _this._http.put(_this._apiUrl, JSON.stringify(mappedEntity), { headers: authHeader }).map(function (response) {
                    var pm = response.json();
                    if (pm) {
                        var mappedResult;
                        mappedResult = _this.MapJsonToEntityPM(pm, true, entityPM);
                        serviceResponse.Result = mappedResult;
                    }
                    var servertime = response.headers.get('ServerExecutionTime');
                    PerformanceLogger_1.PerformanceLogger.InsertPerformanceLog(callTime, new Date(), Number(servertime), "TarrifHeader", "SaveChanges", "");
                    return serviceResponse;
                }).catch(ServiceHelper_1.ServiceHelper.HandleServiceError);
            }
            else {
                serviceResponse.HasError = true;
                serviceResponse.ErrorsArray = errorsArray;
                return Rx_1.Observable.of(serviceResponse);
            }
        });
    };
    TarrifHeaderPMService.prototype.MapJsonToEntityPM = function (jsonPM, mapParent, entityPM) {
        if (mapParent === void 0) { mapParent = true; }
        if (entityPM === void 0) { entityPM = null; }
        if (!entityPM) {
            entityPM = new TarrifHeaderPM_1.TarrifHeaderPM();
        }
        var customFields = [];
        for (var i = 1; i < 11; i++) {
            customFields.push("Field" + i);
        }
        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            if (jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }
            var property = jsonPMKeys[key];
            if (customFields.indexOf(property) > -1) {
                if (jsonPM[property]) {
                    var customFieldClass = new CustomFieldClass_1.CustomFieldClass(jsonPM[property].Value, jsonPM[property].FieldName, jsonPM[property].TableName);
                    entityPM[property] = customFieldClass;
                }
            }
            else {
                entityPM[property] = jsonPM[property];
            }
        }
        this.MapTarrifCharges(entityPM, jsonPM, mapParent); // Call composition tables map methods
        this.MapTarrifFromToes(entityPM, jsonPM, mapParent); // Call composition tables map methods
        if (mapParent) {
            entityPM.OldEntityPM = this.clone(entityPM);
            entityPM.OldEntityPM.TarrifCharges = [];
            for (var item in entityPM.TarrifCharges) {
                var myTarrifChargePM = entityPM.TarrifCharges[item];
                var newTarrifChargePM = this.clone(myTarrifChargePM);
                entityPM.OldEntityPM.TarrifCharges.push(newTarrifChargePM);
            }
            entityPM.OldEntityPM.TarrifFromToes = [];
            for (var item in entityPM.TarrifFromToes) {
                var myTarrifFromToPM = entityPM.TarrifFromToes[item];
                var newTarrifFromToPM = this.clone(myTarrifFromToPM);
                entityPM.OldEntityPM.TarrifFromToes.push(newTarrifFromToPM);
            }
        }
        else {
            entityPM.OldEntityPM = null;
        }
        entityPM.IsDirty = false;
        return entityPM;
    };
    TarrifHeaderPMService.prototype.MapTarrifCharges = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldTarrifCharges = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldTarrifCharges = entityPM.OldEntityPM.TarrifCharges;
        }
        entityPM.TarrifCharges = new Array();
        for (var item in jsonPM.TarrifCharges) {
            var jItem = jsonPM.TarrifCharges[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newTarrifChargePM;
            if (mapParent) {
                newTarrifChargePM = new TarrifChargePM_1.TarrifChargePM(entityPM);
            }
            else {
                newTarrifChargePM = new TarrifChargePM_1.TarrifChargePM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newTarrifChargePM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newTarrifChargePM.UniqueKey = Guid_1.Guid.newGuid();
                newTarrifChargePM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newTarrifChargePM.OldEntityPM = this.clone(newTarrifChargePM);
            }
            else {
                if (newTarrifChargePM.UniqueKey) {
                    if (jItem.IsDirty)
                        newTarrifChargePM.ChangeSetOp = "Update";
                }
                else {
                    newTarrifChargePM.ChangeSetOp = "Insert";
                }
                newTarrifChargePM.OldEntityPM = null;
                newTarrifChargePM.EntityParentPM = null;
            }
            newTarrifChargePM.IsDirty = false;
            entityPM.TarrifCharges.push(newTarrifChargePM);
        }
        if (oldTarrifCharges) {
            for (var itemKey in oldTarrifCharges) {
                if (entityPM.TarrifCharges.filter(function (p) { return p.UniqueKey === oldTarrifCharges[itemKey].UniqueKey; }).length === 0) {
                    if (oldTarrifCharges[itemKey]) {
                        //oldTarrifCharges[itemKey].ChangeSetOp = "Delete";
                        //entityPM.TarrifCharges.push(oldTarrifCharges[itemKey]);
                        var oldItemJson = oldTarrifCharges[itemKey];
                        var deletedPM = new TarrifChargePM_1.TarrifChargePM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        deletedPM.OldEntityPM = null;
                        entityPM.TarrifCharges.push(deletedPM);
                    }
                }
            }
        }
    };
    TarrifHeaderPMService.prototype.MapTarrifFromToes = function (entityPM, jsonPM, mapParent) {
        if (mapParent === void 0) { mapParent = true; }
        var oldTarrifFromToes = [];
        if (entityPM.OldEntityPM && !mapParent) {
            oldTarrifFromToes = entityPM.OldEntityPM.TarrifFromToes;
        }
        entityPM.TarrifFromToes = new Array();
        for (var item in jsonPM.TarrifFromToes) {
            var jItem = jsonPM.TarrifFromToes[item];
            if (mapParent && (jItem.ChangeSetOp == "Delete" || jItem.ChangeSetOp == 3)) {
                continue;
            }
            var newTarrifFromToPM;
            if (mapParent) {
                newTarrifFromToPM = new TarrifFromToPM_1.TarrifFromToPM(entityPM);
            }
            else {
                newTarrifFromToPM = new TarrifFromToPM_1.TarrifFromToPM(null);
            }
            var pmKeysArray = Object.keys(jItem);
            for (var pmKey in pmKeysArray) {
                if ((!mapParent && pmKeysArray[pmKey] === "entityParentPM") || pmKeysArray[pmKey] === "UIProperties" || pmKeysArray[pmKey] === "PropertyChanged") {
                    continue;
                }
                var pmProperty = pmKeysArray[pmKey];
                newTarrifFromToPM[pmProperty] = jItem[pmProperty];
            }
            if (mapParent) {
                newTarrifFromToPM.UniqueKey = Guid_1.Guid.newGuid();
                newTarrifFromToPM.ChangeSetOp = "None";
                jItem.ChangeSetOp = "None";
                newTarrifFromToPM.OldEntityPM = this.clone(newTarrifFromToPM);
                //file not found! child composition TarrifFromTo
            }
            else {
                if (newTarrifFromToPM.UniqueKey) {
                    if (jItem.IsDirty)
                        newTarrifFromToPM.ChangeSetOp = "Update";
                }
                else {
                    newTarrifFromToPM.ChangeSetOp = "Insert";
                }
                //file not found! child composition TarrifFromTo
                newTarrifFromToPM.OldEntityPM = null;
                newTarrifFromToPM.EntityParentPM = null;
            }
            newTarrifFromToPM.IsDirty = false;
            entityPM.TarrifFromToes.push(newTarrifFromToPM);
        }
        if (oldTarrifFromToes) {
            for (var itemKey in oldTarrifFromToes) {
                if (entityPM.TarrifFromToes.filter(function (p) { return p.UniqueKey === oldTarrifFromToes[itemKey].UniqueKey; }).length === 0) {
                    if (oldTarrifFromToes[itemKey]) {
                        //oldTarrifFromToes[itemKey].ChangeSetOp = "Delete";
                        //entityPM.TarrifFromToes.push(oldTarrifFromToes[itemKey]);
                        var oldItemJson = oldTarrifFromToes[itemKey];
                        var deletedPM = new TarrifFromToPM_1.TarrifFromToPM(null);
                        var pmKeys = Object.keys(oldItemJson);
                        for (var key in pmKeys) {
                            if ((!mapParent && pmKeys[key] === "entityParentPM") || pmKeys[key] === "UIProperties" || pmKeys[key] === "OldEntityPM" || pmKeys[key] === "PropertyChanged") {
                                continue;
                            }
                            var property = pmKeys[key];
                            deletedPM[property] = oldItemJson[property];
                        }
                        deletedPM.IsDirty = false;
                        deletedPM.ChangeSetOp = "Delete";
                        //file not found! child composition TarrifFromTo
                        deletedPM.OldEntityPM = null;
                        entityPM.TarrifFromToes.push(deletedPM);
                    }
                }
            }
        }
    };
    //file not found! for child composition TarrifFromTo
    TarrifHeaderPMService.prototype.clone = function (jsonPM) {
        var entityPM;
        entityPM = {};
        var jsonPMKeys = Object.keys(jsonPM);
        for (var key in jsonPMKeys) {
            if ((jsonPMKeys[key] === "entityParentPM") || jsonPMKeys[key] === "UIProperties" || jsonPMKeys[key] === "OldEntityPM" || jsonPMKeys[key] === "PropertyChanged") {
                continue;
            }
            var property = jsonPMKeys[key];
            entityPM[property] = jsonPM[property];
        }
        return entityPM;
    };
    TarrifHeaderPMService.prototype.GetNewEntityPM = function () {
        var entityPM;
        entityPM = new TarrifHeaderPM_1.TarrifHeaderPM();
        entityPM.Tenant = InfraSettings_1.InfraSettings.TenantPM.Id;
        return entityPM;
    };
    TarrifHeaderPMService = __decorate([
        core_1.Injectable(),
        __metadata("design:paramtypes", [])
    ], TarrifHeaderPMService);
    return TarrifHeaderPMService;
}());
exports.TarrifHeaderPMService = TarrifHeaderPMService;
//# sourceMappingURL=TarrifHeaderPMService.js.map