export class SharedLogisticsSettingPM {
    Tenant: number;

    IsAgentShared: boolean;

    IsShipperNotExporterShared: boolean;

    IsNotify1Shared: boolean;

    IsNotify2Shared: boolean;

    IsFreightForwarderShared: boolean;

    IsColoaderShared: boolean;

    IsConsigneeNotImporterShared: boolean;

    IsMainCarrierShared: boolean;

    IsPickDelivCarriesShared: boolean;

    IsInvoicesMenuEnabled: boolean;

    IsMoneyTabEnabled: boolean;

    Id: string;

    IsIssuingCarrierAgentShared: boolean;

    IsCustomsAgentExportShared: boolean;

    IsCustomsAgentImportShared: boolean;

    IsCustomClearancePoinShared: boolean;

    IsConsolidatorShared: boolean;

    IsReleasingAgentShared: boolean;

    IsShipperShared: boolean;

    IsConsigneeShared: boolean;

    IsShowAmountLocalCurrency: boolean;

    IsShipperShowContactTS: boolean;

    IsConsigneeShowContactTS: boolean;

    IsAgentShowContactTS: boolean;

    IsShipperNotExShowContactTS: boolean;

    IsConsigneeNotImShowContactTS: boolean;

    IsNotify1ShowContactTS: boolean;

    IsNotify2ShowContactTS: boolean;

    IsFreightForwardShowContactTS: boolean;

    IsColoaderShowContactTS: boolean;

    IsCustomAgentExShowContactTS: boolean;

    IsCustomAgentImShowContactTS: boolean;

    IsCustomCleaPointShowContactTS: boolean;

    IsConsolidatorShowContactTS: boolean;

    IsReleasingAgentShowContactTS: boolean;

    IsIssuingCarAgentShowContactTS: boolean;

    IsCustomerShared: boolean;

    IsCustomerShowContactTS: boolean;

    IsAccountManagerShared: boolean;

    IsAccountManagerShowContactTS: boolean;

    IsSalesmanShared: boolean;

    IsSalesmanShowContactTS: boolean;

    IsCollectorShared: boolean;

    IsCollectorShowContactTS: boolean;

    IsMainCarShowContactTS: boolean;
    
    IsPickDelivCarShowContactTS: boolean;
}
